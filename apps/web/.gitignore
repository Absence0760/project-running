# dependencies
node_modules/

# build output
.svelte-kit/
build/
dist/

# environment
.env
.env.*
!.env.example
# Committed local-dev defaults — loaded only by `vite dev`, never `vite build`
# (local-stack + public demo keys, no real secrets). See README "Quick start".
!.env.development

# OS
.DS_Store

# editor
.vscode/
.idea/
