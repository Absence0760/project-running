# custom_watch — tier-1 bench-prototype workspace

Rust + Embassy firmware for the ultra-marathon watch research effort. This is *only* the active workspace + per-step build status; the broader research (strategy, BOM, cost tiers, firmware architecture, performance path, parts list) lives under [`docs/custom_watch/`](../../docs/custom_watch/README.md). See also [`CLAUDE.md`](CLAUDE.md) in this directory for the scope rules.

## Status

**Workspace scaffolded 2026-05-28 — no parts yet, no on-board verification yet.** The firmware runs end-to-end under the Renode simulator (`bin/watch-sim.sh`, 2026-07-08): GPS fixes parse from a canned NMEA feed, the status face renders on an emulated Sharp MIP (`--gui` opens the live screen), and phone-link status frames stream over a TCP bridge to the mobile app's dev Sim Watch screen. Steps 3 + 4 below are code-done and sim-verified; on-board verification of everything still lands with the dev kit. The host-testable logic behind steps 5 + 7 also landed 2026-07-08 as pure `no_std` crates ahead of the peripherals: the MAX86177 register driver + a float-free PPG→BPM peak detector (`drivers/max86177/`), a port of the Dart `run_recorder` recording state machine (`watch_core::record`), and the barometric-altitude + cumulative-vert accumulator the BMP581 task consumes (`watch_core::elevation`), plus the `bmp581` register driver — all `cargo test`-green on the host and compiling for the MCU target. Their app-task wiring landed the same day: the `hr`, `record`, and `baro` tasks now run under the sim — HR and baro park cleanly when the part is absent (an async timeout-probe keeps the blocking driver from stalling the executor), while the recorder accumulates distance from the live fix feed and the face shows a live recording line. Bench verification still pends parts. [decisions.md §71](../../docs/architecture/decisions.md#71-own-hardware-an-ultra-marathon-watch-stays-research-only-watch-development-is-deferred-indefinitely) was amended 2026-05-28 to allow owner-personal tier-1 work in evenings/weekends. Tier 2+ (PCB CAD, case CAD, RF consultant spend, ODM conversations) remain gated on the three triggers in §71. The Cargo workspace at this directory is in place per [§ 80](../../docs/architecture/decisions.md#80-tier-1-firmware-uses-embassy-on-rust-on-the-nordic-nrf52840--chosen-for-memory-safety-tooling-and-async-ergonomics-not-for-performance); first flash (toolchain + blink-LED end-to-end against a real nRF52840 DK) lands when the dev kit arrives — see [`parts.md`](../../docs/custom_watch/parts.md).

**Language + framework decided: Rust + Embassy on the Nordic nRF52840.** See [decisions.md §80](../../docs/architecture/decisions.md#80-tier-1-firmware-uses-embassy-on-rust-on-the-nordic-nrf52840--chosen-for-memory-safety-tooling-and-async-ergonomics-not-for-performance) for why (short version: memory safety + tooling + async ergonomics, *not* perf — Rust vs C is within ±5% on this class of MCU). See [`docs/custom_watch/performance_path.md`](../../docs/custom_watch/performance_path.md) for where the performance levers actually live, and [`docs/custom_watch/competitive_landscape.md`](../../docs/custom_watch/competitive_landscape.md) for the strategic framing of why tier 1 exists at all.

## Power discipline

Per [`performance_path.md`](../../docs/custom_watch/performance_path.md), battery life is decided by **hardware** (Sharp MIP vs AMOLED ~3–4×, Ambiq vs nRF52 MCU ~25×, snapshot GNSS ~2–3×) — all tier-2 chip swaps — and the nRF52840 **dev kit can't measure ultra-watch power at all** (~30 mA idle from the onboard debugger). So tier-1's job is not a measured number; it's getting the **firmware architecture** right so the wins land at tier-2: DMA/interrupt-driven I/O, a tickless async runtime, aggressive sleep where every wake is justifiable, partial display updates, and a clean always-on/screen-on split. What that translates to in this workspace:

**Done:**
- **Partial display updates** — the `sharp_mip` driver tracks dirty lines and sends only changed ones (+ the VCOM-only frame when idle); a static screen costs near-nothing.
- **DMA/async, tickless** — every task is Embassy async over EasyDMA (no busy-polling), so the executor sleeps until a real event. **GPS UART reads are burst-buffered** (`gps.rs`, `RX_BURST`) — one CPU wake per ~32 bytes instead of per byte, the doc's DMA-not-polling lever (sim-verified: 53 fixes / 51 s, distance accrues).
- **Animations gated** — the ~1 Hz face animations freeze outside an ~8 s post-interaction window (`ANIM_WINDOW_S`), so an unattended wrist stops the per-second decorative redraw.
- **No gratuitous wakers** — the 2 Hz liveness LED is behind the default-OFF `dev-blink` feature (the sim/flash helpers opt it back in); a plain `cargo build` carries no free-running blinker.
- **BLE long connection interval** — the `ble` build requests a ~1 s interval on connect (`set_conn_params`), vs the 7.5 ms HID default (~100× idle-radio power). Compile-verified only.
- **Hardware EXTCOMIN VCOM** — EXTMODE held high + a free-running ~3.8 Hz PWM on EXTCOMIN (`ui::vcom_task`) drive the panel's bias inversion in hardware, so the display driver (`SharpMip::new_external_vcom`) never toggles the software VCOM bit and never sends a VCOM-maintenance frame: a clean framebuffer now flushes **zero SPI**. This removes the display's standing reason for the CPU to wake every second. Sim-verified to boot + init the PWM + still render the dashboard; the EXTMODE/EXTCOMIN wiring itself is bench-gated (some breakouts strap EXTMODE — confirm on the real panel).
- **Idle CPU-sleep** — building on hardware VCOM: the screen task is **event-driven** (it waits on the state Watches and re-renders only on a change, with a short tick only while animating or watching a fresh fix go stale, else a 30 s heartbeat); the idle-face title row is **static** (no ticking uptime); and the record task **drops its 1 Hz tick while idle/finished** and publishes a snapshot only when it changed. So a resting recorder wakes nothing downstream, and with no sensor events the CPU sleeps seconds apart instead of every second. Sim-verified: recording still drives the dashboard through events — the elapsed hero advances and distance accrues with no per-second timer.
- **GPS fix de-rate while idle** — the `gps` task now watches `state::RECORD` and, while **no run is active**, forwards at most one fix per `IDLE_FIX_MIN_INTERVAL_S` (held one second under the face's `STALE_AFTER_S` freshness budget) instead of every ~1 s fix. A moving idle position used to wake the UI, record, and phone consumers every second for a view nobody is recording; a run (Recording/Paused) lifts the throttle and gets every fix. This is the *software* half of GPS duty-cycling — it cuts the downstream wakes, not the UART stream itself. Sim-verified both ways: the idle (no-autostart) build publishes fixes ~4 s apart with the position still advancing (face stays fresh, never falsely "searching"), while the recording build gets full-rate fixes and distance accrues normally.
- **Selectable GNSS recording modes** (2026-07-10) — the de-rate above, generalised into the mode surface every ultra watch ships: `watch_core::gnss_mode` defines **Performance / Balanced / Expedition** (every ~1 s fix / one per 15 s / one per 60 s — the UltraTrac / UltraMax class), each with a *projected* battery-hours figure (~110/~180/~220 h; a derivation in the module docs, not a measurement — the DK can't measure power). **BTN3 cycles the mode on the idle face** (`MODE PERF ~110H` row; mid-run BTN3 stays on pages via the host-tested `btn3_action`, so a run's mode is frozen), the gps task forwards fixes at the mode's cadence while recording, the recorder's jump gate scales to the interval (`MAX_SPEED_MPS * dt` past 1 s — a fixed 100 m ceiling would reject every legitimate ~240 m Expedition leg; the 1 Hz filter is unchanged), and the face's staleness budget stretches to the cadence (`face::stale_after_s`) so a 40 s-old Expedition fix reads `SATS`, not `STALE` — run-view GPS rows carry the mode tag. Sim-verified with `bin/watch-sim.sh --no-autostart` (boot idle, cycle the mode, BTN1 to start): forwarded-fix cadence measured ~1 s / ~15 s / ~60 s per mode with distance accruing in all three. Still only the *software* half — the tier-2 receiver power-down below is where the mode's real battery win lands.

**Architecturally owed (tier-1 wins, but hardware-gated or behaviour-changing — not yet built):**
- **GNSS power-down between fixes** — the fix de-rate / recording modes above throttle *publication*; the module's UART still streams and the CPU still wakes per burst to drain it. The deeper win powers the GNSS down (u-blox power-save / backup mode, or a load switch) between the selected mode's fixes and wakes it on demand — a behaviour/mode change plus module power/wake wiring that the Renode sim can't model, and the tier-2 snapshot-GNSS lever. The `gnss_mode` surface is the user-facing half this plugs into; its projected battery figures assume this lever lands. Same shape as HR duty-cycling below.
- **HR duty-cycling** — sample HR a few seconds per minute rather than 100 Hz continuously (the LED current dominates HR power); a mode/fidelity trade to make deliberately.
- **Always-on / screen-on split for tier-2** — the sensor tasks are already isolated per subsystem, the boundary a coprocessor (Apollo510B network core / BHI260) slots into at tier-2.

## Local testing

The watch's developer inner loop is `bin/watch-flash.sh` from the repo root (or `cargo run --release` from this directory): build the firmware, flash it to a connected Nordic nRF52840 DK, stream `defmt` logs back to your terminal until Ctrl-C. Host-side unit tests run via `bin/watch-test.sh` or `cargo test` and don't need a board. Run `bin/watch-doctor.sh` once per machine to verify toolchain + board detection. No board at all? `bin/watch-sim.sh` boots the same release ELF on an emulated nRF52840 DK (Renode) with decoded defmt logs streaming and a canned NMEA fixture feeding the GPS UART — the whole boot + blink + task-scheduling path runs today, pre-parts ([decisions.md § 208](../../docs/architecture/decisions.md#208-firmware-simulation-runs-the-unmodified-elf-on-renode-with-a-custom-defmt-rtt-drain)). See [`local_testing.md`](local_testing.md) for the full setup walkthrough, the host-vs-on-target-vs-sim testing split, and the common-error reference.

## Next steps

1. **Order parts** — see [`docs/custom_watch/parts.md`](../../docs/custom_watch/parts.md). Total ~$300 for the MCU + sensor breakouts + battery + breadboard, plus ~$200–$900 in bench tools depending on which soldering iron / multimeter / logic analyzer tier you pick.
2. **Scaffold the Cargo workspace** — **DONE (2026-05-28).** `Cargo.toml`, `rust-toolchain.toml`, `.cargo/config.toml` with probe-rs runner, `app/` crate with Embassy executor + blink-LED stub task, stub modules for each subsystem, driver crates (`sharp_mip`, `ublox_nmea`, `max86177`) as `no_std` stubs, board crate (`nrf52840_dk`) with DK pin assignments, VS Code launch + tasks for in-IDE debug. `nrf-softdevice` is **not** in deps yet — it adds at step 6 with BLE and requires bumping `memory.x` to leave room for the SoftDevice. First flash pending parts arrival.
3. **GNSS bring-up** — **CODE DONE, SIM-VERIFIED (2026-07-08); bench verification pending parts.** `ublox_nmea` streaming parser (RMC/GGA, checksummed, 10 host tests) + the `gps` task reading UARTE0, publishing merged fixes through `watch_core::fix` into an embassy-sync `Watch`. The Renode sim feeds `sim/nmea/bench_jog.nmea` and fixes flow end-to-end.
4. **Display bring-up** — **CODE DONE, SIM-VERIFIED (2026-07-08); bench verification pending parts.** `sharp_mip` driver (framebuffer + dirty-line tracking + generated 8x16 font + a generated 16x16 icon table + the Sharp line-update wire protocol, 13 host tests) on SPIM3, and the `ui` screen task rendering the `watch_core::face` rows at 1 Hz. The face has two layouts (`watch_core::face`, host-tested): an **idle status face** (uptime clock, GPS status, last-known position, speed, altitude, HR, vert — the bench / acquisition view) and, once a run is under way, a **run dashboard** — a 2x-scale elapsed-time **hero** across the top two rows (pixel-doubled from the 8x16 font by `Framebuffer::draw_text_2x`, drawn from `face::hero_line`, with the blinking state tag tucked top-right), then distance, average + current pace, HR, altitude, and cumulative vert, each metric row carrying a 16x16 gutter icon (footsteps / heart / mountain / vert-arrows / satellite) with the values stacked in one glanceable column and raw lat/lon dropped. The icons are authored as editable SVG under `drivers/sharp_mip/icons/`, rasterised to the committed 1-bit `src/icons.rs` table by `scripts/gen_icons.py` (Inkscape → ImageMagick → packed Rust, same discipline as `gen_font.py`), and blitted by `Framebuffer::draw_icon`. Three ~1 Hz animations ride the existing per-second render tick as pure functions of `uptime_s` (so frame choice stays host-tested, not a hidden timer): the **REC** tag blinks while recording, the HR heart pulses (big/small frames) while a pulse is detected, and the GPS satellite cycles growing search arcs (`sat_search0/1` → `satellite`) until a fresh fix locks. To spare the display on a multi-day run, the animations are **gated to an ~8 s window after each button press** (`ANIM_WINDOW_S`): outside it every animated element holds a steady frame, so an unattended wrist stops paying the per-second redraw. This is the only display power lever that matters here — the panel is a reflective Memory LCD with no backlight, so its draw is dominated by VCOM refresh + per-line SPI updates and is independent of the black/white pixel ratio (there is no "dark mode saves power" as on OLED); fewer updates, not darker pixels, is the win. The gate is a pure `animate: bool` on `face::page_rows` / `page_icons`, fed by a `state::INTERACTION` timestamp the `button` task stamps on every press. The run view is **paged** (`watch_core::page::Page`, host-tested; 16 pages as of 2026-07-11): **BTN3** cycles Dashboard → Distance → Pace → Lap → Splits → Zones → Pacer → RacePredictor → TrainingLoad → DistanceBand → CutoffEta → Roadbook → Fuel → Nav → Back-to-start → GearWear, where the glance pages put one metric up large as the 2x hero with the rest as 1x context (the Pace page pairs the average-pace hero with a live **grade-adjusted pace** row — see step 7; the Lap page: current lap time up large, with lap number / last-lap split / lap distance / HR / GPS below; the Zones page: live BPM up large, the current HR zone beside the label, and one row per zone with its banked moving time + a bar scaled to the fullest zone — see step 7; the Pacer page: the virtual-partner ahead/behind time up large as a signed split, the AHEAD / ON PACE / BEHIND verdict beside the label, then goal / target time / projected finish / distance delta — or an honest `PACER --` / `NO GOAL SET` inactive state, see step 7); and the **Nav page**: breadcrumb course following (see the course module below) — the loaded course polyline + a position-marker cross drawn in a 168x96 map panel (`sharp_mip` gained a clipping, dirty-line-aware Bresenham `Framebuffer::draw_line` for it), distance-along-course + perpendicular offset below, honest `NO COURSE LOADED` / `AWAITING FIX` empty states, and a steady 2x `OFF COURSE` banner over the breadcrumb while the off-course alert is latched (the panel repaints only when the marker or alert changes, so a resting Nav page still flushes zero SPI); and the **Back-to-start page**: distance to the run's start up large, 16-wind HDG/BRG rows, a relative direction arrow, and the TrackBack breadcrumb map — see step 7; `face::page_rows` / `page_icons` / `page_hero` render whichever page is current, and the idle status face ignores the page (on the idle face BTN3 instead cycles the GNSS recording mode — see the Power discipline section). Page selection is now **SIM-VERIFIED (2026-07-08)**: the Renode sim injects buttons (`runMacro $btn3` in the monitor), and dumping the panel before/after confirms the display re-renders Dashboard → Distance → Pace. This surfaced a render bug fixed the same day — a one-shot `state::PAGE` change was consumed by the screen task's `select(...changed())` wait and dropped before the top-of-loop `try_changed()` could apply it, so the page never switched on screen even though the state cycled; the wait now applies the value the winning branch yields. `bin/watch-sim.sh --gui` shows the live panel via the `sim/SharpMipDisplay.cs` Renode model.
5. **Optical HR bring-up** — **CODE DONE incl. app wiring, SIM-VERIFIED (2026-07-08); bench verification pending parts.** `max86177` register driver (soft-reset → single-LED PPG channel → FIFO drain over blocking I²C) + a float-free integer PPG→BPM peak detector (DC-baseline removal, envelope-threshold beat detection with a refractory gate, inter-beat-interval smoothing, validity flag; 10 host tests). The `hr` task now drives it over TWISPI0, runs the detector, and publishes to `state::HR`, which the status face renders as an HR row. Because the driver is blocking, the task first does an async, timeout-bounded presence probe so an absent sensor (the sim, an unwired bench) parks instead of stalling the executor — sim-confirmed. The licensed Maxim HR algorithm comes later via `bindgen` against Maxim's C library, post-tier-1.
6. **BLE GATT bring-up** — **CODE DONE behind the `ble` feature (2026-07-08); UNVERIFIED — needs the dev kit, cannot be sim-tested.** The `ble` task enables the Nordic **S140 SoftDevice** via `nrf-softdevice` (git-pinned rev `47d6121c`; compiles + links against this Embassy 0.10 stack), advertises a custom 128-bit GATT service ("Threkir"), and on connection `notify`s one `watch_core::link` status frame per second — the SAME bytes the UART transport emits, so the phone-side decoder is transport-agnostic. Because the SoftDevice is proprietary firmware the Renode sim can't run, this path is compile-and-link-verified only; RAM origin, interrupt priorities, and connection params all need bench confirmation. It's **feature-gated and mutually exclusive with the default build**: `cargo build --release --no-default-features --features ble` (the SoftDevice owns `critical-section`, so the default `single-core-cs` feature is dropped; `build.rs` swaps in `memory-ble.x` to link above the SoftDevice). The default / sim build is byte-for-byte unchanged and still carries the link over UARTE1. Flashing needs the S140 hex programmed once alongside the app — see [`local_testing.md`](local_testing.md) and [decisions.md § 210](../../docs/architecture/decisions.md). ANT+ (S340) and secure pairing/bonding are later work.
7. **Integration** — wire steps 3–6 into a single recording state machine that ports the existing Dart `run_recorder` algorithm to async Rust. **RECORDER + BUTTON CONTROL + ELEVATION + FLASH RUN STORE + BLE SYNC (firmware half) WIRED; recording SIM-VERIFIED (2026-07-08); BLE sync compile-only / UNVERIFIED.** `watch_core::record` holds the host-tested `Recorder` (Idle → Recording → Paused → Finished) — haversine distance, elapsed/moving time, pace, `run_recorder`'s point-acceptance filter (min-move 3 m, max-jump 100 m, max-speed 10 m/s), its derived moving-time auto-pause (0.5 m/s gate), and **laps**: a 1 km auto-lap closes on the fix that carries the current lap past the boundary, and a manual lap (**BTN4** — the Fenix layout's lower-right Lap per decisions §81) closes through the same path and resets the auto countdown. Laps carry index / distance / split (elapsed) / moving time and are **RAM display state only** — the flash run-store wire format is unchanged (persisting laps would be a v2 blob format; deliberately not taken while the format + its Dart mirror are pinned to a golden vector). The recorder also carries **live grade-adjusted pace** (2026-07-09): `watch_core::grade_adjusted_pace` is the **fourth parity port** of the Minetti GAP helper (web `grade_adjusted_pace.ts` canonical, Dart twin, Connect IQ `GradeAdjustedPaceView.mc`) — the identical `grade_factor` polynomial/clamp plus the whole-track batch helper mirrored test-for-test, and a streaming `GapEstimator` in the Garmin field's shape (grade rolled per ≥5 m segment, 0.4 m/s walk gate) that the `Recorder` feeds each accepted fix with the **baro-preferred altitude** (`set_baro_altitude` from `state::ELEVATION`, GPS `alt_m` fallback — the same preference the flash store's point stamping uses). The `Snapshot` gains `gap_s_per_km` and the Pace glance page renders it as a `GAP` row under HR, so raw and effort-equivalent pace read together on a hill; sim-verified on a synthetic 10% climb (GAP hero-vs-row visibly faster than raw pace on the emulated panel). The recorder also banks **HR zones + in-zone time** (2026-07-09): `watch_core::hr_zones` mirrors the main app's default five-zone %-of-max ladder (web `training/hr_zones.ts` canonical — 60/70/80/90/100 % upper bounds, inclusive boundaries, the legacy 190 bpm fallback; max HR is a plausibility-guarded `set_max_hr` hook for a future settings sync), and per-zone time accrues exactly where moving time does — manual pause, auto-pause, and a missing/dropped pulse (`set_hr`, fed by the record task from `state::HR`) bank nothing. The `Snapshot` carries the ladder + accumulators, run-view HR rows show the live zone (`152 BPM Z3`), and the fifth Zones glance page renders the breakdown (sim-verified as the honest no-HR zero state — the sim has no MAX86177, so the page shows `--` / `ZONE --` / all-zero rows while distance accrues). The recorder also carries an **even-pace virtual partner** (2026-07-10): `watch_core::pacer` defines a partner running a goal (distance + time) at perfectly even pace on the **elapsed** clock — a race clock doesn't stop at an aid station, so ahead/behind answers "will I cross the line by the goal time", the Garmin Virtual Partner semantics — and `Pacer::status` reduces the live totals into metres ahead/behind, the same delta as seconds at goal pace, a projected finish at the whole-run average, and an ahead / on-pace / behind verdict inside the app's shared ±5 % dead-band (the web `challenge_progress.ts` `ON_PACE_BAND` ratio rule, so the watch can't grade "on pace" differently than the app's challenge hint). Crossing the goal distance latches the finish: the delta freezes at the banked result instead of drifting while the runner jogs out. The goal is **unset by default** and armed only through `Recorder::set_pacer_goal` — a plausibility-guarded (100 m..1000 km, 60 s..~11.5 days, ignore-don't-clamp) settings-sync hook in the same shape as `set_max_hr`; nothing on-device sets it at tier 1, the sim build arms a demo 1 km/5:00 goal behind `sim-autostart`. The sixth Pacer glance page puts the signed delta up large (`+0:42` ahead / `-1:05` behind) with verdict, goal, target, projected finish, and distance delta below — or an honest `PACER --` / `NO GOAL SET` inactive state, never zeros pretending to be on pace. Grade-aware splitting (PacePro-style terrain allocation via the roadbook's `gradeFactor`) is a deliberate later slice on the same seam. Sim-verified: the delta evolves in the defmt stream and the Pacer page renders it live on the emulated panel (hero `-0:28` → `-0:32`, DIST -94 -> -109 M against the demo partner, elapsed clock running through auto-pause). The firmware also follows a **breadcrumb course** (2026-07-10): `watch_core::course` is the **fifth parity port** — `Course::project` mirrors web `route_snap.ts` `snapToPolyline` (nearest perpendicular foot per segment, along-course accumulation per `route_geometry.ts` `distanceAlongRoute`, tests mirrored case-for-case) and `OffCourseAlert` latches the mobile run screen's route-overlay thresholds (alert past 40 m, re-arm below 20 m) — with a fixed 256-point / 4 KiB course capacity (`from_points` rejects overflow; longer courses must be phone-simplified before the future tier-2 BLE push). The `nav` task projects each `state::FIX` onto the course and publishes a `NavView` to `state::NAV` (warning on the alert's rising edge whatever page is shown); the Nav run-view page renders it (see step 4). The only course today is the canned sim one behind the **default-OFF `sim-course` feature** (built by `bin/watch-sim.sh`): the west + south edges of the `bench_jog` fixture's rectangle, so every sim lap exercises on-course following, the alert (`nav: OFF COURSE (41 m off, 179 m along)`), and the re-arm (`nav: back on course`) — sim-verified end to end, banner + breadcrumb + marker on the emulated panel. The hardware build carries no course and its Nav page reports `NO COURSE LOADED`. 26 recorder host tests + 17 in the GAP module + 5 in `hr_zones` + 12 in `pacer` + 20 in `course`. The same event cadence also drives **on-run alerts** (2026-07-10): `watch_core::alerts` (18 host tests) fires **drink / eat reminders** on a moving-time cadence reduced from the main app's `fuel_plan` defaults (500 ml/hr at a ~125 ml sip → drink every 15 min; 60 g carbs/hr at 25 g per gel → eat every 25 min) — intervals bank exactly where moving time banks, so a pause or aid-station stop accrues nothing — plus an **HR-zone ceiling alert** (`set_zone_ceiling`, off by default and plausibility-guarded like `set_max_hr`, awaiting a settings sync; fires once per excursion above the ceiling, re-arms only when the zone drops back, 60 s anti-flap cooldown). One alert holds the screen at a time for an ~8 s TTL; the zone alert outranks and supersedes a fuel reminder, which re-queues rather than drops (fuel is the alert an ultra runner must never silently lose), and queued reminders promote eat-before-drink. The record task publishes the active alert to `state::ALERT` after the recorder updates — pure engine, L4, an alert can never disturb the recording math — and the ui task draws it as a `!`-prefixed all-caps **2x banner over the hero band** (display-only: the DK has no vibration motor, and the 1-bit panel has no inverse video to spend). The sim shortens the cadences through the public setter under `sim-autostart` (30 s drink / 45 s eat; hardware keeps the fuel_plan-derived defaults) — sim-verified: the defmt stream logs `record: alert Drink` / `Eat` on cadence and the emulated panel shows the banner over the run view; the zone alert is host-test-only until a real HR sensor exists (the sim has no MAX86177). See [decisions § 214](../../docs/architecture/decisions.md#214-tier-1-on-run-alerts-anchor-their-fuel-cadence-to-fuel_plans-defaults-bank-on-moving-time-and-share-one-display-slot). The recorder's accepted-fix seam also feeds **TrackBack / back-to-start** (2026-07-10): `watch_core::trackback` keeps a fixed-RAM decimated breadcrumb (96 points at 20 m spacing that thins by halving + spacing-doubling — the whole track stays represented at coarser resolution, ~245 km in ~768 B) plus the live distance and great-circle bearing back to the start and a course-over-ground heading (derived only over ≥5 m of real displacement, honestly blanked 10 s after movement stops — no magnetometer at tier 1, so a stationary wrist gets `--`, never a stale arrow). The `record` task publishes the view to `state::TRACKBACK`; the eighth **Back-to-start** run page renders the distance hero + HDG/BRG rows and the screen task overlays the relative arrow and a north-up breadcrumb map (via the `sharp_mip` `draw_line` primitive). RAM display state only — the flash run-store wire format is unchanged. 14 trackback host tests; sim-verified over the canned jog (distance/bearing evolve, the arrow points back, the crumb decimates + thins). The `record` task drives it from `state::FIX` **and the `state::RECORD_CMD` command channel** (selecting over tick / fix / command) and publishes a snapshot to `state::RECORD`, which the face renders as a recording line; the sim run shows distance accruing and auto-pause holding moving time. **Button control is wired**: the `button` task debounces the DK's buttons and feeds commands — **BTN1** = start/pause/resume toggle, **BTN2** = stop, **BTN4** = manual lap — with the press→command decision in the host-tested `watch_core::button` (5 host tests). On real hardware BTN1 is the only start path; the old "auto-start on the first fix" stand-in lives behind the **default-OFF `sim-autostart` Cargo feature** so the sim records without needing a button. The buttons themselves are now driveable in the sim too, behind the **default-OFF `sim-buttons` feature** (also built by `bin/watch-sim.sh`): the hardware `button` task waits on `wait_for_falling_edge`, which the nRF52840 drives from the GPIO SENSE/DETECT path that Renode's GPIO model doesn't implement, so `sim-buttons` swaps in a level-polling variant that the `sim/watch.resc` `runMacro $btn1`/`$btn2`/`$btn3`/`$btn4` monitor macros drive (the macros pull the input pin low via `OnGPIO`). Manual lap is sim-verified through that path (2026-07-09): `runMacro $btn4` lands `button: Lap -> Lap` → `record: command Lap`, and cycling BTN3 to the Lap page renders it on the emulated panel. The hardware build keeps the low-power SENSE path untouched. The barometer is wired too — the `baro` task drives the **BMP581** (`drivers/bmp581/`, 5 host tests) over TWISPI1 through the same async probe guard, feeding `watch_core::elevation` (barometric altitude + cumulative-vert accumulator) and publishing a snapshot to `state::ELEVATION`. That snapshot is now surfaced end-to-end: the status face prefers the barometric altitude for its ALT row and adds a cumulative-vert line (`VERT +gain -loss`), and the phone-link status frame carries an additive optional `"elev":{alt_m,gain_m,loss_m}` object (schema still `"v":1`, `null` when the baro is absent) that the mobile dev Sim Watch screen decodes and displays. **Run sync (firmware half) is now wired.** The record task captures the GPS *track*, not just totals: on start it opens a `run_store::RunWriter` over a slot-sized RAM staging buffer, `push_point`s each fix the recorder accepts as a new anchor (the added host-tested `Recorder::last_fix_stored` signal, so stored points mirror the acceptance filter exactly) stamped with the latest HR (`state::HR`) + barometric altitude (`state::ELEVATION`), and on stop finalizes the blob with the snapshot totals and commits it to internal flash. `app/src/run_flash.rs` is the flash driver — a `FlashSink` (`run_store::ByteSink`) over embassy-nrf's NVMC plus a `RunStore` (commit / manifest / read_chunk) over the host-tested `watch_core::flash_store` slot layout: **4 slots × one 4 KiB erase page, reserved at the top of flash in both `memory.x` and `memory-ble.x`** (FLASH shrunk by 16 KiB; the region addressed by absolute offset `0xFC000`). It is **best-effort / L4** — it probes for the NVMC controller once at boot and no-ops every op if it's absent, and any flash error only `warn!`s, so recording is never blocked on flash. The **per-run point cap is 253** (`HEADER + 253·POINT + FOOTER ≤ 4096`): a few minutes at ~1 accepted fix/s. A full ultra needs the tier-2 external QSPI flash; the internal-flash store is a bench-prototype foundation, not shipping capacity. The default (sim) build boots + records unchanged (verified: distance accrues over 30 s, no panic/hang; Renode does model NVMC). With the `sim-buttons` feature the sim can now also **stop** a run (`runMacro $btn2`) — verified to drive the recorder to `Finished`, which runs the `commit` path (a clean end-to-end flash-write capture is still owed). On the **`ble`** build the same Threkir GATT service now carries `run_manifest` (read + notify: `ManifestHeader` + one `ManifestEntry` per finished run) and `run_chunk` (write + notify: the phone writes a `ChunkRequest {run_seq, offset, len}`, the watch notifies back that slice of the blob clamped to the notify MTU + blob end) — the protocol of [firmware.md § Sync protocol](../../docs/custom_watch/firmware.md), live link preserved. Like step 6 this **compiles + links but has never run on hardware**; additionally, with the SoftDevice enabled the NVMC backend must be swapped for `nrf_softdevice::Flash` (SoftDevice-coordinated flash) before real use — see the caveat in `app/src/run_flash.rs`. Reboot-persistence of the slot directory is now **wired (2026-07-10)**: `RunStore::new` scans every slot's flash bytes at boot via the host-tested `flash_store::recover_slot`, rebuilding the directory from the finished blobs already on flash so a run recorded in a prior power cycle is re-advertised instead of being lost until its slot is overwritten. Because the blob doesn't store its own length, the footer is located by scanning point-aligned offsets for the footer magic **plus** a CRC32 that verifies the header+points prefix — the CRC is what makes a stray footer-magic inside track data safe (10 new host tests, incl. the decoy-magic case + erased/truncated/newer-version rejection). `SlotDir::next_run_seq` resumes numbering above the highest recovered seq so a fresh run can't reuse a recovered id, and the record task seeds its counter from it. The core scan + directory rebuild is host-tested; the `app/` flash-read side (`recover_dir`) is best-effort / L4 and, like the rest of the driver, compile-verified only until the dev kit lands. **The phone half is now built too** (decisions §211): `apps/mobile_android/lib/sim_watch_sync.dart` is a Dart mirror of `run_store` — it pulls the manifest, reassembles chunks, `verify_blob`s each run (pinned to the SAME golden vector as the Rust `run_store` test, so the two can't drift), reshapes it into the payload the existing `WatchIngestQueue` / `runFromWatchPayload` → `api.saveRun` path already uploads, and delivers it via `WatchSyncClient` behind an injectable transport seam (production impl over the sanctioned `flutter_reactive_ble`). A dev-gated "Sync runs" action on the Sim Watch screen drives it; the pull→decode→verify→deliver path is unit-tested without a radio. What remains is purely on-hardware: exercising the real SoftDevice + NVMC-vs-`nrf_softdevice::Flash` path, and bench-confirming the boot-time slot recovery reads back real committed blobs — both pending the dev kit.
8. **Fabricate the tier-1 enclosure** — once the integrated unit records a run, 3D-print a chassis that holds the breakout boards + LiPo together and takes a Velcro strap, so it can be worn for a test jog. **FDM (any hobby printer / Shapeways / JLCPCB 3D) is the intended method here, ~$0–50 per print** — see [`parts.md`](../../docs/custom_watch/parts.md) (optional-tools table) and the tier-1 deliverables in [`prototyping.md`](../../docs/custom_watch/prototyping.md#tier-1--bench-prototype-5002k-36-months). This is a *functional chassis to hold breakouts*, **not** case CAD / industrial design / a sealed enclosure — those are tier-2+ and stay gated on the §71 triggers (see [`CLAUDE.md`](CLAUDE.md)). A printed case is not water-resistant and will not pass IPX7 (that needs injection-moulded tooling + a gasket, a tier-2/3 line item).

Each step is roughly 2–4 weeks of evenings depending on prior firmware experience.

**Parity-port analysis cores (2026-07-10).** Six more pure `no_std` `watch_core` modules landed as host-tested parity ports of shipped web helpers, ahead of their UI wiring (the same "logic before peripherals" pattern as steps 5/7): [`roadbook`] + [`fuel_plan`] (the race-crew per-checkpoint schedule + per-leg fueling — the biggest ultra-watch gap), [`pace_segments`] (per-split pace breakdown with the shared colour ramp), [`distance_bands`] (race-distance band), [`gear_wear`] (shoe-mileage wear state), and [`training_load`] (single-run + rolling CTL/ATL/TSB). Each mirrors its web `.test.ts` + Dart twin test-for-test (roadbook 11 / fuel_plan 9 / pace_segments 24 / distance_bands 9 / gear_wear 8 / training_load 31 = 92 new host tests) and compiles for both the default and `ble` targets. Two small helpers the ports depend on but that aren't yet their own `watch_core` modules (route_markers' `parseCutoff`, exercise_calories' `runCalories`) are inlined privately for now, to be de-duplicated when those helpers are ported.

**Wired into the run view (2026-07-11).** All six cores now drive run-view glance pages, folded into the recorder `Snapshot` the same way `cutoff_eta` / `race_predictor` are — each a small `Copy` view computed in `snapshot()`, rendered by `face`, with an honest inactive state until fed. The page cycle grows to 16 (see step 4): **Splits** (distance banked per pace bucket, accrued in `on_fix` by segment speed — the pace analogue of the zones page), **TrainingLoad** (the run's single-run stress; the rolling CTL/ATL/TSB reads `SYNC` — it needs multi-day history the watch doesn't hold), **DistanceBand** (the race band the run distance falls in), **Roadbook** + **Fuel** (a pushed name-free checkpoint schedule via `Recorder::set_roadbook` — the phone builds the roadbook where the polyline lives, the watch runs `build_fuel_plan` over it for carry-to-next-aid + totals), and **GearWear** (the active shoe's wear, run distance folded into the synced baseline via `set_gear`). `gear` + `roadbook` are unset on hardware (honest empty pages) until a phone push / settings sync; the sim arms demos behind `sim-autostart` (a 700/800 km shoe) + `sim-course` (a start / 90 m aid / 180 m finish roadbook). `watch_core` now 351 host tests (a grid-fit check over every page at extreme + inactive values, plus per-core wiring), default + `ble` + sim builds green. Faithful sim-panel verification of the new pages still awaits Renode tooling on the dev machine.

**Five more parity-port cores (2026-07-11).** Another five pure `no_std` `watch_core` modules landed as host-tested ports of shipped web helpers, same logic-before-peripherals pattern: [`age_grade`] (age-graded performance % against the USATF-MLDR 2025 factor tables — web `runs/age_grade.ts` + the generated `age_grade_tables.ts`, whose factor arrays were copied verbatim by a throwaway generator, never hand-retyped), [`hydration`] (daily water target + budget — web `nutrition/hydration.ts`), [`exercise_calories`] (gross run/gym kcal for the dynamic-TDEE goal — web `nutrition/exercise_calories.ts`), [`training_paces`] (the five Daniels intensity-zone paces from a goal pace, with the female + masters calibration — the pace-zone surface of web `training/training.ts`, reusing [`race_predictor`]'s ported `riegel_predict` rather than re-porting Riegel), and [`fitness`] (VDOT / VO2max / per-run TSS / threshold pace / recovery advice + the Fitness-card training-load snapshot — web `training/fitness.ts`, with the web's ISO `started_at` + `nowMs` collapsed to an integer day index exactly as [`training_load`] does; `recovery_advice` returns an enum, not English strings). Test counts mirror each web `.test.ts` (age_grade 12 / hydration 9 / exercise_calories 11 / training_paces 10 / fitness 46 = 88 new host tests, `watch_core` now 435 green); default + `ble` + embedded (`thumbv7em-none-eabihf`) all compile, `cargo fmt --check` + `clippy -D warnings` clean. Pure cores only — **not yet wired to a run-view glance page** (the deliberate follow-up, once the in-flight page-wiring PRs land) and **not sim/hardware-verified** (Renode tooling is absent on the dev machine). Now that `exercise_calories` is its own module, the private `runCalories` inlined in [`roadbook`]/[`fuel_plan`] becomes a live de-dup follow-up. See [decisions § 218](../../docs/architecture/decisions.md).

## Layout

Actual workspace shape (Cargo workspace, all paths relative to `apps/custom_watch/`):

```
Cargo.toml              workspace root
rust-toolchain.toml     pins a recent stable Rust + thumbv7em-none-eabihf target
.cargo/config.toml      runner = probe-rs, default target, defmt log level
app/
  Cargo.toml
  src/
    main.rs             #[embassy_executor::main] entry, spawns tasks
    tasks/
      gps.rs            GNSS NMEA parser task
      hr.rs             MAX86177 polling + naive peak-detect task
      baro.rs           BMP581 sample task (per decisions.md § 90)
      ui.rs             screen update + button-handler task
      ble.rs            GATT server + sync task
      record.rs         recording state machine (port of Dart run_recorder)
drivers/
  sharp_mip/            Sharp Memory LCD driver crate
  ublox_nmea/           u-blox NMEA parser crate (no_std)
  max86177/             MAX86177 register-level driver + bindgen wrapper for the HR algorithm
boards/
  nrf52840_dk/          board-support crate: pinmux, peripheral assignments
```

This is intentionally close to the per-task / per-driver split that the Zephyr-flavoured proposal in [`docs/custom_watch/firmware.md`](../../docs/custom_watch/firmware.md) uses — so the firmware architecture stays portable across the language choice. If we ever revert to C/Zephyr per the §80 fallback, the task boundaries map directly onto Zephyr threads + work queues.
