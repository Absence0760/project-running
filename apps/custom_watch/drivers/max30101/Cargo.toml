[package]
name = "max30101"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

[dependencies]
embedded-hal = "1.0"
watch_core   = { path = "../../core" }
defmt        = { version = "1.1", optional = true }

[features]
default = []
defmt   = ["dep:defmt", "watch_core/defmt"]
