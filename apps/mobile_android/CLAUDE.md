# mobile_android — AI session notes

The most mature Flutter target in the monorepo. Almost every "Android" checkbox in [../../docs/product/roadmap.md](../../docs/product/roadmap.md) Phase 1 is ticked. Treat this app as the reference implementation — when in doubt about what a feature *should* look like on mobile, look here first.

## Scope — read before writing code

**Web is the canonical feature surface. This app mirrors web and adds device-only capabilities.** See [../../docs/architecture/decisions.md § 24](../../docs/architecture/decisions.md#24-web-is-the-canonical-feature-surface-mobile-and-watches-are-platform-additive) and the live matrix at [../../docs/product/parity.md](../../docs/product/parity.md).

**One-Dart-codebase rule.** `apps/mobile_android/lib/` and `apps/mobile_ios/lib/` are kept byte-for-byte identical, plus `apps/mobile_android/test/` and `apps/mobile_ios/test/`. Every change you make under `lib/` or `test/` must be applied to the iOS twin in the same commit. Platform-specific runtime behaviour goes inside the unified files via `Platform.isAndroid` / `Platform.isIOS`. The pubspec deltas between the two apps are limited to `name` and `description` — every dependency, version, and asset stays in lockstep. Verify with `diff -rq apps/mobile_android/lib apps/mobile_ios/lib` (should be empty).

**Build here:**

- **Device-led features** (the physical-exception list in §24): live GPS recording + foreground service, auto-pause, on-device crash recovery, BLE chest-strap HR, pedometer / cadence, haptic / TTS pace alerts, OS share sheets, OS share-target intents (GPX / KML import), Health Connect import, disk-backed tile cache, background sync via WorkManager.
- **Mirroring** of an already-shipped web feature into a Flutter screen + api_client method + (when needed) a domain model in `core_models`. Driven by `parity.md` rows where web is `✓` and android is `✗` / `Partial`.

**Don't build here first:**

- A user-facing feature that doesn't yet exist on web — build it on web first, mirror after. This applies even when the request lands on an Android session ("can you add X to Android?"); push back, ship X on web, then mirror. The exception is the device-led list above.
- New abstractions / DI frameworks. `StatefulWidget + setState + ChangeNotifier` is the entire UI stack. No Provider, no Riverpod, no Bloc. If a screen needs cross-cutting state, add a `ChangeNotifier` and wire `addListener` / `removeListener` in `initState` / `dispose`.
- Direct `Supabase.instance.client.from(...)` calls in screens. Route through `packages/api_client` so the typed client stays the single Supabase entry point. (Realtime channels are the documented exception.)
- Comments, decision docs, READMEs, or "removed X" markers under `lib/`. Conventions in [../../docs/architecture/conventions.md](../../docs/architecture/conventions.md) apply here in full — comments only for non-obvious *why*.

When closing a parity gap, the order is: row classes (already generated) → `api_client` typed methods → domain model in `core_models` if the shape isn't 1:1 → ChangeNotifier or screen-local state → screen. See [../../docs/product/mobile_android_backlog.md](../../docs/product/mobile_android_backlog.md) for the live execution order.

**Multi-modal expansion (Phase 4 — Fitness-hub redesign shipped, G5).** The bottom nav is now `Home / Fitness / [+]Log / Social / You` (`home_screen.dart`): the `Run` tab is gone as a top-level destination, the centre `Log` slot is a raised FAB that fans a Log-run / Log-lift / Log-food **speed-dial** — three **icon-only** buttons in a shallow arc around the button (one top-centre, one down-left, one down-right), not a vertical stack of labelled rows (`widgets/log_speed_dial.dart`; most-recently-used takes the top-centre slot, labels live on tooltips/semantics, long-press repeats the last modality). (The History Log FAB in `runs_screen.dart` keeps the `showLogSheet` bottom sheet — only the centre nav FAB is a speed-dial.) Each action lands on that modality's **dwell-in capture page** — the same in-shell keep-alive `PageView` page model the live recorder uses, so all three behave identically: run → the recorder page (`_pageRun`), lift → `GymScreen` (`_pageGym`), food → `NutritionScreen` (`_pageFood`). You arrive on a workspace you operate in for as long as the session lasts (record the run, build the workout over several sets, log the day's meals), the bottom nav stays visible, and the page's own composer (`showGymComposeSheet` / `showNutritionLogSheet` from the screen's `+`) is one tap away. **Don't route a Log action to a one-shot modal** — the dwell-in page is the contract (an earlier build opened the composer directly; users wanted to stay on a page and keep adding). All three are keep-alive capture pages with no nav destination (so an in-progress recording / half-built workout / day's food log survives swiping to Home and back) — **do not demote any of them to a pushed route.** The `PageView` is `[Home(0), Fitness(1), Run(2=_pageRun), Gym(3=_pageGym), Nutrition(4=_pageFood), Social(5), You(6=_pageYou)]`.

**Fitness is the modality hub** (`fitness_hub_screen.dart`) — a review/plan destination DISTINCT from the keep-alive capture pages. A top sub-tab strip switches four surfaces: **All** = the unified cross-modal activity timeline (the former standalone History tab, *absorbed* here — `RunsScreen` mounted WITH the gym+food stores and `showKindChips: false` so the hub's TabBar owns the kind axis); **Runs** = the dedicated offline-first run list (`RunsScreen` WITHOUT a gym store) PLUS a Routes entry (relocated out of Social — `RunsScreen.onOpenRoutes` surfaces a route action in its AppBar that pushes `RoutesScreen`) and a Plans entry (`RunsScreen.onOpenPlans` surfaces a training-plans action that pushes `PlansScreen` — the hub threads the existing `TrainingService` in from `home_screen` for this); **Gym** = `GymScreen`; **Nutrition** = `NutritionScreen`. The hub's Gym/Nutrition tabs host SEPARATE instances from the keep-alive `_pageGym`/`_pageFood` capture pages. The self-hiding contract holds — empty Gym/Nutrition render their own onboarding state. **There is no standalone History destination anymore** — don't re-add one.

**You is profile + Settings** (`you_screen.dart`) — a thin host that puts a profile-header tile above the unchanged `SettingsScreen` body. Settings vacated its own nav slot and folds in here; `SettingsScreen`'s public constructor is unchanged (its mount site just moved). **Coach has no nav slot** — `dashboard_screen.dart` (Home) pins an always-visible "Ask your coach" entry at the top of the body (one tap → `CoachScreen`, gated on `api != null && training != null`).

`GymScreen` / `NutritionScreen` are *also* reachable via the self-hiding Home today's-lift / nutrition-rings cards + the dashboard "View all" (those push a separate instance). The reshape ships **ungated** with a `Preferences.keepRunPrimary` "Run as primary action" toggle (the protect-the-core-runner mechanism). The 5-slot ceiling is why the centre slot is an action, not a tab — don't add a sixth nav destination. See [decisions.md § 63](../../docs/architecture/decisions.md#63-single-app-multi-modal-expansion-run--gym--nutrition-under-one-nav-one-db) + [multi_modal.md](../../docs/features/multi_modal.md).

## Stack

- **Flutter** stable, Dart 3.x
- **State management:** `StatefulWidget` + `setState` throughout. Stores (`LocalRunStore`, `LocalRouteStore`, `Preferences`) are plain `ChangeNotifier`-style singletons that screens subscribe to via `addListener` in `initState` and unsubscribe in `dispose`. No Provider, no Riverpod, no Bloc. If you add a new screen, follow the same pattern — do not introduce a DI framework.
- **Hot-path exception — `run_screen.dart`:** the per-second `_onSnapshot` handler does NOT call `setState`. It updates mirror fields (for `_saveInProgress`, `_refreshLockScreenNotification`, and the `_formattedX` getters) then publishes to a `ValueNotifier<_LiveStats>`. The affected subtrees (map, off-route banner, route-remaining badge, stats panel) are wrapped in `ValueListenableBuilder`, so the rest of the recording tree (activity chips, GPS-lost / permission-revoked banners, layout) doesn't rebuild at GPS rate. If you add a new stat that updates per snapshot, put it behind the notifier, not in a `setState`. Control-state changes (state transitions, manual pause, lap mark) still go through `setState` — that cadence is low enough that a full rebuild is fine.
- **Maps:** `flutter_map` with a MapLibre-compatible raster tile source. Cached via `flutter_map_cache` + `dio_cache_interceptor` for the disk-backed persistent tile cache. See [decisions.md § 8](../../docs/architecture/decisions.md) and [§ 5](../../docs/architecture/decisions.md).
- **Recording:** delegates to `packages/run_recorder` — state machine, GPS filter chain, auto-pause, off-route detection, all live there. This app holds the UI and the screens; the recording logic is a package so the iOS and Wear OS apps can eventually reuse it.
- **Auth / backend:** `packages/api_client` for Supabase. Google Sign-In is wired via the native `google_sign_in` package → exchanges the ID token through `ApiClient.signInWithGoogleIdToken`. Apple Sign-In is wired via `sign_in_with_apple` → `ApiClient.signInWithAppleIdToken`. Both buttons render on `sign_in_screen.dart` and `sign_up_screen.dart`. parkrun athlete-number import lives behind a Settings tile rather than a true OAuth flow (parkrun has no public OAuth surface).

## Internationalization (i18n)

Localized via the standard Flutter stack — `flutter_localizations` + `intl` + gen-l10n. See [decisions.md § 113](../../docs/architecture/decisions.md#113-mobile-i18n-uses-flutter-gen-l10n--arb-with-committed-non-synthetic-output-and-a-per-device-locale).

- **Catalogues:** `lib/l10n/app_<locale>.arb` for the six locales `en/de/fr/es/ja/pt_BR` (plus a base `pt` fallback gen-l10n requires). `app_en.arb` is the template (carries the `@key` metadata); the rest are value-only.
- **Generated code is committed** under `lib/l10n/gen/` (config in `l10n.yaml`, `output-dir: lib/l10n/gen`). After editing any ARB, run `flutter gen-l10n` and **mirror the regenerated files to the iOS twin** — they're byte-identical (gen-l10n output is package-name independent).
- **Adding a string:** add the key to **all six** ARBs (real translations, not English placeholders — `test/l10n_parity_test.dart` enforces non-empty + key parity + faithful `{placeholders}`), regenerate, then `AppLocalizations.of(context).<key>` at the call site. ARB keys are camelCase derived from web's dotted keys (`nav.dashboard` → `navDashboard`).
- **Locale selection:** per-device, **not** DB-synced — `Preferences.locale` (`_kLocale`, canonical tag; null = follow device) drives the global `localeNotifier` → `MaterialApp.locale`. Picker is in Settings → Preferences. Pure negotiation lives in `lib/l10n/locale_support.dart` (Dart twin of web's `locale.ts`).
- **Test harnesses** that pump a screen using `AppLocalizations` must set `localizationsDelegates: AppLocalizations.localizationsDelegates` + `supportedLocales: AppLocalizations.supportedLocales` on the test `MaterialApp`, or `AppLocalizations.of(context)` throws.
- **Dates:** locale-aware `intl` `DateFormat` lives in `lib/l10n/date_format.dart` (pure helpers — `formatDateMed`/`formatDateShort`/`formatMonthName`/`formatDow`/`formatDowNarrow`/`formatMonthAbbr`/`formatDateTime`/`formatTime`/`formatDowDateShort`, each taking a BCP-47 tag). Every helper uses CLDR skeletons (`yMMMd`, `MMMd`, `MMMEd`, …), so field order is locale-appropriate — month-first for `en` (US), day-first for de/fr/es/pt, year-first for ja. **Every helper normalizes a UTC instant to local time before formatting** (`_local(dt)` = `dt.isUtc ? dt.toLocal() : dt`): stored timestamps (`runs.started_at`, comment/review `created_at`, event starts) arrive as UTC `DateTime`s from `DateTime.parse` of a `Z`-suffixed string, and `DateFormat` renders raw wall-clock fields with no zone conversion — so formatting them directly showed the UTC calendar day, which rolled a day for viewers behind/ahead of UTC (the "manual run saved as tomorrow" bug). Pass synthetic calendar values (`DateTime(2026,1,5)` for DOW labels, `DateTime(2000, month)` for month names) as local `DateTime(...)` (`isUtc == false`) — they pass through untouched. **Don't re-add `.toLocal()` at call sites; the helper owns it.** `main.dart` calls `initializeDateFormatting()` once at startup (tests do it in `setUp`). Get the tag from `localeToTag(Localizations.localeOf(context))` where a `BuildContext` exists, else `activeLocaleTag` (context-free accessor in `locale_support.dart`, kept in sync from `main.dart` via `registerActiveLocaleTag` on every `localeNotifier` change — mirrors `registerActivePreferences`). The old hand-rolled month/day arrays are gone; `recurrence.dart`'s `weekdayLabel` stays English (TS↔Dart parity pair, not a display formatter).
- **TTS is locale-aware** (Epic C3, done). `audio_cues.dart` resolves the gen-l10n catalogue for the active locale via `lookupAppLocalizations(localeFromTag(activeLocaleTag))` (context-free) and re-applies the engine language per announcement via `setLanguage(ttsLanguageTag(activeLocaleTag))` (`en`→`en-US`, `de`→`de-DE`, `pt-BR`→`pt-BR`, unknown→`en-US`; best-effort, a failure never stops the cue). Spoken split / pace / speed / off-route / workout-step / pace-drift phrases live behind `tts*` ARB keys; `guided_runs.dart`'s scripts (titles + cue text) live behind `guidedRun*`/`guided*` keys and the library is now `guidedRunLibrary(AppLocalizations)` (was a `const`). The pure utterance helpers take an optional `localeTag` defaulting to `activeLocaleTag`, so `audio_cues` unit tests keep asserting the verbatim English output.
- **String extraction complete** (2026-06-03): every user-facing screen/widget now routes through `AppLocalizations`. A final wider sweep (stricter than the `Text('[A-Z]` heuristic) closed the real remainder: the `settings_screen` landing tiles, `period_summary` (`Week of`/`All time`/share text — pure helpers via context-free `lookupAppLocalizations`), the `verified_badge` tooltip, and dashboard/run-detail best-effort distance names (`bestEffortDistanceLabel` in `run_stats.dart`). Intentionally left English (not gaps): `devices_screen.overrideKeyRegistry` `label`/`hint` (fallbacks behind the `labelFor`/`hintFor` l10n switch), `workout_review_section.fromMap` labels (display uses `localizedLabel(l10n)`), `$e`-in-banner exception text, the `"DELETE"` deletion challenge token, and proper nouns. RTL (`EdgeInsetsDirectional`) is the only remaining mobile i18n work — deferred, no RTL locale ships yet.

## What's real vs stubbed

Nearly everything under Phase 1 "Android" in `roadmap.md` is implemented. Specifically:

**Live and working:**
- All of the `lib/screens/` files in the list below are wired to real data.
- Background GPS recording with foreground service, auto-pause, lap markers, wakelock.
- Offline-first: runs save to `LocalRunStore` → sync on connectivity change / app foreground / WorkManager periodic background sync → reconcile via `last_modified_at` timestamps.
- Strava ZIP import (GPX/TCX/FIT) and Health Connect import. Batch cloud push via `saveRunsBatch`.
- Disk-backed tile cache.
- Personal bests, weekly goals, edit title/notes, share as GPX, delete.
- Runs filter by activity type (run/walk/cycle/hike filter chips).
- Configurable split interval for voice cues (Settings > Split interval).
- Advanced GPS mode (Settings > Advanced GPS) for higher accuracy under tree cover.

**Stubbed or deferred:**
- Strava live OAuth (the ZIP import path is shipped). The OAuth surface is a Settings → Connect Strava button that's still scaffolded behind the same `apps/web` flow that ADR §41 covers.

## Files

**Screens** (`lib/screens/`):
- `onboarding_screen.dart` — first-launch permission ask (local `Preferences.onboarded` flag, pre-sign-in)
- `setup_wizard_screen.dart` — post-signup account-setup wizard, mobile twin of web's 7-step `/onboarding`. Gated on `user_profiles.onboarded_at`: `home_screen.dart` fetches the profile after the first frame and pushes this once when the column is null, then `Finish` / header-`Skip setup` stamps it via `ApiClient.completeOnboarding` / `markOnboarded`. Collects display name → units → primary goal (`primary_goal` bag) → demographics (gender + DOB + weight) under the Art 9 health-consent gate (mirrors `settings_body_metrics_screen` — gender + consent stamp only under the checkbox; DOB written unconditionally for the minor-exclusion floor) → privacy default → notifications (`push_notifications` bag) → done. Pure contract in `onboarding.dart` (TS↔Dart pair with web `onboarding.ts`). **Distinct from `onboarding_screen.dart`** (first-launch permission flow). 10 unit/widget tests (`onboarding_test.dart` + `setup_wizard_screen_test.dart`).
- `sign_in_screen.dart` — email/password + Google sign-in
- `sign_up_screen.dart` — email/password registration + Google sign-in, linked from sign-in
- `home_screen.dart` — the multi-modal nav shell (G5, Fitness-hub redesign): a `BottomAppBar` (Home / Fitness / Social / You) + a docked centre Log FAB, over a `PageView` whose pages are `[Home(0), Fitness(1=FitnessHubScreen), Run(2=_pageRun), Gym(3=_pageGym), Nutrition(4=_pageFood), Social(5), You(6=_pageYou=YouScreen)]`. Run / Gym / Nutrition have no nav destination but stay keep-alive dwell-in capture pages reached via the Log action so a session survives. Log FAB: tap → `showLogSpeedDial` fans the capture options above the button and navigates to the picked modality's page (or one-tap run start when `keepRunPrimary`), long-press → repeat last (or the speed-dial). Hosts the dashboard on page 0.
- `fitness_hub_screen.dart` — the Fitness modality hub (G5): a `TabController(length: 4)` + top TabBar strip (All / Runs / Gym / Nutrition) over a `TabBarView`. All = `RunsScreen` WITH gym+food stores + `showKindChips: false` (the absorbed History timeline); Runs = `RunsScreen` WITHOUT a gym store + `onOpenRoutes` + `onOpenPlans` + `showSyncActions: false` (the dedicated run list + the relocated Routes entry + the nested Training-plans entry; the cloud sync/refresh slot lives only on the All tab — side-by-side duplication overflowed the Runs AppBar title); Gym = `GymScreen`; Nutrition = `NutritionScreen`. Each sub-tab body owns its own Scaffold/AppBar/composer; the hub provides only the TabBar chrome (mirrors `social_screen.dart`). FitnessHubScreen takes a required `TrainingService` (`training`, threaded from `home_screen`) so it can build `PlansScreen`. The `onStartRun` handoff threads up to `home_screen._startRunWithRoute` so "Start with this route" still lands on the keep-alive recorder page. DISTINCT from the keep-alive capture pages.
- `you_screen.dart` — the You tab (G5): a thin host that renders a profile-header tile (own `ProfileScreen`) above the unchanged `SettingsScreen` body. Settings folded in here from its old bottom-nav slot; `SettingsScreen`'s constructor is unchanged.
- `run_screen.dart` — the primary recording screen (countdown, live stats, route overlay, finish summary); hosts the recorder state machine and all the hardening described in [../../docs/features/run_recording.md](../../docs/features/run_recording.md)
- `dashboard_screen.dart` — weekly mileage, PBs, goal progress; AppBar actions push `coach_screen.dart` (AI Coach), `feed_screen.dart` (activity feed) and `profile_screen.dart` (own profile)
- `coach_screen.dart` — AI Coach chat. Mirrors web `CoachChat.svelte` + `/coach/+page.svelte`. Plan switcher in the AppBar (when >1 plan), end-Drawer archive sidebar, context strip (plan / runs window 10|20|50|100 / HR pill / weekly-goal pill), markdown bubbles via `flutter_markdown`, inline edit / regenerate / copy / thumbs actions, multi-line composer with circular send button, daily-cap banner driven by `get_coach_usage` + `is_pro`. Streams from the web `/api/coach` endpoint via `dart:io` HttpClient + UTF-8 SSE parser; Realtime subscription on `coach_messages` stitches in messages persisted by other tabs.
- `coaching_screen.dart` — human coach-athlete roster (mirror web `/coaching`, persona #46). Mint/copy/share + revoke invite links, athlete list (tap → `coaching_athlete_screen`), my-coaches list with leave; end/revoke gated behind an `AlertDialog`. Reached from the Settings → Coaching tile (`settings_screen.dart`, Profile section, after sign-in). **Distinct from `coach_screen.dart`** (the AI chat) — this is the human coach relationship surface. Reads/writes through the `api_client` coaching methods (`createCoachInvite` / `fetchMyAthletes` / `fetchMyCoaches` / `fetchPendingCoachInvites` / `revokeCoachInvite` / `endCoachLink`). The public-link host comes from `WEB_BASE_URL` (pure `coachInviteLink`, falls back to `https://threkir.com`).
- `coaching_athlete_screen.dart` — coach run-review surface (mirror web `/coaching/athletes/[id]`). A linked athlete's recent runs (public + private, column-narrowed — no track) + active-plan compliance window + the assign-a-plan control (when the athlete has no active plan and the coach has plans). The `assign_plan_to_athlete` RPC RAISE (e.g. "athlete already has an active plan") is surfaced via `showTopBanner`, not swallowed. `fetchAthleteRuns` / `fetchAthletePlanOverview` / `fetchMyPlans` / `assignPlanToAthlete`.
- `feed_screen.dart` — activity feed of public runs from people you follow (14-day window, mirrors web `/feed`); activity-segmented filter + author dropdown + infinite-scroll. Tapping a card body pushes `public_run_screen.dart`
- `profile_screen.dart` — public user profile (`/u/[id]` mirror): header, Follow toggle, and tabs for Runs / Followers / Following / Notifications (notifications tab gated to self)
- `public_run_screen.dart` — read-only mirror of web `/share/run/[id]`. Reads via `ApiClient.fetchRunById` (RLS hides private), reuses `LiveRunMap` + segment chips + social section. Routes the fetched track through `clipTrackForUser` for non-owner viewers (anon included) before render — owner is detected by `api.userId == row.userId` (decisions §33). Reachable from feed-card body taps
- `public_route_screen.dart` — read-only mirror of web `/share/route/[id]`. Reads via `ApiClient.fetchRouteById` (returns `({Route? route, String? ownerId})` so the screen can clip for non-owner viewers); renders the planned-route polyline + stats + segments panel (read-only). Same owner gate as `public_run_screen` (decisions §33). Reachable from the Routes tab on `club_detail_screen`
- `live_spectator_screen.dart` — read-only mirror of web `/live/[run_id]`. On hydrate it reads the run's terminal state via `fetchPublicRunById` (mirror of web's `runIsFinished` + the `is_dnf` column, migration `20261207_001`): a **DNF** (race-marked) or **Finished** (`runIsFinished` — saved duration ends >2 min ago) run freezes on its saved totals and never opens the realtime subscription. Otherwise it hydrates existing `live_run_pings`, subscribes via `Supabase.channel.onPostgresChanges(insert)` filtered by `run_id`, and renders the trace on `LiveRunMap` (follow-runner). The status badge has five distinct states — **DNF** (danger) / **Finished** (neutral, terminal) / **Delayed** (amber — a *live* runner whose last ping went stale, NOT terminal) / **Live** (green) / Idle / Connecting; the terminal verdict outranks the live/stale freshness axis. `runIsFinished` is `@visibleForTesting`
- `devices_screen.dart` — Settings → Devices: lists `user_device_settings` rows with Rename / Edit overrides… / Remove popup actions. Mirrors web `/settings/devices`
- `privacy_zones_screen.dart` — Settings → Privacy zones: tap-to-add geofence picker on `flutter_map`. Persists to `user_settings.prefs.privacy_zones` via `SettingsService.updateUniversal`. Mirrors web `PrivacyZonePicker`
- `runs_screen.dart` — dual-purpose: the Fitness hub's **All** sub-tab AND (mounted without a `gymStore`) the dedicated **run-list** screen (the hub's **Runs** sub-tab + the "View all" / period drill-ins). **All-tab / timeline mode** (gym+food stores wired): renders the day-grouped cross-modality timeline (`widgets/activity_timeline_list.dart`). It still owns the All/Runs/Lifts/Meals kind chips when mounted standalone, but the Fitness hub passes `showKindChips: false` so the hub's TabBar is the single kind selector (no duplicate strip) — the timeline then stays pinned to All. The `onOpenRoutes` + `onOpenPlans` callbacks (set by the hub's Runs tab) surface Routes (relocated out of Social) and Training-plans (nested under the run surface) actions in the run-list AppBar. Shows All/Runs/Lifts/Meals kind chips once a second modality has data and — mirroring web — **every chip (including Runs) renders the day-grouped cross-modality timeline** (`widgets/activity_timeline_list.dart`). The timeline is assembled from the **local stores** by `lib/local_activities.dart` (`buildLocalActivities` over `LocalRunStore` + `LocalGymStore` + `LocalFoodStore`) — offline-first, always all modalities, rebuilt on any store change (so a lift/meal logged from the Log FAB shows at once). History **hydrates the gym + food stores itself on mount** (`_hydrateModalities`, best-effort, mirroring dashboard) — it's a first-class store consumer now, so it can't assume it was reached via dashboard/gym; skipping this was the "no Lifts tab when History is the entry point" bug. Each single-modality tab shows a `View all` link (`historyViewAll`) that pushes that modality's full surface — Runs → `RunsScreen` **without** a `gymStore` (the run list below), Lifts → `GymScreen`, Meals → `NutritionScreen` (needs `foodStore`, plumbed from `home_screen`). Run rows open run-detail (local lookup, else `fetchRunById`), lift rows open `GymDetailScreen`, meals are read-only. The **add FAB is per-chip** (mirroring web's `/history` Log action): on the cross-modal **All** chip it opens the run/lift/meal `showLogSheet` picker → that modality's one-shot composer; a single-modality chip adds straight in (Add run → `AddRunScreen`, Log lift → `showGymComposeSheet`, Log food → `showNutritionLogSheet`); a lift/meal save kicks a best-effort store sync. **Run-list mode** (no `gymStore`, or a pure runner with no lift/meal): the full run list with sorting / source / date-range filters, bulk-delete, pagination, FAB → `add_run_screen`. The inline run list is NOT an offline fallback (the timeline is local-first and renders offline) — it's just what a single-modality (run-only) user sees. See [decisions.md §63 amendment](../../docs/architecture/decisions.md#63-single-app-multi-modal-expansion-run--gym--nutrition-under-one-nav-one-db).
- `add_run_screen.dart` — manual-entry form: date/time + duration + distance + optional saved route. Pushed as a full-screen dialog (`fullscreenDialog: true`) to match the other create/edit-entity surfaces; keeps its own `Scaffold` (Form + validation + nested route picker) rather than the `FullScreenFormBody` chrome.
- `run_detail_screen.dart` — single run map + stats (primary + secondary) + interactive elevation/pace chart + best efforts + pace-bar splits + laps. **Map-match offline fallback:** the matched-track read (`_maybeFetchMatchedTrack`) classifies an unreachable-backend error (`isMatchUnreachableError`) distinctly from a server verdict — on offline it keeps the raw track rendering (L1) and shows the "Offline — will retry" pill (`MatchPillKind.offline`) rather than a hard error; subscribes to connectivity and re-fetches on reconnect, bounded by `shouldRetryMatchFetch` so a settled (`failed`/`skipped`/clean-`matched`) run never re-hits the backend. The pill state + retry gating are the pure `core_models/run_match_info.dart` selectors.
- `import_screen.dart` — GPX / KML / KMZ / GeoJSON / TCX file picker
- `routes_screen.dart` — saved routes list with per-row star toggle (owner-only) that drives the watch's starred-only route picker (see `docs/architecture/decisions.md § 44`). Hosted as the Routes sub-tab of `SocialScreen` (`embedded: true`); supports a standalone Scaffold path too for any legacy caller. The dual "Build" + "Import" FAB column is exposed via `RoutesScreenState.buildRouteFabs` so the embedded host hoists it on its own Scaffold. **Bulk delete** mirrors `runs_screen`: long-press an owned route to enter selection, tap rows to add/remove, then delete from a top-of-body banner (a banner instead of an AppBar replacement so the embedded-under-SocialScreen path doesn't need to plumb selection state up). Bookmarks aren't deletable since they're someone else's data — only OWNED rows enter selection. Calls `ApiClient.deleteRoute` per id with per-row failure isolation; successful ids drop locally via `LocalRouteStore.deleteMany` (batched notify so the list doesn't flicker).
- `routes_heatmap_screen.dart` — route-discovery browser; mobile mirror of web `/routes/heatmap` (decisions §§ 99–102, layout § 112). Heat-density background + clustered discoverable pins (pure-Dart `heatmap_clustering.dart`), a Filters bottom sheet (lens `popular`/`friends`/`featured`/`hidden_gems` + multi-select race-distance bands via `distance_bands.dart`). The map is full-bleed: a floating "N routes" pill opens the results list as a dismissible modal sheet, and tap-to-preview (tap a pin or row → that route's line + halo via `fetchRouteById`) shows a compact dismissible selection card → View. **Mobile-only layout** — web keeps its desktop sidebar (§ 112); don't port the pill/modal/card to web. Reached from the Heatmap button on `routes_screen`.
- `run_heatmap_screen.dart` — personal run-track heatmap; mobile mirror of web `/runs/heatmap`. A Strava-style "everywhere you've run" map of the user's OWN tracks (distinct from `routes_heatmap_screen.dart`, the public route-discovery heatmap). Downloads the owner's own track blobs via the owner-only `fetchTrackByPath` (newest-first `getRuns`, `_maxTracks` cap + bounded concurrency), aggregates them with the pure `run_heatmap.dart`, and renders heat cells as low-opacity `CircleLayer` dots that crossfade into the actual track `PolylineLayer` as you zoom past street level (flutter_map has no GPU heatmap layer). Owner-only path → no privacy-zone clip. Reached from the run-list AppBar flame action (`runs_screen`, run-list mode).
- `route_detail_screen.dart` — route map + metadata + reviews + public/private toggle + star-for-watch toggle (owner-only)
- `route_builder_screen.dart` — full-screen in-app route builder (Phase 3, web-parity). Tap-to-place waypoints with OSRM road / trail / straight modes; long-press a pin → tap-to-move drag-reshape; snap-to-start (pulsing halo on the start pin when 3+ waypoints) closes the loop with a tap near the start; MapTiler place search in the AppBar; "Locate me" FAB; live elevation gain via Open-Meteo; purple over-stroke on out-and-back retraced sections (`route_overlap.dart`); undo / clear / save (name + public toggle) → `ApiClient.saveRoute` + `LocalRouteStore.save`. Reached from the second "Build" FAB on `routes_screen`. Pure helpers live in `routing.dart`, `elevation.dart`, `geocoding.dart`, `route_overlap.dart`, `widgets/snap_to_start.dart`.
- `period_summary_screen.dart` — browsable weekly/monthly/all-time summary with stats, run list, and share (text or screenshot). The dashboard's Week / Month / All-time stat cards and a tap on any "Last 20 Weeks" heatmap column open it (`PeriodType.all` = lifetime, nav arrows disabled). Mirrors web's `PeriodSummary`.
- `explore_routes_screen.dart` — search and browse public routes with filters (distance, surface), save to library
- `settings_screen.dart` — Settings landing. Mirrors the web `/settings` left-nav shape (`apps/web/src/routes/settings/+layout.svelte`) with three sections — **Profile** (Account, Preferences), **Apps & data** (Integrations, Devices, Gear), **Account & legal** (Pro & support, Licenses) — each tile drilling into a dedicated sub-screen. The landing is a thin router; all tile content lives on its sub-screen. Keeps the public `SettingsScreen` constructor unchanged — its mount site moved from `home_screen.dart`'s old Settings nav slot into `you_screen.dart` (the You tab hosts it below a profile header).
- `settings_account_screen.dart` — email + sign-in/out, Profile photo (avatar upload/remove via `ApiClient.uploadAvatar`/`removeAvatar` → public `avatars` bucket, EXIF-stripped client-side; mirrors web `/settings/account`), View profile, Guided runs, Privacy zones, Sentry opt-out, Import / Full backup / CSV export / Restore-from-backup, Change password, Delete account.
- `settings_preferences_screen.dart` — units, pace format, map style, dark mode, default activity, audio cues, split interval, live pace alert, keep screen on, advanced GPS, **Show raw GPS track** (`showRawTrack` — forces the raw recorded line on run-detail even when a map-matched track exists; verification aid, off by default), **Run as primary action** (`keepRunPrimary`, G5), a **Body metrics** tile → `settings_body_metrics_screen.dart`, DOB, resting / max HR, HR zones, weekly goal, week start, default privacy, Strava auto-share, coach personality, email notifications, push notifications. Takes `apiClient` (for the body-metrics drill-in). All bag-backed prefs route through `SettingsSyncService.updateUniversal`.
- `settings_body_metrics_screen.dart` — Settings → Body metrics (G5, multi_modal.md § "Body metrics & sensitive data"). An explicit Art 9 health-data consent toggle gates height + weight (height → `user_profiles.height_cm` via `api.setMyHeightCm`; weight → `body_metrics` via `api.recordBodyWeightKg`, appended only on change; withdrawal calls `withdrawHealthDataConsent` + `clearBodyWeightHistory`). Activity-level + goal are ordinary nutrition prefs that auto-save to the universal bag. Feeds `loadNutritionTargets`. Toasts via `showTopBanner` (not ScaffoldMessenger — arch guard).
- `settings_integrations_screen.dart` — Strava connect/sync/disconnect (`flutter_web_auth_2` + Edge Function), parkrun athlete-number import, BLE heart-rate strap pairing (`HeartRateMonitorTile` + scan sheet, both moved off the old monolithic screen).
- `settings_pro_screen.dart` — Subscribe to Pro, Restore purchases, Manage subscription, Support the app. Apple Guideline 3.1.1 + Play subscription-policy disclosures (price + auto-renewal + management URL) are pinned by the `Restore purchases (Apple/Play subscription policy)` group in `architecture_guards_test.dart` against this file.
- `settings_licenses_screen.dart` — app version + `showLicensePage`.
- `clubs_screen.dart` — Browse / My clubs, hosted as the Clubs sub-tab of `SocialScreen` (the 4th bottom-nav tab). FAB opens `widgets/club_form_sheet.dart` (Create club)
- `social_screen.dart` — the Social bottom-nav tab. Four sub-tabs: Feed · People · Clubs · Discover. Each embeds its screen widget in `embedded: true` mode (returns body only, no Scaffold/AppBar/FAB) and SocialScreen hosts the chrome — one AppBar with a TabBar at the bottom and a single FAB slot that hoists whichever child FAB matches the active tab (Clubs → Create club; Feed / People / Discover have none). Routes used to be a sub-tab here; the Fitness-hub redesign relocated it to Fitness → Runs (a run-modality surface). `SocialScreen` still takes `routeStore` (ClubsScreen surfaces club-owned routes) but no longer takes `preferences` / `onStartRun`.
- `discover_screen.dart` — the Social hub's Discover tab (mirror web `SocialDiscover.svelte` + `/social?tab=discover`). Cross-club activity search over the `search_public_events` RPC (public clubs only): a debounced search field + category chips (run/cycle/class/social, reusing `eventEditorCat*` labels) + cadence / weekday / time-of-day dropdowns + free/paid chips. Result cards show discipline-or-title + club name + `category · cadence` + a free/currency price chip, and open `EventDetailScreen` on tap. Calls `ApiClient.searchPublicEvents` → `PublicEventResult` (core_models); failures fall back to an empty state (L4). Embedded under `social_screen.dart`; standalone Scaffold path kept for any direct caller. Time-of-day is timezone-aware server-side (`events.timezone`).
- `club_detail_screen.dart` — five tabs: Feed (threaded posts) · Events (admin Create button + RSVP cards) · Members (admin pending-requests panel + count) · Routes (club-owned routes via `routes.club_id`, lazy-loaded; tap → `public_route_screen.dart`) · Templates (admin/member-visible plan templates with per-row Adopt → `clone_plan_template` RPC; a Session-templates section → `clone_session_template`; and a gym-routine-templates section → `ApiClient.cloneGymRoutineTemplate`, member-gated Adopt → navigate to the cloned personal routine)
- `event_detail_screen.dart` — per-instance RSVP + admin update composer + admin-only Race control card (Arm / Fire Go / End / Cancel, gated on `ClubView.isRaceDirector`, backed by `SocialService.armRace` / `startRace` / `endRace`). **Slice E (typed club events):** every athletic affordance (distance / target-pace metrics, Race control, results leaderboard / Submit-my-time) is gated behind `isAthleticEventCategory(e.row.category)` — `run`/`cycle` render as today; `class`/`social` are attendance-only. A `class` with a `discipline` shows the prominent `EventDisciplineLabel` (`@visibleForTesting`) eyebrow + free-text label in the header instead. `_eventSelectCols` in `social_service.dart` selects `category, discipline` (required — `EventRow.fromJson` casts `category` non-null). It does NOT select `host_user_id` — that column stays revoked from authenticated/anon (the paid-events payout recipient, `20261228_001`/`20261230_001`), so selecting it returns 42501; mirrors web's `EVENT_SELECT_COLS`.
- `plans_screen.dart` — Training plans list (accessed from Run tab idle button)
- `plan_new_screen.dart` — Wizard with live pace + week-outline preview
- `plan_detail_screen.dart` — Hero + progress ring + today card + current-week strip (`CurrentWeekStrip`) + month calendar + week grid. **Owner-only (mirror web `/plans/[id]`):** adherence banner (`plan_adherence.dart`), Re-plan + Adaptive re-plan flows (`plan_replan.dart` / `plan_adaptive_replan.dart` → preview modal → apply), per-week Duplicate action (`TrainingService.duplicatePlanWeek`). Owner gate reads the Supabase auth uid (`viewerIdOverride` test seam). **P2 adaptive fitness gate (gated OFF):** `_proposeAdaptiveReplan` passes an `AdaptiveFitness?` from `_adaptiveFitnessInput()` — computed from the threaded `LocalRunStore`'s FULL `Run`s (carrying `metadata.avg_bpm`, NOT `_recentRuns`/`RecentRunRow`) via `computeTrainingLoadSeries`, mirroring web's `adaptiveFitnessInput`. Returns null unless `dotenv.env['ADAPTIVE_FITNESS_GATE']` is `1`/`true` (OFF by default, pending P2 CISO sign-off, decisions §144); when on + the runner is fatigued (TSB < 0), an under-trend add-volume is held with `planDetailAdaptiveFitnessHeld`. `runStore` is threaded from the Fitness hub via `PlansScreen`; other call sites pass null (P1 behaviour). Owner gate reads the Supabase auth uid (`viewerIdOverride` test seam).
- `nutrition_meal_detail_screen.dart` — Per-meal detail (mirror web `/nutrition/[date]/[slot]`): one meal slot's items + macro breakdown + a 7-day calorie trend, reached by tapping a meal-group header on `nutrition_screen.dart`. Reads the offline-first `LocalFoodStore`.
- `workout_detail_screen.dart` — Structured-interval breakdown + per-kind advice
- `gym_screen.dart` — Phase 4 multi-modal Gym (decisions §63). Workout list + PR badges + a log composer (AppBar +). Mirrors web `/gym`. Reads + writes through `LocalGymStore` (offline-first); a best-effort `api.fetchGymWorkoutsWithSets` overlay on mount hydrates the store, pending writes drain on the next online refresh. List stats (volume / exercise count) + PR badges compute from the store's inline sets via the pure top-level helpers `gymWorkoutVolume` / `gymExerciseCount` / `gymPrWorkoutIds` / `gymExerciseSuggestions` (unit-tested, mirror web `/gym`'s derivations). **Reachable (G5)** via the centre Log sheet ("Log lift") and the Home today's-lift card (`widgets/gym_summary_card.dart`).
- `gym_detail_screen.dart` — single gym workout (mirror web `/gym/[id]`): exercise blocks with per-exercise PR chips (`gym_prs.dart#workoutPrs` judged against earlier workouts in the store), each set's reps × weight + RPE, notes, owner edit / delete. Reads from `LocalGymStore`; `_ensureLoaded` pulls the list once if reached before the list hydrated the store (deep-link path). **Save as routine** (promotes the session's grouped sets into a routine draft via `gym_routine.dart#routineFromWorkout`, opens `routine_builder_sheet.dart`) + **Repeat last** (seeds a fresh gym log via the existing composer) are AppBar actions (gym-programming P1); both own a lazily-init'd `LocalRoutineStore`.
- `routine_library_screen.dart` — gym-programming P1 routine library (mirror web `/gym/routines`). Authored routines most-recently-modified first; self-hiding empty onboarding state; `New routine` opens `routine_builder_sheet.dart`; tap → `routine_detail_screen.dart`. Reads `LocalRoutineStore` (offline-first), best-effort server hydrate on mount. Reached from the Routines action on `gym_screen.dart`'s AppBar.
- `routine_detail_screen.dart` — single routine (mirror web `/gym/routines/[id]`): planned targets per exercise (reps range + weight), a **Start routine** FAB (P1 prefill-only — expands via `gym_routine.dart#prefillFromRoutine` and seeds a fresh gym log through the composer, no execution loop), and Delete behind an `AlertDialog`. Reads `LocalRoutineStore`. **Club templates (migration `20270109_001`):** an author + admin-club-gated publish-row (pick a club → `ApiClient.publishGymRoutineAsTemplate`) renders when an optional `SocialService` is wired (`fetchMyClubs` filtered to `isAdmin`; `viewerIdOverride` test seam); a "Club template" badge shows once the routine is club-owned (`StoredRoutine.clubId`).
- `nutrition_screen.dart` — Phase 4 multi-modal Nutrition (mirror web `/nutrition`). Mifflin-St Jeor macro rings vs targets (hidden when body metrics are absent — anti-clutter), meal-slot daily view, a tap-to-increment water tracker (per-day `SharedPreferences` int), and a 7-day calorie trend whose header carries the `X under/over goal/day` calorie chip + a `Protein met/total days` consistency chip (both from `nutrition_week.dart`). Reads + writes through `LocalFoodStore` (offline-first); a best-effort `api.fetchFoodLog` overlays the last 7 days on mount. Targets are resolved by the shared top-level `loadNutritionTargets(api, settings)` helper (height/sex from `fetchMyProfile`, weight from `fetchLatestBodyWeightKg`, age from the date-of-birth pref, activity-level + goal from the universal bag — set in **Settings → Body metrics**, `settings_body_metrics_screen.dart`, G5). **Reachable (G5)** via the centre Log sheet ("Log food") and the Home nutrition rings card (`widgets/nutrition_rings_card.dart`); takes an optional `settingsSync` so the rings hide when body metrics are absent.

**Top-level (`lib/`):**
- `main.dart` — app entry, Supabase init, service wiring; calls `WearAuthBridge().attach(...)` so the paired Wear OS watch inherits the Supabase session, plus `WearRoutesBridge().attach(routeStore)` so the watch's route picker reflects the user's starred subset without needing its own network
- `sync_service.dart` — bulk-sync button, auto-sync on connectivity/foreground, conflict resolution
- `wear_auth_bridge.dart` — forwards Supabase session changes to the paired Wear OS watch via a `run_app/wear_auth` method channel (native `WearAuthBridge.kt` under `android/app/src/main/kotlin/com/threkir/app/` writes to the Wearable Data Layer)
- `wear_routes_bridge.dart` — sibling of `wear_auth_bridge.dart`. Subscribes to `LocalRouteStore`; on every change pushes the user's **starred** subset (id + name + distance + waypoints) to the watch at `/saved_routes` via the `run_app/wear_routes` method channel. Native `WearRoutesBridge.kt` writes the DataItem. Watch-side `RoutesBridge.kt` listens at the same path and updates its DataStore-backed `LocalRouteStore`. Local-only "Save for offline" pin (separate from star) is gated on `LocalRouteStore.pinOffline` / `unpinOffline` / `isOfflinePinned`. See [decisions.md § 64](../../docs/architecture/decisions.md#64-offline-pin-is-a-separate-local-only-flag-from-star-phone-pushes-starred-routes-to-wear-os-via-datalayer)
- `run_notification_bridge.dart` — replaces geolocator's "Run in progress" foreground-service notification with live time/distance/pace (native `RunNotificationBridge.kt` reposts on the same channel id so the lock-screen row is live instead of static)
- `push_messaging_bridge.dart` — native-push (FCM/APNs) device-token registration, sibling of `wear_auth_bridge.dart`. Attached in `main.dart` after Supabase init; on sign-in requests the OS notification permission, fetches the FCM token, and upserts it onto `device_tokens` via `ApiClient.registerDeviceToken` (`platform = Platform.isIOS ? 'ios' : 'android'`); re-registers on `onTokenRefresh`; removes the row + deletes the platform token on sign-out so the next account on the device doesn't inherit pushes; routes a notification tap's deep link through `onOpenNotification`. The settings `push_notifications` toggle mirrors its choice to this device's `is_notifications_enabled` flag via `PushMessagingBridge.instance.setNotificationsEnabled`. **Credential gate (L4):** every call routes through the `PushMessaging` seam (`firebase_push_messaging.dart` is the production impl over `firebase_messaging`); when Firebase isn't configured on the device (no `google-services.json` / `GoogleService-Info.plist`) `isAvailable` is false and `attach()` is a complete no-op — the app compiles + runs in dev without the operator's Firebase artifacts. The server leg is the Go worker's `native_push` consumer. See [../../docs/features/native_push.md](../../docs/features/native_push.md) + [decisions.md § 166](../../docs/architecture/decisions.md). The native glue (Android `FirebaseMessagingService` / iOS `AppDelegate` APNs registration) lives outside `lib/` (`android/` / `ios/`) so it is **not** twin-shared — each app owns its native side; the operator supplies the config files.
- `firebase_push_messaging.dart` — the production `PushMessaging` impl over `firebase_messaging` + `initFirebaseForPush()` (best-effort `Firebase.initializeApp`, never rethrows). Every method is gated on `Firebase.apps.isNotEmpty` so an uncredentialed build runs inert.
- `background_sync.dart` — WorkManager periodic background sync (hourly, network-connected)
- `background_location_nudge.dart` — pure `shouldNudgeBackgroundLocation(...)` (persona #57). Android 11+ only grants "while in use" from the initial dialog; `run_screen._maybeNudgeBackgroundLocation` calls this before a run starts and, when foreground location is granted but background isn't, shows a dialog that deep-links to app settings (`openAppSettings()`). Non-blocking — the run proceeds either way. Android-only (the helper returns false on iOS). 4 unit tests in `test/background_location_nudge_test.dart`.
- `battery_optimisation_hint.dart` — pure `shouldShowBatteryOptHint(...)` (persona round-5). Aggressive OEM app-killers (Samsung Stamina, Xiaomi, OnePlus) freeze the recording foreground service mid-run unless the app is exempted from battery optimisation, silently truncating a long effort. `run_screen._maybeShowBatteryOptHint` calls this once (gated on `Preferences.batteryOptHintShown`) before a run starts and, on Android, surfaces a non-blocking dismissible dialog that deep-links to app settings. Android-only; the run proceeds either way. 4 unit tests in `test/battery_optimisation_hint_test.dart`.
- `local_run_store.dart` / `local_route_store.dart` — `ChangeNotifier`-style on-disk stores. **`LocalRunStore` is windowed** (decisions § 135): cold-load reads a compact `index.json` (one `RunSummary` per run — scalars only, no track) into the full in-memory `summaries`, and hydrates only a **resident window** of full `Run`s into `_runs` — newest `kResidentWindow` (200) by date **∪ all unsynced** (the residency invariant the sync drain relies on). Read the right seam: `summaries` / `summaryRuns` (full history, track-less) for all-time stats / filters; `runs` (the resident window) + `runById(id)` (resident → on-disk hydrate) for display / detail-nav; `recentWindow(N)` for the timeline feed; `iterateAllRuns` for a full walk. The index is a batched, atomic, crash-safe **cache** of the per-run files (never the source of truth) — a missing / drifted index self-heals via the full parallel walk on cold-load. **Don't reintroduce a full-history read of `runs`** — it's the window now; reach for `summaries`/`summaryRuns`. `residentWindow` is `@visibleForTesting`-injectable.
- `gear_backfill.dart` — pure helper `gearBackfillCandidates(gearKind, since, runs)` that filters `LocalRunStore.runs` for the activity-type+date-range that matches a new gear's `purchased_at`. Backs the post-create "Attach past runs to …" prompt on `gear_screen`. Shoes match `run` / `walk` / `hike`; bikes match `cycle`. Missing `metadata.activity_type` defaults to `run`. Newest-first sort. 9 unit tests in `test/gear_backfill_test.dart`.
- `widgets/gear_backfill_sheet.dart` — modal bottom sheet with one [CheckboxListTile] per candidate run. Calls `ApiClient.addGearToRuns(gearId, runIds)` on confirm. All candidates default to selected; user can deselect individuals or use the master "Select all / none" toggle. Online-only (requires server round-trip for `run_gear` upsert); skipped silently when api is null or signed-out. 4 widget tests in `test/gear_backfill_sheet_test.dart`.
- `local_gear_store.dart` — `ChangeNotifier`-style on-disk gear store. Mirrors `LocalRunStore`'s pattern: one JSON file per row at `<appDocs>/gear/<id>.json`, in-memory cache, full CRUD that works offline. Each row carries a `GearSyncState` (`synced` / `pendingCreate` / `pendingUpdate` / `pendingDelete`); `syncWithServer(api)` drains every non-synced row in create → update → delete order and is a no-op on a clean store. Offline-create uses a client-minted v4 UUID that becomes the server id on the eventual INSERT, so no temp-id reconciliation is needed. See [decisions.md § 73](../../docs/architecture/decisions.md#73-gear-has-a-localgearstore-mirroring-localrunstore-client-minted-uuids-sidestep-temp-id-reconciliation) for the rationale.
- `screens/gear_rotations_screen.dart` — Settings → Gear → Rotations (roadmap §7, decisions §183). Named multi-pair gear groupings ("Daily trainers", "Race day"), distinct from and additive to the single `is_default` current pair. Reached from the `sync_alt` action on `gear_screen`'s AppBar (gated on an online api). **Online-only by design** — rotations live OUTSIDE `LocalGearStore` (like the wear-log + backfill sub-flows): the screen reads/writes rotations through `ApiClient.fetchMyGearRotations` / `createGearRotation` / `renameGearRotation` / `deleteGearRotation` / `setGearRotationMembers`, and reads gear rows from the passed `LocalGearStore` for the member-checkbox sheet (`_RotationMembersSheet`). 4 widget tests in `test/gear_rotations_screen_test.dart`.
- `local_routine_store.dart` — gym-programming P1 offline cache for reusable routines (`StoredRoutine`), sibling of `local_gym_store.dart` over `OfflineSyncStore` (one JSON file per routine under `<appDocs>/routines/`, exercises + their planned sets carried **inline**, client-minted UUID = server id, `syncWithServer` drains create → delete). P1 has no edit path (build / save / delete only, mirroring web), so a routine never reaches pendingUpdate. Drives `routine_library_screen.dart` + `routine_detail_screen.dart`; pushed via the `api_client` routine CRUD (`createGymRoutine` / `fetchGymRoutines` / `fetchGymRoutineDetail` / `deleteGymRoutine`). See [gym_programming.md](../../docs/features/gym_programming.md).
- `local_gym_store.dart` / `local_food_store.dart` — Phase 4 multi-modal offline caches, same shape as `local_gear_store.dart` (one JSON file per row under `<appDocs>/gym/` and `<appDocs>/food/`, per-row sync state, client-minted UUIDs, `syncWithServer(api)` drains create → update → delete). `LocalGymStore` stores each workout's sets inline (`StoredGymWorkout.sets`, positional `set_index`) so a workout + its `gym_sets` travel as one file; the drain replays them via `createGymWorkout`/`updateGymWorkout`. `LocalFoodStore` exposes `entriesForRange(from, to)` for the single-day nutrition view. **Both share the `OfflineSyncStore` summary index** (decisions § 135): cold-load reuses a drift-validated `index.json` (a `summaryOf` record per row) for a fast summary view + the `loadInWindow(from,to)` / `estimateRowsInWindow` API, falling back to the full walk + rebuild on a missing/drifted index. Unlike `LocalRunStore` they still hydrate every full row on load (their consumers read full rows directly); gear implements `summaryOf` but writes no index (no windowed surface). **Driven by the gym/nutrition screens + the Home dashboard** — `gym_screen.dart` and `nutrition_screen.dart` each hydrate their store on mount (best-effort `api` fetch → `replaceFromServer`) and drain pending writes via `syncWithServer(api)`; `dashboard_screen.dart` also best-effort hydrates both on mount (G5) so the self-hiding today-modality cards surface on a fresh launch (using the same 7-day food window). They are **not** wired into the global `main.dart`/`sync_service` sweep (each surface owns its store's sync). See [decisions.md § 122](../../docs/architecture/decisions.md#122-gym--food-get-localgymstore--localfoodstore-mirroring-localgearstore-gym-stores-its-sets-inline-one-file-per-workout).
- `preferences.dart` — SharedPreferences wrapper for settings
- `locale_defaults.dart` — locale-derived first-run defaults (`regionOfLocale` / `defaultUnitForLocale` / `defaultWeekStartForLocale` over the device locale). Dart twin of web `format/locale_defaults.ts` (TS↔Dart parity pair; the lockstep contract is the region tables — web additionally consults `Intl` week data). Seeds the setup wizard's units step + the preferences screen's week-start fallback. Tests in `test/locale_defaults_test.dart`.
- `parkrun_regions.dart` — parkrun country footprint + `parkrunLikelyUnavailable`. Dart twin of web `integrations/parkrun_regions.ts` (TS↔Dart parity pair). Swaps the Settings → Integrations parkrun tile subtitle to a "may not have events near you" disclosure outside the footprint without gating the import. Tests in `test/parkrun_regions_test.dart` + `test/settings_integrations_parkrun_note_test.dart`.
- `tile_cache.dart` — disk-backed map tile cache glue (general LRU `map_tiles`). Distinct from the eviction-resistant `offline_tile_pack.dart` sibling cache.
- `offline_tile_pack.dart` — offline map-tile packs for a route pinned "Save for offline" (trail_navigation.md, decisions §170). `OfflineTilePackStore` (a `ChangeNotifier`) downloads the `{z}/{x}/{y}` tiles covering the route bbox (via the pure `tile_pack.dart` `tilesForBbox` + 5000-tile cap) to `${cacheRoot}/offline_packs/<routeId>/` — a **sibling** of `map_tiles` so a pinned pack is never LRU-evicted. Hooked onto `route_detail_screen`'s offline-pin toggle (download on pin, `deletePack` on un-pin). `OfflinePackTileProvider` is the read-through `TileProvider` (disk pack first, network/LRU fallback) wired into `LiveRunMap` via the optional `offlineTileProvider` param. **L4:** each tile fetch is isolated (a failed tile → partial pack + retry, never aborts the pin or the online map); injectable `TileBytesFetcher` test seam. 10 tests in `test/offline_tile_pack_test.dart`.
- `turn_cues.dart` — pure geometric turn-cue generator over a route polyline (`generateTurnCues`). Dart twin of `apps/web/src/lib/routes/turn_cues.ts` (TS↔Dart parity pair, 10 mirror tests in `test/turn_cues_test.dart`). The offline-first turn-by-turn baseline (decisions §169).
- `tile_pack.dart` — pure slippy-map tile-enumeration math (`tilesForBbox` / `estimateTileCount` + the per-pack cap). Dart twin of `apps/web/src/lib/routes/tile_pack.ts` (TS↔Dart parity pair, 8 mirror tests in `test/tile_pack_test.dart`). The fetch/write lives in `offline_tile_pack.dart`.
- `turn_cue_announcer.dart` — pure, Flutter-free decision core for turn-by-turn voice cues (`TurnCueAnnouncer`). Holds per-turn announce state and, given the runner's distance-along-route, decides which cue to fire (announce at ~300/100/0 m, once per band per turn, nearest turn only). Mobile-only (not a parity pair). Wired into `run_screen.dart`'s snapshot handler behind the `Preferences.turnByTurnCues` toggle; fires through the best-effort `_ttsCue` wrapper → `audio_cues.announceTurn`. 11 tests in `test/turn_cue_announcer_test.dart`.
- `audio_cues.dart` — TTS for splits and pace alerts, **locale-aware** (Epic C3): phrases read from the gen-l10n `tts*` keys for the active locale, engine language follows `ttsLanguageTag(activeLocaleTag)`. Pace alerts are paired with a `HapticFeedback.heavyImpact()` pulse in `run_screen.dart` (two pulses for "speed up", one for "slow down") so the cue registers with headphones paused. `announceTurn(direction, distance:)` speaks the turn-by-turn cue (decisions §169).
- `run_stats.dart` — pace/distance/split formatting helpers (tested)
- `goals.dart` — `RunGoal` model + pure `evaluateGoal` for dashboard goal cards (tested)
- `live_hub_client.dart` — HTTP push wrapper around the Go live-hub's `POST /v1/live/{run_id}/push` endpoint. `LiveBroadcaster` swaps to this path when `LIVE_HUB_URL` is in `dotenv.env`. Pluggable fetcher seam mirrors `routing.dart` / `elevation.dart` / `geocoding.dart`. See `apps/job_worker/internal/livehub/` for the server.
- `revenuecat.dart` — Dart port of `apps/web/src/lib/billing/revenuecat.ts`. RevenueCat native SDK wrapper for the in-app Pro purchase sheet. `isRevenueCatConfigured` + `configureRevenueCat` + `startProCheckout` + `managementUrl` + `proMonthlyPriceString` (the store-localised price for the Subscribe tile — Apple 3.1.1 / Play policy: the displayed amount must come from the store, not a hard-coded USD string), all gated on `dotenv.env['REVENUECAT_API_KEY_ANDROID']` / `_IOS` so unconfigured builds compile + the "Subscribe to Pro" tile in `settings_pro_screen` falls back to the existing web `/settings/upgrade` URL (and to the `$9.99` USD list price + regional note when no offering resolves). User-cancelled purchases map to `PurchaseResult.cancelled` so no red error toast on a normal dismissal. 8 unit tests pin the unconfigured behaviour. Live sheet still needs an RC dashboard / `pro_monthly` product / API keys on the operator side — see `docs/product/followups.md § #9`.
- `strava.dart` — Dart port of `apps/web/src/lib/integrations/strava.ts`. Strava OAuth helpers for the future native connect flow: `isStravaConfigured()` reads `STRAVA_CLIENT_ID` from `dotenv.env` (with the same 12345-placeholder check as the web); `stravaAuthUrl(redirectUri)` builds the `/oauth/authorize` URL with the `activity:read_all,read` scope and `approval_prompt=auto`. The connect button on `settings_integrations_screen` now drives a native `FlutterWebAuth2` flow when `STRAVA_CLIENT_ID` is in `dotenv` and falls back to the web `/settings/integrations` page otherwise. The `strava-import` Edge Function (wrapped by `ApiClient.syncStrava`) handles the code-for-token exchange. 7 unit tests on `isStravaConfigured` + URL building.
- `route_simplify.dart` — Ramer–Douglas–Peucker track simplifier used when saving a run as a route (tested)
- `routing.dart` — Dart port of `apps/web/src/lib/routes/routing.ts`. OSRM client for the in-app route builder: `snapToRoad(point, profile: foot|car)` for the nearest-road service + `fetchRouteThrough(points, profile, {cache})` for a road-snapped polyline through N waypoints. Pass a `RouteSegmentCache` (the builder holds one, clears it on Clear) so re-routing after adding one waypoint fetches only the new segment — keeps a long route responsive on every pin tap; only ok segments are cached (`segmentCacheKey` is order- + profile-scoped). Mirrors web `segment_cache.ts` — keep in lockstep (decisions §136). Pluggable `OsrmFetcher` callback so unit tests can replay canned bodies (`test/routing_test.dart`, 14 tests). Default fetcher uses dart:io's `HttpClient` (no new package dep). The public OSRM endpoint is unauthenticated and free for low volumes; if usage grows past the demo server's quota, swap the base URL for a self-hosted instance.
- `elevation.dart` — Dart port of `apps/web/src/lib/routes/elevation.ts`. Open-Meteo elevation client used by the route builder to render `N m ↑` while drawing. `fetchElevations` (batched at ≤100 points per request, falls through to all-zero on error), `calculateElevationGain` (sums positive deltas), `sampleCoordinates` (down-sample a dense polyline before the API call). Same pluggable-fetcher seam as `routing.dart`. 12 tests in `test/elevation_test.dart`.
- `geocoding.dart` — MapTiler Geocoding API client used by the route builder's AppBar place-search. `searchPlaces(query, apiKey, limit)` returns up to N `PlaceResult`s; empty-list contract on any error (keeps the search box graceful when MapTiler is unconfigured or down). 7 tests in `test/geocoding_test.dart`.
- `distance_bands.dart` — race-distance bands (5K/10K/Half/Marathon/Ultra) for `routes_heatmap_screen`. Dart twin of `apps/web/src/lib/routes/distance_bands.ts` (TS↔Dart parity pair) — `bandForDistance` badges list rows, `bandsToRanges` builds the parallel min/max arrays the `discoverable_routes_in_bbox` RPC takes. 9 tests in `test/distance_bands_test.dart`.
- `heatmap_clustering.dart` — pure-Dart pixel-proximity pin clustering for `routes_heatmap_screen` (flutter_map has no native clustering and web gets it free from MapLibre; this avoids a new twin dep). `clusterPins` greedily merges pins within `radiusPx` at the current zoom via an injected projector. 6 tests in `test/heatmap_clustering_test.dart`.
- `run_heatmap.dart` — pure grid-quantised aggregation for the personal run-track heatmap (`run_heatmap_screen.dart`). Dart twin of `apps/web/src/lib/routes/run_heatmap.ts` (TS↔Dart parity pair) — `buildHeatCells` collapses many tracks' points into weighted cells (clamped at `kMaxCellWeight`), `heatBounds` returns the `[[w,s],[e,n]]` fit box. The web GeoJSON emitters are MapLibre-specific and have no Dart twin (the mobile renderer consumes `HeatCell`s + tracks directly). 9 tests in `test/run_heatmap_test.dart`.
- `route_overlap.dart` — `detectOverlapSpans(polyline, toleranceM, maxScannedPoints)` walks segment pairs with a haversine endpoint + midpoint test plus a path-distance gap gate so dense polylines don't false-positive. Down-samples to ≤200 points for the O(n²) scan. Used by the route builder to render a purple over-stroke on out-and-back retraced sections. 7 tests in `test/route_overlap_test.dart`.
- `widgets/snap_to_start.dart` — `shouldSnapToStart(tap, existingWaypoints, toleranceM)` — pure helper for the route builder's loop-close affordance. Returns true when ≥ 3 existing waypoints AND the tap is within `kSnapToStartMetres` (30 m default) of the first one. The builder uses it both to decide whether to close the loop on tap and to flag the start pin's pulsing halo on. 5 tests in `test/snap_to_start_test.dart`.
- `health_connect_importer.dart` / `strava_importer.dart` / `csv_run_importer.dart` — bulk importers (the CSV path round-trips Settings → "Export runs as CSV"; lossy, no track; offline-first; idempotent via `csv:<iso>-<dist>-<dur>` external_id — see `decisions.md § 65`)
- `health_connect_exporter.dart` — the write-back inverse of the importer (persona #36). `writeRun(run)` maps `metadata.activity_type` → `HealthWorkoutActivityType` (pure `healthWorkoutTypeForActivity`) and calls `health.writeWorkoutData` (ExerciseSession + Distance), then best-effort writes heart rate (persona round-5): per-point chest-strap BPM from the track, or a single `metadata.avg_bpm` sample when no per-point data — sample selection is the pure `heartRateSamplesForRun(run, start)`. Needs `WRITE_HEART_RATE` (added to `AndroidManifest.xml` + `res/xml/health_permissions.xml`) alongside `WRITE_EXERCISE`/`WRITE_DISTANCE`. The HR write is wrapped in its own try/catch so a missing grant can't roll back the workout write. Opt-in per device via `Preferences.writeToHealthConnect` (off by default), toggled from Settings → Integrations; `run_screen` fires a best-effort fire-and-forget write after the local save, Android-only. iOS/HealthKit write path is gated off (needs separate entitlements). 9 tests in `test/health_connect_exporter_test.dart` (mapping + HR-sample selection).
- `widgets/live_run_map.dart` — live map with route overlay, off-route banner, NRC-style pace heatmap (when `activity` is non-null, falls through to the legacy single gradient polyline otherwise). On the run-detail surface (`showDecorations: true`) it also overlays the per-km / per-mile distance pins and the direction-arrow chevrons via `widgets/track_decorations.dart` so the trace mirrors the web run-detail map. Live recording leaves these off — the pulsing dot already makes direction implicit.
- `widgets/track_decorations.dart` — pure helpers `computeDistanceMarkers` + `computeChevrons` that walk a polyline and emit km-/mile-boundary pins and direction arrows along the track. Mirrors the `computeDistanceMarkers` + `trace-arrows` symbol layer on `apps/web/src/lib/components/RunMap.svelte`. 10 unit tests in `test/track_decorations_test.dart`.
- `widgets/track_segment.dart` — pure helpers backing the tap-to-select segment popup on the run-detail map. `nearestTrackIdx` is a linear scan, `buildSegmentAt` expands ±150 m around a click index and aggregates distance / duration / pace / avg BPM / elevation gain + loss for the slice. Mirrors `buildSegment` in `apps/web/src/lib/components/RunMap.svelte` — keep both paths in sync so a click on web and a tap on mobile produce identical stats. 9 unit tests in `test/track_segment_test.dart`.
- `widgets/pace_segments.dart` — pure helpers `buildPaceSegments` / `paceBucketForSpeed` / `ageBandFor`. Pace-coloured, age-faded segment builder for the live track. No Flutter state — unit-tested in `test/pace_segments_test.dart`
- `widgets/track_preview.dart` — pure `CustomPainter` thumbnail mirroring the SVG geometry of `apps/web/src/lib/components/TrackPreview.svelte` (viewBox short axis = 100, PAD = 4, four directional chevrons, green-start / red-end caps). `projectTrack` is the testable pure-math projection (cos(midLat) longitude correction so a square loop renders square at any latitude — see [decisions.md § 51](../../docs/architecture/decisions.md)); `isTrackRenderable` ports the web `isMoving` 5 m jitter guard. Mounted on `routes_screen`, `explore_routes_screen`, and the Routes tab of `club_detail_screen` (routes carry waypoints inline) and as the inner renderer for `RunTrackPreview`. See [decisions.md § 49](../../docs/architecture/decisions.md).
- `widgets/run_track_preview.dart` — lazy track-fetcher around `ApiClient.fetchTrackByPath` with a static URL→`List<Waypoint>?` cache (the `null` entry is a "fetch failed" sentinel so broken Storage objects don't get retried on every rebuild). Mounted on the run-list rows in `runs_screen` and the per-card thumbnail strip in `feed_screen`. Mirrors `apps/web/src/lib/components/RunTrackPreview.svelte`.
- `widgets/collapsible_panel.dart` — the run screen's expandable stats panel
- `widgets/run_share_card.dart` — portrait share card + modal sheet; captures a PNG via `RepaintBoundary.toImage` and hands it to `share_plus`
- `widgets/full_screen_form.dart` — the shared create/edit-entity presentation. `showFullScreenForm<T>(context, title:, builder:)` pushes a `MaterialPageRoute(fullscreenDialog: true)` wrapping the body in `Scaffold` + `AppBar(title)` + `SafeArea`; `FullScreenFormBody` is the scrollable column chrome (20px inset that grows to clear the soft keyboard `viewInsets` else the gesture/nav bar `viewPadding`); `FormSectionLabel` is the uppercase muted group label. Every add/edit form routes through it (gear, club, event, goal, gym, planned-workout edit) so the surface is one consistent full-screen dialog. See [decisions.md § 129](../../docs/architecture/decisions.md).
- `widgets/log_sheet.dart` — the Log capture sheet (G5): `showLogSheet` → `LogAction?` (run / lift / food — one per modality, mirroring web; food is a single entry, the meal slot is picked in the composer as on web's `/nutrition/log`). Pure `orderedLogActions(recent)` floats the most-recently-used to the top; `logActionFromWire` / `.wire` round-trip `Preferences.lastLogType`. A single Semantics focus group; the caller does the navigation so the sheet stays store/api-free. **Used by the History Log FAB (`runs_screen.dart`)**; the centre nav Log FAB uses the `log_speed_dial.dart` variant instead.
- `widgets/log_speed_dial.dart` — the centre-Log speed-dial (G5): `showLogSpeedDial` → `LogAction?`, an `Overlay`-based fan of the three actions as **icon-only buttons in a shallow arc around the docked FAB** (`_kSlotUnits`: top-centre / down-left / down-right) over a dismiss scrim, each icon riding out along its slot vector on a staggered entrance. `orderedLogActions` puts the recent action in the top-centre slot. Labels are on `Tooltip` + `Semantics` (no visible text), so tests find them via `find.byTooltip`. Same resolve-the-picked-action / caller-navigates contract as `showLogSheet`, so `home_screen`'s `_openLogMenu` swaps one for the other. Reuses `LogAction` / `orderedLogActions` from `log_sheet.dart`; no new i18n keys (reuses `logRun`/`logLift`/`logFood`/`logSheetTitle`). Distinct from the History Log FAB, which keeps the bottom sheet.
- `widgets/gym_summary_card.dart` — Home "today's lift" card (G5): title + exercise count + unit-aware volume (via `gymExerciseCount` / `gymWorkoutVolume` from `gym_screen.dart`), dumbbell glyph + label, tap → `GymScreen`. Self-hiding is the dashboard's job.
- `widgets/nutrition_rings_card.dart` — Home "today's nutrition" card (G5): the four macro rings (kcal/P/C/F) vs targets; unfilled (raw consumed numbers, no zeroed target) when targets are absent; tap → `NutritionScreen`.
- `widgets/recent_lifts_card.dart` — dashboard "Recent lifts" trend card: the five most-recent gym sessions (date + title + exercise count + unit-aware volume), each row tapping into `GymDetailScreen`, a "View all" link to `GymScreen`. Sits below the training-load chart; self-hides when the gym store is empty. Mirrors web `/dashboard`'s recent-lifts card.
- `widgets/activity_timeline_list.dart` — the unified History timeline (multi_modal.md § History): a day-grouped, reverse-chronological list of `ActivityRow`s. Source-agnostic — `runs_screen.dart` feeds it rows assembled from the local stores (`lib/local_activities.dart`); the `summary` map keys match what the server `activities` view would emit. Type-coded by leading glyph + label (run / lift / meal), run/lift rows tappable (chevron) + meal rows read-only. Grouping is the pure `lib/activity_timeline.dart` (`groupActivitiesByDay`). Hosted by `runs_screen.dart`'s History tab.
- `local_activities.dart` — pure `buildLocalActivities({runs, workouts, foods, limit})` → newest-first `List<ActivityRow>` assembled from the three offline-first local stores. The History timeline's data source (replaced the server `fetchActivities`): always all modalities, offline, live. **Bounded-build:** each source is **required to arrive newest-first** (which `LocalRunStore.runs` / `LocalGymStore.workouts` / `LocalFoodStore.rows` all guarantee), so it caps each source at `limit` *before* building rows — a rebuild is O(limit log limit) regardless of total history, instead of allocating an `ActivityRow` for every run/lift/meal and sorting the lot on every store-change rebuild. Don't feed it unsorted lists. Unit-tested in `test/local_activities_test.dart`.
- `activity_timeline.dart` — pure `groupActivitiesByDay(List<ActivityRow>)` → per-local-day buckets for `widgets/activity_timeline_list.dart`. Mirrors the web History `timelineGroups` derivation; unit-tested.
- `widgets/gear_form_sheet.dart` — full-screen-dialog gear add/edit form (`showGearFormSheet` → `GearFormResult?`, null on cancel). Name / brand / model / purchased-at / unit-aware retirement target / notes. Writes through `LocalGearStore` (offline-first). Mounted from `gear_screen`.
- `widgets/goal_editor_sheet.dart` — full-screen dialog (via `showFullScreenForm`) for creating/editing/deleting a `RunGoal` (type + period + target)
- `widgets/nutrition_log_sheet.dart` — fullscreen-dialog nutrition log composer (`showNutritionLogSheet` → `NutritionLogSheet`). Flutter twin of web `/nutrition/log`: meal-slot dropdown, debounced Open Food Facts search → tap a result → confirm-portion `AlertDialog` → log, with a manual macro-entry fallback. **Camera barcode-scan fast-path (2026-06-20, decisions §178):** a Scan-barcode `IconButton` opens a `_BarcodeScanScreen` (full-screen `MobileScanner`, EAN/UPC formats) → on detect, `food_search.dart#lookupBarcode` resolves the product → on match drops into the same `_PortionDialog`; no-match shows the not-found message, scan/lookup failure shows a distinct scan-failed message, both keeping the search / manual fallback. The whole scan+lookup is wrapped in one try/catch (L4 — never breaks manual logging); camera-permission denial renders an inline message + `openAppSettings()`. Injectable `BarcodeScanner` test seam (returns a canned raw code) drives the lookup path without a camera; the camera widget itself isn't unit-tested. Writes through `LocalFoodStore.createLocal` so logging works offline; injectable `fetcher` test seam. Resolves `true` when an entry was logged (caller kicks a sync). `food_search.dart` (Open Food Facts client — search `searchFoods` + barcode `lookupBarcode`/`parseOffProduct`/`normaliseBarcode`, injectable fetcher like `routing.dart`) + `nutrition_totals.dart` (`sumMacros` / `groupByMealSlot` / `ringFraction`) are the pure helpers behind the screen + sheet — Dart twins of the web `food_search.ts` / `nutrition_totals.ts`. Camera dep: `mobile_scanner` (BSD-3) + Android `CAMERA` permission / iOS `NSCameraUsageDescription`.
- `widgets/gym_compose_sheet.dart` — fullscreen-dialog gym-workout composer (`showGymComposeSheet` → `GymComposeSheet`). Flutter twin of web `GymEditor.svelte`: optional title, free-text exercise name with `RawAutocomplete` over the user's history, inline sets (reps / weight-kg / RPE), add/remove exercise + set, share-to-feed toggle. Writes through `LocalGymStore.createLocal` / `updateLocal` so logging a lift works offline. Resolves `true` when a workout was saved (the caller kicks a sync). Mounted from `gym_screen` (new) + `gym_detail_screen` (edit). The optional `seedSets`/`seedTitle` prefill a NEW log on the create path — the gym-programming P1 "Start routine" / "Repeat last" entry seeds the composer with a routine's planned targets (or a prior session's sets) as the new session's actuals.
- `widgets/routine_builder_sheet.dart` — fullscreen-dialog routine builder (`showRoutineBuilderSheet` → `RoutineBuilderSheet`, resolves the new routine's id). Flutter twin of web `RoutineEditor.svelte` (+ the `gym_compose_sheet.dart` idiom): routine name, free-text exercise with `RawAutocomplete`, inline planned sets (target reps / weight), add/remove exercise + set. `normaliseExerciseName` stamps the `exercise_key`; weight entry honours the `weight_unit` pref. Writes through `LocalRoutineStore.createLocal` (offline). gym-programming P1 — build / save only, no execution / supersets / progression.
- `widgets/upcoming_event_card.dart` — Run tab idle-state card shown when the user has a `going` RSVP within 48h
- `widgets/todays_workout_card.dart` — Run tab idle-state priority card when an active plan has a workout scheduled today
- `widgets/plan_calendar.dart` — Dart port of `apps/web/src/lib/components/PlanCalendar.svelte`. Month-by-month grid with prev/next chevrons, Monday-first DOW row, kind-coloured cells, completed-tick. Renders on `plan_detail_screen` between the today card and the week cards.
- `widgets/current_week_strip.dart` — Dart twin of `apps/web/src/lib/components/CurrentWeekStrip.svelte`. Focused current-week 7-day planned-vs-completed ribbon, Monday-first, kind-coloured cells + completed-tick + a `done / active` count. The window is anchored to `startDate + weekIndex*7` (the plan's week bucket, NOT the calendar week) so the count matches the week card. Renders on `plan_detail_screen` between the re-plan section and the month calendar; tapping a cell opens the workout. 4 widget tests in `test/current_week_strip_test.dart`.
- `widgets/this_week_strip.dart` — Dart twin of `apps/web/src/lib/components/ThisWeekStrip.svelte`. Dashboard activity ribbon for the REAL calendar week containing today (7 day cells, busiest-day-scaled bars, today highlight + future dimming, header distance + activity count). Honours the `week_start_day` pref + the user's `DistanceUnit`; day-bucketing lives in the pure `current_week.dart` helper. Distinct from `current_week_strip.dart` (plan-anchored). Mounts on `dashboard_screen.dart` below the period-stat row; 6 widget tests in `test/this_week_strip_test.dart`.
- `widgets/workout_edit_sheet.dart` — full-screen dialog (via `showFullScreenForm`) for inline edit of a `PlanWorkoutRow` (kind, target distance, target pace, notes). Opened via the pencil button or long-press on a workout row in `plan_detail_screen`; saves through `TrainingService.updateWorkout`.
- `widgets/workout_execution_band.dart` — top-of-map overlay shown while a structured workout is running. Reads through a `ValueListenable<WorkoutBandState>` so transitions / progress events don't trigger a Stack-wide rebuild. Surfaces the current step label, pace pip with signed delta, progress bar + remaining metres, and Skip / Abandon controls.
- `widgets/workout_review_section.dart` — post-run planned-vs-actual table on `run_detail_screen`. Reads `runs.metadata.workout_step_results` + `workout_adherence`; hidden when those keys are absent. Mirrors the web `/runs/[id]` Workout section. Pure widget + helpers (`paceDeltaOf`, `formatPace`) — no controller state, widget-tested.
- `widgets/run_social_section.dart` — kudos pill + one-level comment thread + composer for `run_detail_screen`. Self-fetches engagement and `fetchRunCommentsWithAuthors` on mount. Optimistic kudos toggle with rollback. Replies bucket client-side; author/owner gate the Delete affordance. Mirrors `apps/web/src/lib/components/RunSocial.svelte`.
- `widgets/run_photos.dart` — run-detail photo gallery. Owner gates Add photo (uses `image_picker` to pick from gallery, runs the bytes through `stripJpegExif` to drop EXIF/XMP GPS before upload — persona #52 — then uploads via `ApiClient.addRunPhoto` to the `run-photos` Storage bucket), inline caption edit + delete. Tap a tile for a full-screen `InteractiveViewer` lightbox. Mounted on both `run_detail_screen` (owner: full controls) and `public_run_screen` (read-only when viewer is not the owner). Mirrors `apps/web/src/lib/components/RunPhotos.svelte`.
- `widgets/route_photos.dart` — route-detail photo gallery (backlog C1 — the `run_photos` capability applied to routes). Same shape as `run_photos.dart`: owner gates Add photo (EXIF-stripped, uploaded via `ApiClient.addRoutePhoto` to the private `route-photos` bucket), caption edit, delete, lightbox. Mounted on `route_detail_screen` (owner controls) and `public_route_screen` (read-only for non-owner). Mirrors `apps/web/src/lib/components/RoutePhotos.svelte`. Helpers pinned by `test/route_photos_helpers_test.dart` (11 tests).
- `widgets/route_conditions.dart` — route-detail community condition-reports panel (trail_navigation.md, decisions §171). Lists reports newest-first (condition chip + severity colour + relative age + optional "at X km" + note, fading a report older than 30 days) with a report composer for a signed-in viewer who can see the route. Reads through `ApiClient.fetchRouteConditions` (the privacy-redacting RPC; failures fall back to empty, L4); author-or-owner gates Delete (`AlertDialog`). Mounted on `route_detail_screen` (full) + `public_route_screen` (`canReport: false`). Mirrors `apps/web/src/lib/components/RouteConditions.svelte`. 5 widget tests in `test/route_conditions_test.dart`.
- `widgets/club_form_sheet.dart` — full-screen-dialog create-club form (name, description, location, public/private, join policy). Auto-slugifies the name; visibility ↔ policy linking matches web `ClubEditor.svelte`. Mounted from the FAB on `clubs_screen.dart`.
- `widgets/event_form_sheet.dart` — full-screen-dialog create-event form. **Typed events (slice E):** a `category` picker (run/cycle/class/social) is the first control, a free-text `discipline` field shows only for a `class`, and the athletic distance field self-hides + clears off the athletic categories via `isAthleticEventCategory` (mirrors web `EventEditor.svelte`'s `pickCategory`); `SocialService.createEvent`/`buildCreateEventBody` persist `category`/`discipline`/`gym_template`. Plus title, starts-at picker, description, meet label, distance / duration, recurrence none/weekly/biweekly/monthly with auto-byday from the picked DOW. **Members-only toggle (§148):** a `SwitchListTile` shown only when `clubIsPublic` (threaded from `club_detail_screen` as `c.row.isPublic`) writes `events.is_public=false` via `buildCreateEventBody`; hidden for a private club whose events are already members-only. Mounted from the admin Create button on the Events tab of `club_detail_screen`.
- `widgets/run_segment_efforts.dart` — per-run segment-effort chips on `run_detail_screen`. On mount, when the viewer owns the run and it's linked to a saved route, walks the track via `lib/segments.dart#autoComputeEffortsForRun` to insert any new efforts (idempotent against the unique(segment_id, run_id) constraint), then fetches the joined effort + segment + rank rows via `ApiClient.fetchEffortsForRunWithSegments`. Mirrors `apps/web/src/lib/components/RunSegmentEfforts.svelte`.
- `widgets/segments_panel.dart` — route-detail segments panel on `route_detail_screen`: lists every segment, expand-on-tap to a per-segment leaderboard via `fetchSegmentLeaderboardWithAthletes`. Route owner gets a "New segment" form (name + start_m + end_m, ≥100 m enforcement) and a per-row delete button (uses `ApiClient.deleteSegment`). Mirrors `apps/web/src/lib/components/SegmentsPanel.svelte`.
- `widgets/training_load_chart.dart` — dashboard "Fitness, Fatigue & Form" `CustomPaint` chart. Reads HR prefs from `SettingsService` (`resting_hr_bpm` + `max_hr_bpm`); renders three line series (CTL / ATL / TSB) over the last 90 days plus a dashed zero line, a legend, and a one-line reading. Mirrors `apps/web/src/lib/components/TrainingLoadChart.svelte`.
- `widgets/intensity_card.dart` — dashboard "Training Intensity" card showing time-in-zone (Z1..Z5) over the last 30 days. Self-hides when the user has no `hr_zones` configured or no HR-tracked runs in the window. Renders a 5-segment bar + per-zone legend with rounded percentages (uses `<1%` rather than `0%` for non-empty slivers). Backed by `lib/run_intensity.dart`. Mirrors the inline intensity-breakdown card on web's `/dashboard`.
- `widgets/mileage_trend_card.dart` — dashboard "Mileage" card with a weekly / monthly / yearly SegmentedButton toggle (12-bucket window in each mode). Renders a bar chart of total distance per bucket; bar height is fraction of the tallest bucket, numeric value above the bar honours the user's `DistanceUnit`. Self-hides when there are no runs. Mirrors web's mileage-chart card on `/dashboard` — same Monday-anchored ISO-week grouping, same metres-stored / unit-formatted-on-render split.
- `run_intensity.dart` — pure helpers backing `widgets/intensity_card.dart`. `computeIntensityBreakdown(runs, zones, windowDays, now)` classifies each run by `metadata.avg_bpm` against the supplied 5-cutoff ladder; `parseHrZones(raw)` parses the universal-bag `hr_zones` map into a strictly-ascending 5-element list (shared with `run_detail_screen` for the per-run HR-zone panel).
- `mileage_trend.dart` — pure helper backing `widgets/mileage_trend_card.dart`. `aggregateMileage(runs, view, now, maxBuckets)` groups runs by `MileageView.{weekly, monthly, yearly}` and returns the most-recent N `MileagePeriod` buckets (label + summed `distanceM` + `startsAt` sort key) in chronological order. Weekly buckets are Monday-anchored to match `lib/goals.dart`.
- `training_load.dart` — pure Dart port of `apps/web/src/lib/training/training_load.ts` (decisions §34). Same TRIMP-when-HR / distance-fallback stress ladder, same daily aggregation, same EWMA trio (ATL halflife 7, CTL halflife 42); `hasTrimpSignal` drives the "HR-based" vs "volume-based" subtitle.
- `segments.dart` — pure Dart port of `apps/web/src/lib/segments/segments.ts` (decisions §37). `computeEffortFromTrack` walks a track once, accumulates haversine distance, and interpolates timestamps at the start / end crossings; `autoComputeEffortsForRun` fans the walker over every segment on a route and posts new efforts. Sparsity guard mirrors web (median sample step must be ≤ segLen / 5). 8-test mirror suite in `test/segments_test.dart`.
- `privacy.dart` — pure Dart port of `apps/web/src/lib/routes/privacy.ts` (decisions §33). `PrivacyZone` shape, `isInAnyZone` haversine check, `clipPointsToZones` walks-from-each-end clipper. Owner-side preview only — non-owner tracks go through the `clip_track_for_user` SECURITY DEFINER RPC server-side. 8-test mirror suite in `test/privacy_test.dart`.
- `widgets/fitness_card.dart` — the dashboard's Fitness card (VO₂ max + VDOT + qualifying-run count, then CTL / ATL / TSB, then a recovery-advice line). Renders nothing when `qualifyingRunCount == 0`. Reuses `lib/fitness.dart` for the math; widget-tested.
- `widgets/race_predictor_card.dart` — the dashboard's Race-time predictor card (mirror web `RacePredictorCard.svelte`): the 5K / 10K / Half / Marathon ladder with each rung's predicted time + unit-aware pace + a confidence chip (long-press tooltip = the reason). Feeds `race_predictor.dart#predictRaceLadder` the `qualifyingRuns` mapped to efforts; self-hides when the gate yields no anchor. Mounts on `dashboard_screen.dart` below `FitnessCard`. Widget-tested.
- `social_service.dart` — `ChangeNotifier` wrapping all Supabase calls for clubs / events / posts
- `training_service.dart` — `ChangeNotifier` wrapping Supabase calls for training plans + workouts
- `ble_heart_rate.dart` — BLE chest-strap GATT client for live BPM stream (tested); wires into the run screen via `BleHeartRate.stream`. Auto-reconnects on a mid-run drop (exponential backoff via the pure `bleReconnectDelay`, capped at 20 attempts) and exposes `BleHrStatus` transitions on `statusStream` so `run_screen` can disclose the drop. Initial connect failures (strap off at launch) don't auto-retry — they now emit the distinct `BleHrStatus.connectFailed` (vs `disconnected`) so `run_screen` shows a one-tap "Reconnect" banner (`_reconnectHeartRate` → `BleHeartRate.reconnect()`, a thin `connectCached` wrapper). Only a connection that dropped after working triggers the auto-reconnect loop.
- `ble_treadmill.dart` — BLE FTMS treadmill GATT client (service 0x1826, Treadmill Data characteristic 0x2ACD). Same GATT-client/status-stream/auto-reconnect shape as `ble_heart_rate.dart`: pure `parseTreadmillData` decodes flags + instantaneous speed (km/h, 0.01) + total distance (uint24 m) + inclination (sint16 0.1%) into a `TreadmillSample`; `bleTreadmillReconnectDelay` is the backoff schedule. Constructed once as an **app-owned singleton** in `main.dart` (`connectCached` kicked at startup, like `BleHeartRate`) and threaded through `HomeScreen` → `RunScreen` (the `treadmill` constructor arg) and `HomeScreen` → `YouScreen` → `SettingsScreen` → `SettingsIntegrationsScreen` → `TreadmillTile`, so the belt paired in Settings is the same instance the run screen reads — no second GATT client. Paired from Settings → Integrations via `TreadmillTile`; persists to its own `treadmill_device_id`/`treadmill_device_name` SharedPreferences keys (device-local, not the synced settings bag — see `docs/backend/settings.md`). **Additive/opt-in distance source** for the recorder via the `setTreadmillSample` seam (see below); never participates in a GPS run. **Live run-screen toggle shipped** — `run_screen.dart` `_toggleTreadmillMode` renders a paired-belt-gated `SwitchListTile` over the recording view; turning it on pumps belt samples into `setTreadmillSample` (L4 opt-in, own try/catch + banner-revert, never disturbs the L0/L1 path), turning it off calls `clearTreadmillMode`. `debugEmitSample`/`debugEmitSampleError`/`debugEmitStatus` are `@visibleForTesting` seams for driving the screen's wiring without a BLE stack.
- `hr_zones.dart` — pure helpers `hrZoneBreakdown` (time-weighted, 30 s pause cap) and `bpmStatsOf`. No Flutter state — unit-tested. Backs the HR-zone panel on `run_detail_screen` for runs whose track carries per-point `bpm` (Strava, FIT/TCX imports, watch recorders).
- `fitness.dart` — Dart port of `apps/web/src/lib/training/fitness.ts` (VDOT / VO₂ max from Daniels' formula, EWMA-based ATL/CTL/TSB, `recoveryAdvice`). Pure functions; backs the dashboard's Fitness card. Keep in sync with the web module.
- `race_controller.dart` — live-race orchestration: pings spectator feed, auto-submits finisher time to the leaderboard
- `settings_sync.dart` — reads/writes the user-preferences row so settings roam across devices. Accepts a `SettingsCache` so the bag-backed prefs stay readable + writable when offline; on cold start the cached bags hydrate before the server fetch completes, on offline-edit the change is mirrored to cache and queued for the server.
- `settings_cache.dart` — `SharedPrefsSettingsCache`: SharedPreferences-backed [`SettingsCache`] implementation. Keys are user-scoped (and device-scoped for the device bag) so a sign-out + sign-in as a different user on the same device can't read another user's data. Pending writes are JSON-encoded so a new prefs key round-trips without a cache-format migration.
- `backup.dart` — export + import of the local run / route stores (troubleshooting + device-swap path)
- `recurrence.dart` — Dart port of `apps/web/src/lib/social/recurrence.ts`, keep in sync
- `training.dart` — Dart port of `apps/web/src/lib/training/training.ts` (VDOT, Riegel, plan generator); keep in sync, 17-test mirror suite in `test/training_test.dart`
- `plan_adherence.dart` — Dart twin of `apps/web/src/lib/training/plan_adherence.ts` (`weeklyDrift` >±20% over/under + `missedWorkoutAdvice` make-up/skip). Pure; 11-test mirror suite in `test/plan_adherence_test.dart`. Backs the plan-detail adherence banner.
- `plan_replan.dart` — Dart twin of `apps/web/src/lib/training/plan_replan.ts` (`replanRemaining` — make-up a missed long ≤+15%, ease-off after over-running; past + taper + explicitly-skipped frozen). Reuses `plan_adherence.dart`; 13-test mirror suite in `test/plan_replan_test.dart`. Backs the plan-detail Re-plan flow.
- `relink_candidates.dart` — Dart twin of `apps/web/src/lib/training/relink_candidates.ts` (`filterRelinkCandidates` — eligible runs to re-link to a planned workout: ±7-day window, excludes runs already linked to another workout so `plan_progress` can't double-count, keeps the current run). Pure; 9-test mirror suite in `test/relink_candidates_test.dart`. Backs the `workout_detail_screen` Re-link picker; `TrainingService.fetchRelinkCandidates` does the owner-scoped fetch.
- `race_predictor.dart` — Dart twin of `apps/web/src/lib/training/race_predictor.ts` (`predictRaceLadder` — multi-distance race-time predictor: 5K/10K/Half/Marathon ladder from recency-weighted qualifying efforts, anchor by recency-weighted Riegel equivalence, per-rung confidence via `predictionConfidence`). Pure; reuses `riegelPredict` + `predictionConfidence` from `training.dart`; 11-test mirror suite in `test/race_predictor_test.dart`. Backs `widgets/race_predictor_card.dart` on the Home dashboard (mirrors web's `RacePredictorCard` on `/dashboard`). Backlog #11.
- `backend_timeout.dart` — guarded `Future.timeout` wrapper for every PostgREST + Storage call so a single hung request can't lock the UI for the system default. Used by the home screen's pull-to-refresh and the sync paths.
- `watch_ingest_queue.dart` — disk-backed buffer for incoming watch payloads. iOS only writes to it (the Android-side method channel is a no-op); replays on sign-in / connectivity return so a phone restart doesn't lose a run that landed from the watch while the user was signed out.
- `widgets/error_state.dart` — reusable empty/error/retry surface used by every screen that fetches from Supabase. Widget-tested.
- `widgets/route_share_card.dart` — portrait-orientation share card for routes (sibling of `widgets/run_share_card.dart`). Same `RepaintBoundary.toImage` → `share_plus` flow.
- `widgets/top_banner.dart` — top-anchored notification pill, the canonical replacement for `ScaffoldMessenger.showSnackBar` across the mobile app. Renders via `Overlay`, auto-positions below the nearest `AppBar`, and coalesces to a single banner at a time. See [../../docs/architecture/conventions.md § Mobile in-app notifications](../../docs/architecture/conventions.md#mobile-in-app-notifications--showtopbanner).
- `widgets/report_sheet.dart` — modal report sheet behind `showReportSheet(context, api:, targetKind:, targetId:)`. Five fixed reasons (`spam` / `harassment` / `inappropriate` / `impersonation` / `other`) keyed to the `reports` CHECK constraint, optional notes (trimmed; empty → null so the moderator dashboard's "had context" filter stays accurate). `targetKind ∈ {'user','club','route','comment','club_post','run'}` (the title switch renders a per-kind title, falling back to "Report content"). Mounts the flag IconButton in the AppBar of `profile_screen` (when not self), `club_detail_screen`, `route_detail_screen` (when not owner), `public_run_screen` (when a logged-in non-owner views a public run → `run`), and on each non-author club-feed post in `club_detail_screen` (→ `club_post`). Friendly copy for the 23505 duplicate-report path; P0001 rate-limit routed through `rate_limit_errors.dart`. Mirrors web's `submitReport` flow. 10 widget tests in `test/report_sheet_test.dart`.
- `fit_export.dart` — FIT-binary writer for runs, used by Settings → Export run. Sibling of `lib/gpx_export.dart`. 3 tests in `test/fit_export_test.dart`.
- `exif_strip.dart` — pure `stripJpegExif(Uint8List)` — a lossless JPEG marker-walk that removes the APP1 (EXIF + XMP, incl. GPS) segment and copies every other segment (JFIF/APP0, ICC/APP2) + scan data verbatim. Non-JPEG input returned unchanged. Called by `widgets/run_photos.dart` before photo upload so a geotagged original never reaches the bucket ahead of the server worker's async strip (persona family-club #52). 6 tests in `test/exif_strip_test.dart`.
- `finisher_certificate.dart` — pure finisher-certificate shaping (`isCertificateEligible` + `formatCertificateTime` / `formatCertificateDistance` / `ordinalPlace`). TS↔Dart parity twin of `apps/web/src/lib/runs/finisher_certificate.ts` (web builds an SVG string; mobile renders natively, so only the unit/locale-agnostic shaping is shared). Backs the event-detail finisher certificate (persona #44). 4 test groups in `test/finisher_certificate_test.dart`.
- `widgets/finisher_certificate_card.dart` — the downloadable finisher certificate on mobile. `showFinisherCertificateSheet(...)` opens a modal hosting `FinisherCertificateCard` (a landscape card mirroring the web SVG — cream ground, amber double border, finisher name hero, time/distance/placing) and shares the `RepaintBoundary.toImage` PNG via `share_plus` (the run-share-card idiom). Mounted on each finished + organiser-approved row of `EventResultsSection` in `event_detail_screen.dart`. Widget-tested in `test/finisher_certificate_test.dart`; row visibility pinned in `test/event_results_section_test.dart`.

## Dart analyzer policy — treat `info` as noise

`dart analyze` on this package reports ~2100 issues. **Every remaining entry is `info`-level** — the package is clean of `warning`/`error` as of this pass. The noise buckets (top categories from `dart analyze | grep -oE ' - [a-z_]+$' | sort | uniq -c | sort -rn`):

- `always_use_package_imports` (~220) and `avoid_relative_lib_imports` (~140) — these two interact with [decisions.md § 39](../../docs/architecture/decisions.md#39-mobile_android-and-mobile_ios-share-a-byte-for-byte-dart-codebase): tests use relative imports so the same file resolves on both targets. Not being fixed.
- `deprecated_member_use` (~46) — mostly `withOpacity` → `withValues` and `share_plus` v13 (`Share.shareXFiles` → `SharePlus.instance.share`). Deferred until we do a theme/deps pass.
- `unnecessary_underscores` / `use_null_aware_elements` / `prefer_single_quotes` / `use_build_context_synchronously` — small stragglers.

**Do not waste a turn on these.** Only act on `info` if your change touched that specific file. **Do act on any new `warning`/`error`** — the bar is "zero warnings", so a fresh one is a regression your change introduced. The CI "Test Flutter packages" job runs `melos exec -- dart analyze` and **its exit code is enforced** — `dart analyze` exits non-zero on any `warning`/`error` (info-level is not fatal), so a single fresh `warning` anywhere in the melos-analyzed packages (incl. `test/`) reds the job. This bit us once (run 28033171233): three `unused_element_parameter` warnings on never-exercised test-fake params + one `unnecessary_cast` sat red on `main` for days because an earlier note here wrongly claimed the exit code was ignored. It is not — fix the warning at the root.

## Tests

Test files in `test/`:
- `run_stats_test.dart` — 13 tests: moving-time helpers + `fastestWindowOf` rolling-window scanner
- `local_run_store_test.dart` — 64 tests: store persistence, sync state, in-progress save/load, deleteMany batch, newer-wins guards, `_v` schema-version stamp + legacy-record load, edge cases, plus the **summary index** group (index.json written/maintained per mutation, markSynced flips in-memory only, structurally-invalid index tolerated), the **windowed API** group (runById / recentWindow / hydrateOlder / iterateAllRuns), and the **windowed cold-load** group (index-first fast path hydrates only the window, runById disk hydration, unsynced-stays-resident, drift rebuild on missing index / orphan file). Uses `residentWindow` injection to exercise windowing without seeding 200+ runs.
- `local_gym_store_test.dart` — 37 tests for `lib/local_gym_store.dart` (incl. the `OfflineSyncStore` summary-index + `loadInWindow`/`estimateRowsInWindow` + drift-rebuild coverage): createLocal mints a v4 UUID + marks pendingCreate, sets survive reload, newest-started sort; updateLocal keeps pendingCreate (and replaces sets) / flips synced→pendingUpdate / preserves an omitted set list; delete tombstone semantics; replaceFromServer preserves pending + hydrates server sets + **newer-wins** (a stale server fetch can't clobber a locally-newer synced copy; a newer fetch overwrites); drain order create → update → delete with the local id + the inline sets, per-row failure isolation, clean-store no-op; `_rewriteAll` crash-atomic ordering — orphan cleanup keeps pending + leaves no temp files, and per-write failure isolation (a directory planted at a row's file path forces that row's atomic rename to fail while the others still write and a stale orphan is still cleaned); `_v` schema-version stamp on writes + legacy-record load via the migration hook. Mirrors `local_gear_store_test.dart`.
- `local_food_store_test.dart` — 36 tests for `lib/local_food_store.dart`: same lifecycle + drain + `_rewriteAll` crash-atomic / per-write-isolation + **newer-wins** + `_v` schema-version coverage as the gym store, plus `entriesForRange` half-open day-window filtering and newest-logged sort, and the shared `OfflineSyncStore` summary-index + `loadInWindow`/`estimateRowsInWindow` + drift-rebuild coverage. Mirrors `local_gear_store_test.dart`.
- `gym_screen_test.dart` — 7 tests for `lib/screens/gym_screen.dart`: 5 for the pure list helpers (`gymWorkoutVolume` sums reps·weight ignoring incomplete sets, `gymExerciseCount` distinct case-insensitive, `gymPrWorkoutIds` flags the first workout + any later one that beats it / ignores empties, `gymExerciseSuggestions` most-used-first keeping the original spelling) + 2 widget (offline empty state renders, a seeded workout row shows title / exercise count / PR badge).
- `full_screen_form_test.dart` — 4 widget tests for `lib/widgets/full_screen_form.dart`: `showFullScreenForm` pushes a fullscreen-dialog route with an AppBar title + body + Close affordance (not Back), resolves to the popped value; `FullScreenFormBody` renders children in a `SingleChildScrollView`; `FormSectionLabel` uppercases.
- `gear_form_sheet_test.dart` — 3 widget tests for `lib/widgets/gear_form_sheet.dart`: opens as a full-screen dialog (AppBar + Close, the "Add shoes" heading, Name field); Cancel returns null and writes nothing; entering a name + Add writes a `pendingCreate` row to `LocalGearStore` (the in-memory write before createLocal's file-write await — the popped result + dismissal are covered by `full_screen_form_test`).
- `gear_rotations_screen_test.dart` — 4 widget tests for `lib/screens/gear_rotations_screen.dart` (fake `ApiClient` + real `LocalGearStore`): the empty state renders the onboarding copy; a seeded rotation renders its name + member count; opening the member sheet + toggling a gear + Done calls `setGearRotationMembers` with the toggled id set; deleting a rotation confirms via the `AlertDialog` then calls `deleteGearRotation`.
- `gym_compose_sheet_test.dart` — 3 widget tests for `lib/widgets/gym_compose_sheet.dart`: empty save shows the validation error and writes nothing; entering an exercise + saving creates a `pendingCreate` workout with the right title + inline set; editing pre-fills the title + set list from a synced stored workout (the update→pendingUpdate write path is covered by `local_gym_store_test`).
- `local_routine_store_test.dart` — 5 tests for `lib/local_routine_store.dart` (gym-programming P1): createLocal mints a v4 UUID + marks pendingCreate + drops blank-named exercises; routines reload from disk newest-modified-first with inline exercises/sets surviving the round-trip; deleteLocal on a pendingCreate drops it outright; deleteLocal on a synced routine writes a pendingDelete tombstone; replaceFromServer preserves a local pendingCreate.
- `routine_builder_sheet_test.dart` — 2 widget tests for `lib/widgets/routine_builder_sheet.dart`: empty title shows the validation error and writes nothing; a seeded title + exercise saves a `pendingCreate` routine with the right title / exercise_key / target reps + canonical-kg weight.
- `routine_library_screen_test.dart` — 2 widget tests for `lib/screens/routine_library_screen.dart`: the empty library renders the self-hiding onboarding state (no rows); a seeded routine renders a row with its title + exercise count.
- `routine_detail_screen_test.dart` — 6 widget tests for `lib/screens/routine_detail_screen.dart`: a seeded routine renders its planned targets (single-value + range rep labels) and the Start FAB; a missing routine renders the not-found state (no Start FAB); plus the club-template group — the publish-row shows for an author of a personal routine with an admin club, hides when the viewer has no admin club / is not the author, and the "Club template" badge shows when `club_id` is set.
- `food_search_test.dart` — 16 tests for `lib/food_search.dart` (Dart twin of web `food_search.ts`): `parseOffSearch` maps Open Food Facts products + drops nameless/calorie-less ones + keeps the first brand, tolerates string-typed nutriments, returns `[]` on malformed input; `scalePortion` scales per-100g to a gram portion (rounded, 0 g → 0); `searchFoods` skips the fetcher on an empty query, parses a canned response via the injected fetcher, **rethrows** a fetch failure (so the caller shows a retry state, not a misleading empty), and treats valid-but-empty JSON as genuinely empty (`[]`); plus the **barcode** group — `normaliseBarcode` strips non-digits / rejects empty, `parseOffProduct` maps a found single-product `{status, product}` response + returns null for missing (`status != 1`) / unloggable, `lookupBarcode` skips the fetcher on a blank/non-numeric code, parses a found product, returns null on a genuine no-match (`status 0`), and **rethrows** a fetch failure (so the caller can tell "not in DB" from "lookup failed").
- `nutrition_totals_test.dart` — 4 tests for `lib/nutrition_totals.dart` (Dart twin of web `nutrition_totals.ts`): `sumMacros` sums fields treating null as zero (and zeros for an empty day); `groupByMealSlot` orders slots and omits empty ones (null slot folds into snack); `ringFraction` clamps to [0,1] and returns null on a missing/zero target.
- `nutrition_screen_test.dart` — 3 widget tests for `lib/screens/nutrition_screen.dart`: the empty state renders with the rings + water cards when nothing is logged today; a logged meal shows under its meal-slot group with calories (350 appears twice — the calories ring centre + the meal-row value); the water tracker increments by one 250 ml unit on tap (`SharedPreferences` mock).
- `nutrition_log_sheet_test.dart` — 8 widget tests for `lib/widgets/nutrition_log_sheet.dart`: manual entry logs a food item to the store (name + meal slot + calories); search renders Open Food Facts results via the injected fetcher; a failed search / failed save surfaces the right state; plus the **barcode-scan** group (over the injectable `BarcodeScanner` + `fetcher` seams — the camera itself isn't unit-tested): a scanned barcode that matches opens the confirm-portion dialog, a no-match shows the not-found message (with the search/manual fallback intact), a lookup failure shows the distinct scan-failed message, and a cancelled scan is a no-op. (the confirm-portion + store-write path is also covered by `local_food_store_test` + `food_search_test`).
- `log_speed_dial_test.dart` — 3 widget tests for `lib/widgets/log_speed_dial.dart`: `showLogSpeedDial` fans the three capture options + resolves to the picked one (then tears the overlay down); a scrim tap dismisses + resolves null; the recent action sits nearest the FAB (greater dy) in the upward fan.
- `lift_load_test.dart` — 3 tests for `lib/lift_load.dart` (mirror of web `gym/lift_load.test.ts`): `liftsFromSetHistory` groups flat set rows by workout, drops rows with no workout id / date, and the grouped lifts raise the load series (separability — run-only `atl` recoverable).
- `recent_lifts_card_test.dart` — 4 widget tests for `lib/widgets/recent_lifts_card.dart`: renders nothing on an empty list; header + View-all + a row per workout; caps at five most-recent; row tap fires `onOpenWorkout`, View-all fires `onViewAll`.
- `activity_timeline_test.dart` — 4 tests for `lib/activity_timeline.dart`: `groupActivitiesByDay` empty input, buckets by local calendar day preserving order, same-day-different-time stays one bucket, sequential (non-global-sort) grouping re-opens a day.
- `activity_timeline_list_test.dart` — 4 widget tests for `lib/widgets/activity_timeline_list.dart`: empty state, per-kind run/lift/meal summaries (distance+duration / title+sets+volume / item+kcal) + Today header, run/lift tappable + meal read-only (chevron count), lift volume in the user weight unit.
- `local_activities_test.dart` — 4 unit tests for `lib/local_activities.dart#buildLocalActivities`: merges run/lift/meal newest-first with the right `summary` keys (incl. set-count + Σ volume, incomplete sets counted but adding no volume); drops a workout with no `started_at` + a meal with an unparseable date; caps at the limit keeping the newest; per-source cap still surfaces the global newest across all three modalities (the bounded-build correctness pin).
- `runs_screen_timeline_test.dart` — 6 widget tests for the History timeline on `runs_screen.dart`, seeding the **local stores** (the timeline's source): chips appear once a second modality has data; **every chip (incl. Runs) stays on the unified timeline filtered by kind** (`ActivityTimelineList` stays mounted; Runs is not the inline run list); a single-modality tab shows a `View all` link while All does not; the **add FAB follows the active chip** (All → a "Log" FAB that opens the run/lift/meal picker; Runs → Add run; Lifts → Log lift; Meals → Log food) and a run-only mount keeps the plain Add-run FAB; no chips / inline run list when the gym store is absent (run-only).
- `period_summary_test.dart` — 23 tests: period boundary computation, stats aggregation, share text generation, formatting helpers
- `goals_test.dart` — 20 tests: goal evaluation (distance/time/pace/run-count, weekly/monthly, multi-target)
- `route_simplify_test.dart` — 8 tests: Ramer-Douglas-Peucker track simplification
- `training_test.dart` — 18 tests: VDOT, Riegel, pace derivation, plan generation (mirrors `apps/web/src/lib/training.test.ts`)
- `plan_adherence_test.dart` — 11 tests for `lib/plan_adherence.dart` (mirror of `apps/web/src/lib/training/plan_adherence.test.ts`): `weeklyDrift` on-track / under / over / threshold edge / no-plan / negative-clamp, `missedWorkoutAdvice` make-up / taper / recovery-soon / precedence / non-long-run.
- `plan_replan_test.dart` — 13 tests for `lib/plan_replan.dart` (mirror of `plan_replan.test.ts`): on-track no-op, missed-long make-up capped at +15%, no-shrink of an already-longer next long, taper skip, over-running ease-off, frozen-past/taper guard, plus the multi-missed-long regression block (largest wins whether it's the earliest or latest week, under-cap make-up reaches the largest miss exactly, a taper miss is excluded from the make-up max, three misses still produce exactly one capped change), plus the explicit-skip block (a deliberately-skipped long run is never made up; a skipped long is excluded from the make-up max while a genuine miss still makes up).
- `relink_candidates_test.dart` — 9 tests for `lib/relink_candidates.dart` (mirror of web `relink_candidates.test.ts`): in-window newest-first, ±window exclusion + boundary inclusivity, excludes a run linked to another workout (double-count guard), keeps the workout's own current run even when linked / out of window, empty input, default 7-day window.
- `race_predictor_test.dart` — 11 tests for `lib/race_predictor.dart` (mirror of web `race_predictor.test.ts`): empty pool → null, non-finite/non-positive efforts filtered, one-effort full ladder, rungs match `riegelPredict`, pace = time/km, a recent effort out-anchors a faster-but-stale PR, a recent PR still wins when fastest, `qualifyingCount` reflects the filtered pool, a 10K anchor grades the marathon rung below the 10K rung, a stale-only pool still predicts (no null-out), future-dated (clock-skew) effort treated as weight 1.
- `workout_detail_screen_test.dart` — initial render (loading spinner, no AppBar yet) plus the **re-link picker** group: Re-link opens the picker and picking a non-current run calls `markCompleted` with that run id; the empty-candidate state renders; no Re-link button when the workout is only manually completed (run-link-only). Uses a fake `TrainingService` overriding `fetchWorkout` / `fetchRelinkCandidates` / `markCompleted`.
- `plan_detail_adherence_test.dart` — 6 widget tests for the owner surfaces on `lib/screens/plan_detail_screen.dart`: the under-running drift banner + the missed-long make-up callout render (owner only), hidden for a non-owner; the Re-plan flow proposes a make-up + applies it via `updateWorkout`; the per-week Duplicate action calls `duplicatePlanWeek` (owner only). Uses fake `TrainingService`/`SocialService` + `viewerIdOverride`.
- `nutrition_meal_detail_screen_test.dart` — 2 widget tests for `lib/screens/nutrition_meal_detail_screen.dart`: a seeded breakfast (two items) shows the slot title + macro total + items + 7-day trend (and excludes a same-day lunch item); the empty-items state renders for a slot with nothing logged.
- `ble_heart_rate_test.dart` — 12 tests: BLE HR characteristic 0x2A37 parser (8-bit/16-bit BPM, edge cases) + `bleReconnectDelay` backoff schedule (2/4/8/16→30 s cap, negative clamp) for the mid-run auto-reconnect loop (persona #13)
- `ble_treadmill_test.dart` — 20 tests: FTMS Treadmill Data 0x2ACD parser (speed-only, 0.01 km/h resolution, m/s conversion, total distance bit 2, average-speed skip keeping the distance offset correct, positive/negative sint16 inclination, distance+inclination together, zero speed, more-data flag → null, truncation edge cases) + `bleTreadmillReconnectDelay` backoff + connect-status/pairing helpers (C3)
- `treadmill_tile_test.dart` — 2 widget tests for the `TreadmillTile` in `settings_integrations_screen.dart`: not-paired subtitle + chevron when no treadmill stored, paired-name subtitle + forget affordance when stored (C3)
- `pace_segments_test.dart` — 15 tests: pace-bucket clamping, activity-specific scaling, age-band partitioning, coalescing, vertex-sharing continuity, timestamp-less fallback — backs the NRC-style pace heatmap in `widgets/pace_segments.dart`
- `track_preview_test.dart` — 8 tests for `widgets/track_preview.dart`: 4 for `isTrackRenderable` (rejects empty / single-point / sub-5 m jitter tracks, accepts genuine tiny laps + multi-km loops), 4 for `projectTrack` (square 100 m loop at 51 °N renders square within 2 %, longitude scale matches `cos(midLat)` at high latitudes, horizontal segments stay on the x-axis, empty input). Mirrors the `isMoving` + projection helpers on the web `track_projection.ts` module.
- `track_decorations_test.dart` — 10 tests for `widgets/track_decorations.dart`: 6 for `computeDistanceMarkers` (one pin per km / mile, empty short tracks, miles produces fewer pins, `totalDistanceM` rescales sparse-polyline runs, mid-segment interpolation), 4 for `computeChevrons` (right cadence, north-bound segments rotate to point up, empty for invalid stepMetres). Mirrors the `computeDistanceMarkers` + `trace-arrows` layer on web's `RunMap.svelte`.
- `track_segment_test.dart` — 9 tests for `widgets/track_segment.dart`: `buildCumulativeDistances` is monotonic, `nearestTrackIdx` finds the closest waypoint, `buildSegmentAt` expands ±150 m, computes pace + duration from timestamps, returns null pace under 10 m, aggregates avg BPM and elevation gain on the slice, widens past a degenerate single-point selection at the track edge. Mirrors `buildSegment` on web's `RunMap.svelte`.
- `hr_zones_test.dart` — 8 tests: time-weighted vs sample-count fallback, the 30 s pause cap, custom cutoffs, malformed-input rejection, `bpmStatsOf` aggregation
- `fitness_test.dart` — 17 tests: VDOT range checks (5k, marathon), `currentVdot` 90-day window + qualifying-source filter, `runTss` Coggan reference, EWMA training-load smoke, `recoveryAdvice` thresholds, `computeSnapshot` end-to-end
- `training_load_test.dart` — 10 tests for `lib/training_load.dart`: distance-fallback stress (~50 for an easy 5k), TRIMP path lights up with avg_bpm + resting + max, zero-input → 0, daily aggregation sums same-day runs, series emits exactly windowDays entries, TSB rises during a 14-day taper, all-zero with no runs, `hasTrimpSignal` gating (no avg_bpm / has avg_bpm + prefs / prefs missing). Mirrors `apps/web/src/lib/training_load.test.ts`.
- `mileage_trend_test.dart` — 9 tests for `lib/mileage_trend.dart`: weekly view groups same-week runs (Monday anchor), separates across week boundaries chronologically, caps to 12 buckets by default, respects `maxBuckets` override, returns empty on no runs; monthly view groups same-month runs and separates across boundaries; yearly view groups same-year runs and separates across boundaries.
- `mileage_trend_card_test.dart` — 6 widget tests for `lib/widgets/mileage_trend_card.dart`: hides when there are no runs; renders the header + SegmentedButton labels (Week / Month / Year); default view is weekly (week-start label visible); tapping Month switches the bucket labels; tapping Year switches to 4-digit year; numeric labels honour the user `DistanceUnit` (10000 m → "10.00" in km, "6.21" in mi).
- `run_intensity_test.dart` — 14 tests for `lib/run_intensity.dart`: 9 for `computeIntensityBreakdown` (empty when no runs, drops runs older than window, drops runs without avg_bpm or with sentinel zero, correct zone classification across the five bands, accumulates within-zone seconds, rejects non-ascending / wrong-length zones, rejects non-positive window, < semantics on exact boundary); 5 for `parseHrZones` (valid map → ascending list, rounds non-int numerics, null on non-Map / missing / non-numeric / non-ascending). Backs the dashboard `IntensityCard` + `run_detail_screen`'s per-run HR-zone panel.
- `intensity_card_test.dart` — 6 widget tests for `lib/widgets/intensity_card.dart`: hides when `hrZones` is null, hides when no runs in window carry `avg_bpm`, renders the `TRAINING INTENSITY` header + `last N days` window label when populated, renders five zone legend rows with rounded percentages (Z3 = 20% pinned for cleanliness), helper text reports HR-tracked-run count with correct singular / plural, uses the `<1%` marker for non-empty sub-1% slivers (not `0%`).
- `segments_test.dart` — 8 tests for `lib/segments.dart`: clean segment, run shorter than segment end, ≤2-point tracks, zero / negative segment windows, sparse-sampling guard (median step > segLen / 5), missing timestamps in the interpolation bracket, mid-segment interpolation, sample-aligned endpoints. Mirrors `apps/web/src/lib/segments.test.ts`.
- `privacy_test.dart` — 8 tests for `lib/privacy.dart`: `isInAnyZone` (empty / centre / far), `clipPointsToZones` (empty zones returns input, drops leading + trailing in-zone, keeps interior, every-point-in-zone returns empty, multi-zone union). Mirrors `apps/web/src/lib/privacy.test.ts`.
- `plan_calendar_test.dart` — 3 widget tests: month rendering with kind labels, chevron navigation, completed-tick presence
- `workout_execution_band_test.dart` — 6 widget tests: empty render, header / step counter / remaining-metres formatting, Skip + Abandon callbacks, complete + abandoned shells, em-dash for null pace
- `workout_review_section_test.dart` — 11 tests for `widgets/workout_review_section.dart`: hidden when metadata absent / empty / null, header + adherence pill rendered with one row per step, kind-specific labels (Warmup / Rep i/n / Recovery i/n / Cooldown), strikethrough + "skip" label for skipped steps, em-dash + neutral colour when actual pace is null, signed `+/−Ns` delta label, and tone classification thresholds (`on` / `amber` / `off` against the 10 s tolerance) plus `formatPace` formatting
- `race_predictor_card_test.dart` — 3 widget tests for `widgets/race_predictor_card.dart`: self-hides with no qualifying runs / no runs at all; renders the title + the four uppercased column headers + the per-rung confidence chips when the gate passes.
- `fitness_card_test.dart` — 5 widget tests for `widgets/fitness_card.dart`: hidden with no runs, hidden when no runs qualify (sub-3 km / sub-5 min), populated card renders all six stat labels + the recovery-advice icon + the qualifying-run count, em-dash placeholder when VDOT can't be computed but qualifying runs exist, plus a smoke test for the `FitnessStat` value-over-label primitive
- `metadata_registry_test.dart` — 2 tests: (1) every `runs.metadata` key referenced in Dart source is registered in [docs/backend/metadata.md](../../docs/backend/metadata.md) — catches cross-client drift like `metadata.activityType` vs `metadata.activity_type` at CI time; (2) a soft "dead-key" info log surfacing registered keys that no Dart reader touches (may be web/watch/EF-only). Fails on unknown-key writes; purely informational on the reverse direction.
- `recurrence_test.dart` — 8 tests: weekly / biweekly / monthly `expandInstances`, hour/minute local-tz preservation, `count` cap, `until` cap, non-recurring single-instance
- `importer_external_id_test.dart` — 2 tests: `StravaImporter` ZIP import produces `strava:<id>` prefix; source-text guard confirms `HealthConnectImporter` uses `healthconnect:<uuid>` prefix
- `routing_test.dart` — 14 tests for `lib/routing.dart` (OSRM client used by the in-app route builder): snapToRoad parses + falls back to input on non-Ok / network error, builds /foot vs /car path correctly; fetchRouteThrough returns empty on <2 points, parses routes[0].geometry + distance, semicolon-joins lng,lat pairs, per-segment fallback + cancellation + batching; plus the `RouteSegmentCache` group (key order/profile sensitivity, incremental re-route fetches only the new segment, no-cache re-fetches all, failed segments aren't cached, clear() re-fetches).
- `elevation_test.dart` — 12 tests for `lib/elevation.dart`: fetchElevations returns empty for empty input, parses elevation array, returns zeros on fetcher error / length mismatch, batches inputs > 100 points across 2 requests, builds comma-joined lat/lng query params; calculateElevationGain sums positive deltas only, returns 0 for flat / descending / <2-sample series; sampleCoordinates returns input unchanged ≤ maxPoints, downsamples to exactly maxPoints, produces evenly-spaced indices.
- `geocoding_test.dart` — 19 tests for `lib/geocoding.dart` (MapTiler geocoding + the region-aware club-search helpers): 8 for `searchPlaces` (empty list on <2-char query / empty apiKey, parses MapTiler features into PlaceResult, URL-encodes the query + includes limit/key params, empty list on fetcher error, skips malformed center coords, falls back to `text` field when `place_name` absent, timeout); 2 for `haversineM` (zero distance for identical points, London↔Paris within 1 km of reference 344 km); 1 for `bboxRadius` (1° square at the equator → ~78.5 km half-diagonal); 8 for `geocodePlace` (null on <2-char / empty apiKey / no features / no center / fetcher error, parses top feature into `GeocodedPlace` with bbox-derived radius for "Virginia" in the 400–600 km range, defaults to 5000 m radius when bbox absent, falls back to `text`). Backs `SocialService.searchClubs`.
- `route_overlap_test.dart` — 7 tests for `lib/route_overlap.dart`: empty for <4 points, one-way / no-retracing returns no overlap, out-and-back marks return-half as overlap, tolerance gates parallel-but-separate streets, span covers contiguous indices, OverlapSpan.contains hits indices inside the span, long polylines down-sample (perf bound under 500 ms).
- `run_heatmap_test.dart` — 9 tests for `lib/run_heatmap.dart` (mirror of web `run_heatmap.test.ts`): grid-cell collapse, repeated-route weight accumulation, distinct-cell separation, `kMaxCellWeight` clamp, invalid/out-of-range point drop, empty/all-invalid → no cells + null bounds, `heatBounds` extent, invalid-gridDeg fallback.
- `run_heatmap_screen_test.dart` — 3 widget tests for `lib/screens/run_heatmap_screen.dart`: renders the AppBar chrome; aggregates the downloaded tracks (via the `fetchRunsFn`/`fetchTrackFn` seams) and shows the legend; the empty state renders when no run carries a track. flutter_map tile fetches fail under the test runner (the standard map-test constraint) — the test pins the data path + chrome, not tile load.
- `snap_to_start_test.dart` — 5 tests for `lib/widgets/snap_to_start.dart`: shouldSnapToStart is false with <3 existing waypoints, true within 30 m of start when 3+ waypoints, false outside tolerance, false at-start when route too short, respects a custom tolerance.
- `turn_cues_test.dart` — 10 tests for `lib/turn_cues.dart` (mirror of web `turn_cues.test.ts`): straight line → no cues, empty / single-point / two-point → no cues, 90° left → one left cue at the vertex, 90° right → one right cue, sub-threshold wiggle suppressed, slight bend just over threshold → slight, near-reversal → u-turn, coincident-vertex merge.
- `tile_pack_test.dart` — 8 tests for `lib/tile_pack.dart` (mirror of web `tile_pack.test.ts`): bbox → correct tile set at one zoom, multi-zoom union, count cap triggers, degenerate point bbox → one tile/zoom, swapped corners normalised, antimeridian/degenerate handling.
- `turn_cue_announcer_test.dart` — 11 tests for `lib/turn_cue_announcer.dart`: empty/straight cue lists announce nothing, the 300 m band fires first, each band fires once per turn near→far, the 0 band is the "now" announcement, a turn behind the runner is never announced, only the nearest upcoming turn is considered, the far turn fires only after the near one is passed, slight/u-turn directions announced.
- `offline_tile_pack_test.dart` — 10 tests for `lib/offline_tile_pack.dart` (injected `TileBytesFetcher`): happy path writes one file per tile → ready; a failing tile / a thrown fetch is isolated → partial; a retry only fetches the gaps; `deletePack` removes the dir; the count cap → `tooLarge` with no download; `cachedTile` hit/miss; `init` reconciles an already-downloaded pack to ready.
- `route_conditions_test.dart` — 5 widget tests for `lib/widgets/route_conditions.dart` (fake `ApiClient` subclass): empty state + Report affordance for a signed-in viewer, a seeded condition renders chip + severity + note, the composer writes via the api, `canReport: false` hides the composer, a signed-out viewer sees no composer. (`showTopBanner` timer drained per the mobile-test gotcha.)
- `replay_dot_index_test.dart` — 6 tests for `replayDotIndex` in `lib/screens/run_detail_screen.dart`: null/degenerate inputs return null; a same-length displayed track (no map match) round-trips to the same index; a shorter or longer map-matched track maps proportionally and stays in range; the result is always a valid index into the displayed track. Pins the fix for the replay dot drifting off the map-matched line.
- `show_raw_track_test.dart` — 5 tests for `displayedRunTrack` in `lib/screens/run_detail_screen.dart` (the map-matching "Show raw GPS track" toggle): prefers the matched line when present + `showRaw` off (default); `showRaw` forces the raw track even when a matched line exists; falls back to raw with no match info; falls back to raw when the match isn't renderable (pending / sub-2-point); falls back to raw when a `matched` row couldn't download its gz (`trackUnreachable`, the offline-fallback L1 pin). The offline map-match selectors themselves (`isMatchUnreachableError` / `matchPillKind` / `shouldRetryMatchFetch`) are pure + live in `core_models/run_match_info.dart`, unit-tested in `packages/core_models/test/run_match_info_test.dart` (13 tests).
- `settings_email_notifications_test.dart` — 3 widget tests for the email-notifications picker on `settings_preferences_screen.dart`: the tile renders the default ("Important only") subtitle; picking "Everything" writes `{email_notifications: all}` and picking "Off" writes `{email_notifications: off}` through `SettingsSyncService.updateUniversal`.
- `settings_push_notifications_test.dart` — 3 widget tests for the push-notifications picker on `settings_preferences_screen.dart` (mirror of the email one): the tile renders; picking "Everything" writes `{push_notifications: all}` and picking "Off" writes `{push_notifications: off}` through `SettingsSyncService.updateUniversal`.
- `push_messaging_bridge_test.dart` — 4 unit tests for `lib/push_messaging_bridge.dart` (native-push device-token registration) over a fake `PushMessaging` seam + fake `ApiClient`: an unavailable-messaging `attach()` is a complete no-op (no permission ask, no registration); `setNotificationsEnabled` is a guarded no-op when no token is registered (proves the null-token guard, not just a swallow) and routes nothing to the api; `attach()` exposes the bridge as the static `instance`. The auth-driven register-on-sign-in / re-register-on-refresh / remove-on-sign-out paths need a live Supabase session (mobile has no e2e by design — `docs/testing/testing.md`); they're exercised on-device.
- `report_sheet_test.dart` — 10 widget tests for `lib/widgets/report_sheet.dart`: title + reason radio count + Submit button render; title reflects targetKind (user / club / route / club_post / run); Submit forwards the picked reason + trimmed notes to `submitReport`, and forwards a `run` targetKind; empty notes pass `null` not `""`; PostgrestException 23505 surfaces "already have a pending report" copy and keeps the sheet open; Cancel pops without calling the API; default reason is `spam`.
- `strava_test.dart` — 7 tests for `lib/strava.dart`: `isStravaConfigured` is false when `STRAVA_CLIENT_ID` is absent / empty / the `12345` placeholder, true for a real client id; `stravaAuthUrl` throws when unconfigured, builds a `/oauth/authorize` URL with the right client_id/scope/approval_prompt/redirect_uri, URL-encodes the redirect.
- `revenuecat_test.dart` — 8 tests for `lib/revenuecat.dart` (mobile Pro purchase flow wrapper): `isRevenueCatConfigured` returns false when no platform key is in `dotenv.env`, true when `keyOverride` non-empty, false when `keyOverride` empty; `configureRevenueCat` returns false when no API key is provided; `keyOverride` is honoured (configure attempts but headless test surfaces MissingPluginException internally → wrapper catches and returns false — no crashes on host-test runner); `startProCheckout` returns `PurchaseResult.notConfigured` when SDK has no API key; `managementUrl` returns null when SDK has no API key; `proMonthlyPriceString` returns null when SDK has no API key (so the Subscribe tile falls back to the `$9.99` USD list price). The wrapper is the integration glue used by `settings_pro_screen`'s "Subscribe to Pro" tile.
- `route_builder_screen_test.dart` — 9 tests for `lib/screens/route_builder_screen.dart`: initial state shows "Tap the map" hint + Save disabled; AppBar renders the place-search field + Locate FAB; mode toggle has Trail / Road / Straight segments; `straightLineDistance` math; `overlapLatLngsFor` slices the polyline by span indices, clamps endIndex, skips out-of-range spans, drops <2-point slices.
- `live_broadcaster_test.dart` — 12 tests for `lib/live_broadcaster.dart`: 8 legacy `insertLivePing`-path tests (starts inactive; `pushPing` is a no-op when inactive; `attach` flips `isActive`; first ping after attach goes through with the full field set; back-to-back pings within the 5 s window are throttled; `detach` silences future pings; api errors are swallowed (L4); re-attach resets the throttle so the next run's first ping fires immediately) plus 4 new transport-selection tests (routes through the Go hub when `hubClient` is configured; falls back to `insertLivePing` when `hubClient` is null or `isConfigured=false`; hub HTTP failure stays swallowed per the L4 contract). Pure Dart, fake `ApiClient` + stub fetcher.
- `live_hub_client_test.dart` — 9 tests for `lib/live_hub_client.dart` (HTTP push wrapper for the Go live hub): `isConfigured` flag semantics; `pushPing` targets `/v1/live/{run_id}/push`, URL-encodes the run id, strips trailing slashes, omits null optional fields, includes set optional fields, returns the fetcher status, propagates fetcher errors.
- `services_integration_test.dart` — wire-level tests against a real local Supabase via the seed user (gated on `SUPABASE_TEST_URL`). Drives `SocialService.browseClubs` / `searchClubs` / `fetchMyClubs` / `fetchClubBySlug` / `fetchClubRoutes` / `fetchClubPosts` / `setRouteClub` / `createClub` + `leaveClub` / `joinClub` / `createPost` + `deletePost` / `fetchUpcomingEvents` / `fetchEventById` / `fetchAttendees` / `rsvpEvent` + `clearRsvp` / `createEvent` / `submitEventResult` + `fetchEventResults` + `removeEventResult` / `fetchRecentRuns`, plus `TrainingService.fetchMyPlans` / `fetchActiveOverview` / `fetchPlan` / `fetchWorkout` / `fetchPlanForWorkout` / `updateWorkout` / `fetchClubTemplates` / `createPlan` + `updateStatus` + `deletePlan` / `markCompleted` / `clonePlanTemplate` / `publishPlanAsTemplate` through the `withClient(SupabaseClient)` DI seam — the same shape as `ApiClient.withClient`. Region-aware `searchClubs` cases use a stub geocoder so the RPC's text branch + geographic branch can be driven independently of MapTiler. CI runs this file from the `api-client-integration` job.
- `architecture_guards_test.dart` — 148 tests: static source-level assertions that pin in place the efficiency + layering optimizations (no `setState` in `_onSnapshot`, `markSynced` doesn't rewrite the run file, sync paths use `saveRunsBatch`, `ErrorWidget.builder` override present, RunNotificationBridge pins geolocator channel constants, plus the `LocalRunStore` newer-wins guards — `save`/`update` must stamp `last_modified_at`, `saveFromRemote` must not — added in the Apr 2026 data-sync hardening pass; the May 2026 UI-freeze guards: `StravaImporter.importFromZip` must dispatch to `compute()`, `BackupService` must keep the `_encodeArchiveInIsolate` / `_decodeArchiveInIsolate` helpers, `routes_screen._importFile` must wrap `RouteParser` in `compute()`, and both stores' `_loadAll` must keep the directory walk on `listSync` (the async stream form deadlocks `RunsScreen` widget tests under flutter_test — see [decisions.md § 48](../../docs/architecture/decisions.md#48-heavy-import--backup-parsers-run-in-compute-isolates-never-on-the-ui-thread)); and the May 2026 notification-primitive guards: no direct `showSnackBar` or `ScaffoldMessenger.of(context)` calls anywhere under `lib/screens/` or `lib/widgets/` — the canonical primitive is `showTopBanner` from `widgets/top_banner.dart` (see [../../docs/architecture/conventions.md § Mobile in-app notifications](../../docs/architecture/conventions.md#mobile-in-app-notifications--showtopbanner))). Latest additions: the May 2026 iOS Info.plist guards — auto-skip on the Android twin (no `ios/` folder there) and assert on the iOS twin that every plugin-required usage-description key is set (`NSHealthShareUsageDescription`, `NSBluetoothAlwaysUsageDescription`, `NSMotionUsageDescription`, `NSPhotoLibraryUsageDescription`, plus the two location keys) and that the Workmanager `BGTaskScheduler` identifier `com.threkir.backgroundSync` is permitted with `UIBackgroundModes:processing`. The Jun 2026 crash-atomic write guards: no bare `writeAsString` in any of the five `local_*_store.dart` files (every record + sidecar write goes through `writeJsonAtomic` from core_models) and gym/food/gear `_rewriteAll` must write new files before deleting orphans (see [decisions.md § 123](../../docs/architecture/decisions.md)). The Jun 2026 at-rest / backup-exclusion guards: the phone manifest must set `android:allowBackup="false"` (keeps the GPS/HR cache + the SharedPreferences-stored Supabase session out of Android Auto-Backup) and `AppDelegate.swift` must mark the Documents dir `isExcludedFromBackup` (iOS-twin-only check, auto-skips on Android) — see [decisions.md § 127](../../docs/architecture/decisions.md). **When one of these fails, read the `reason:` before rubber-stamping a fix** — a failure means a recent change reversed an optimization we deliberately codified.
- plus `run_recorder`'s own tests in `packages/run_recorder/test/` — 18 behavioural in `run_recorder_test.dart` + 7 guards in `architecture_guards_test.dart` + 6 in `laps_serialiser_test.dart` (canonical `metadata.laps` round-trip) + 13 in `workout_runner_test.dart` (step expansion, auto-advance, halfway / last-50m progress, skip / abandon, pace-adherence wayBehind, results JSON shape)

See [../../docs/testing/testing.md](../../docs/testing/testing.md) for how to run them and the patterns they use. Widget tests exist for every screen and widget, including `RunScreen` (`run_screen_test.dart`) and `LiveRunMap` (`live_run_map_test.dart`) (~178 `testWidgets` calls across ~48 files); see `test/` for the full list.

## Running it locally

See [local_testing.md](local_testing.md). Short version: `cd apps/mobile_android && flutter run -d <device>`. The seed user in local Supabase is `runner@test.com` / `testtest`.

## Deploying to production

See [deployment.md](deployment.md) — Play Console signing, GitHub Secrets, release workflow, observability, rollback, DR.

## Before reporting a task done

- Run `dart analyze` on this package and confirm no new `warning`/`error` level issues. `info` are OK per policy above.
- Run `flutter test` for the package if you touched anything under `test/` or the files it covers.
- If you changed anything user-visible, update the corresponding "Android" checkbox in `roadmap.md`.
- If you introduced a new screen, add it to the file list above.
- If you hit a new gotcha, add it here so the next session doesn't re-hit it.
