/.gradle
/captures/
/local.properties
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Dev-only build flags. The committed local-stack defaults are applied ONLY by
# the debug build type (defaultConfig stays release-safe), so this is safe to
# track. See the .env.local header + README "Quick start".
.env.local
!.env.local
