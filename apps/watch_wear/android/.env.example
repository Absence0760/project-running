# Copy this file to `.env.local` (gitignored) and flip flags as needed.
# Values are read at Gradle-configure time and emitted as BuildConfig
# constants. Changing .env.local requires a rebuild.

# Auto-sign-in with the seed dev user (`runner@test.com` / `testtest`) at
# app start instead of showing the sign-in screen. Only ever use this
# against a local / dev Supabase. Default false so release builds can't
# accidentally inherit dev creds.
BYPASS_LOGIN=false
# NOTE: This flag is only consulted on cold start when there is no cached
# session. A previously-signed-in user stays signed in across rebuilds
# because DataStore persists through `installDebug`. To get back to the
# sign-in screen, either tap the "Sign out" chip on the pre-run screen
# or wipe app data: `./gradlew uninstallDebug && ./gradlew installDebug`.

# Heart rate from the Wear OS Health Services API (avg_bpm into
# run.metadata) defaults **ON** so real watches record HR. The Wear OS
# emulator synthesises HR samples that look real, so set DISABLE_HR=true
# here when developing on the emulator to keep fabricated readings out of
# the runs table. (persona samsung #33)
# DISABLE_HR=true

# MapTiler raster tile API key. Empty ⇒ the in-run mini-map falls back
# to a polyline + position dot on a midnight background, no tile layer.
# Set this and the map renders street tiles (streets-v2-dark) under
# the polyline so the runner can read where they are on actual roads.
# Same key as the web app's PUBLIC_MAPTILER_KEY — get one at
# https://cloud.maptiler.com/account/keys/.
PUBLIC_MAPTILER_KEY=

# Optional dev override that points the tile fetcher at a different
# raster-tile URL template (typically a local Protomaps tileserver-gl).
# When set, the watch resolves tiles via the override and the MapTiler
# key above is unused. Empty ⇒ MapTiler path. The mini-map's `enabled`
# flag lights up when EITHER value is set. Use 10.0.2.2 instead of
# localhost when running on the Wear OS emulator. Pass through gradle:
# `./gradlew installDebug -PPUBLIC_TILE_URL_TEMPLATE=http://10.0.2.2:8080/styles/basic/{z}/{x}/{y}.png`
# Boot the local server with `bin/protomaps-dev.sh start`. See
# docs/protomaps_local_setup.md + decisions.md § 68.
PUBLIC_TILE_URL_TEMPLATE=
