name: Compliance drift

on:
  pull_request:

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  compliance-drift:
    name: Compliance drift advisory
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # The drift script diffs against origin/<base>; full history not
          # needed but the base ref must exist locally.
          fetch-depth: 0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24.20.0'
      - name: Run compliance-drift detector
        run: |
          if ! node --test scripts/check_compliance_drift.test.mjs; then
            echo "::error::The compliance-drift detector's own unit tests failed, so whatever it says about this PR's diff cannot be trusted. This is not a compliance finding."
            echo "Reproduce with: node --test scripts/check_compliance_drift.test.mjs (stdlib only, no install needed)."
            exit 1
          fi
          node scripts/check_compliance_drift.mjs
        env:
          # 'warn' = print findings + summary, never fail the job.
          # 'fail' = exit 1 when findings exist. Flip when the team is
          # ready for hard enforcement.
          COMPLIANCE_DRIFT_MODE: warn
