name: core_models
description: Shared Dart data types for Run, Route, and Waypoint.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.4

dependencies:
  json_annotation: ^4.8.1

dev_dependencies:
  build_runner: ^2.4.0
  json_serializable: ^6.7.0
  flutter_lints: ^3.0.0
  test: ^1.25.0
