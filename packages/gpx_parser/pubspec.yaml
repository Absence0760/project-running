name: gpx_parser
description: GPX, KML, KMZ, and GeoJSON parsing into Route objects.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  core_models:
    path: ../core_models
  xml: ^6.3.0

dev_dependencies:
  flutter_lints: ^3.0.0
  test: ^1.25.0
