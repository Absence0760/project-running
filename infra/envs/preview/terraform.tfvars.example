# Copy to terraform.tfvars (gitignored) before applying.

apex_domain              = "runonward.app"
preview_subdomain        = "preview"
public_supabase_url      = "https://api.runonward.app"  # or a separate preview project
public_supabase_anon_key = "ey..."

extra_lambda_env = {}
