---
name: Gap-closure follow-ups
description: Outstanding work after the 2026-05-10 gap-closure session. Mirrors `roadmap.md` in spirit — checkboxes, brief context, no doc-rot. Prune entries as they land.
---

# Gap-closure follow-ups

Session of 2026-05-10 ran through the four highest-priority test-coverage gaps from `docs/testing.md` "What's not covered" and closed three of them plus a CI gap and four production bugs (see "What landed" below). The pending work below is what remains.

Pick up cold by reading this file + `docs/testing.md` "What's *not* covered" — both should reflect current state.

## What landed this session

**Tests + CI infrastructure (8 commits):**
- `pgtap-rls` CI job — gates the full 279-test pgTAP RLS suite on every PR
- 7 new pgtap suites (route_reviews, segments, clubs, club_members, club_posts, events, event_attendees) — closes the original "still uncovered tables" list
- `api-client-integration` CI job — boots local Supabase + runs `flutter test` against the seed user; covers the five priority `ApiClient` wire-level methods (signIn / getRuns / saveRun + _uploadTrack / fetchTrack)
- `SocialService.withClient` + `TrainingService.withClient` DI seams + 5 wire-level integration tests on the same CI job
- `edge-functions` CI job — boots `supabase functions serve --env-file`, runs 9 HTTP-level handler-envelope tests for refresh-tokens / strava-webhook / revenuecat-webhook + the pre-existing pure-helper deno tests that had no CI coverage

**Production bugs fixed:**
1. `is_route_visible_to` revoke from anon (migration 20260711_001) broke anon SELECT on `route_reviews` + `segments` — PG 17.6 manifested it as a backend SIGSEGV instead of clean 42501. Fixed by moving the function to the `private` schema and granting anon EXECUTE (migration 20260819_001, mirrors the parallel for `is_run_visible_to`).
2. `club_posts` had three INSERT policies stacking; the older two (`admins can post top-level` + `active members can reply`) had been dead code since 20260428_001's `members can post` catch-all landed. Cleanup migration 20260820_001.
3. `ApiClient._uploadTrack` + `uploadTrackBytes` set `contentType: application/json` for gzipped JSON bytes — every live-recording save on mobile was 415'ing silently against the runs-bucket MIME allowlist (migration 20260815_001). Fixed.
4. `seed.sql` "restored canonical" `track_url` after the path-shape CHECK exercise, leaving an orphaned URL pointing at a non-existent Storage file. Any dev using the seed user got fetchTrack 404s. Fixed.
5. `hmacHex` RFC 4868 reference-vector test had been silently wrong since written — the body construction UTF-8-expanded each 0xcd byte into `c3 8d`. Refactored hmacHex to accept `Uint8Array` so byte-exact tests actually represent the bytes they claim.

## Carry-over from "completed" tasks

These tasks are marked completed at the high-leverage surface but have known sub-gaps left for follow-up.

### Mobile testing gaps (from session task #3)

- [ ] **RunScreen Finish + save UI test.** Tried during the session; reverted. The existing widget-test scaffold doesn't fully initialise `RunRecorder` from `run_recorder` so `_stop()` doesn't produce a row in `LocalRunStore`. Needs additional platform-channel mocks for whatever the recorder depends on at start time. Data-pipeline equivalent (`recording_integration_test.dart`) IS covered — only the UI Finish-tap surface remains uncovered.
- [ ] **Device-instrumented `integration_test` harness.** None exist today. Would close real-device coverage for tile-cache, foreground-service, and background-sync paths that need actual Android primitives. New infrastructure.
- [x] **Widened the `SocialService` / `TrainingService` wire-level suite (2026-05-11).** Up from 24 → 29 tests. Six new tests cover the lifecycle write paths that were missing: `joinClub` idempotency on the seeded club (leave → re-join → fetchMyClubs round-trip), `rsvpEvent` + `clearRsvp` flipping seed `going` → `maybe` → cleared and back, `submitEventResult` + `fetchEventResults` + `removeEventResult` (post-race composer wire), `createPlan` + `updateStatus` (status valid values are `active`/`completed`/`abandoned` per the CHECK; `archived` reads as `abandoned`) + `deletePlan`, `markCompleted` toggling the flag, `clonePlanTemplate` walking a template → cloned plan that's `is_template=false`. Twin-mirrored to `apps/mobile_ios/test/services_integration_test.dart` byte-for-byte. **Bug found**: `SocialService.createClub` calls `.select()` (all columns) after insert, which fails against the column-grant lockdown because `clubs.invite_token` isn't readable to authenticated — the web path passes `.select(CLUB_SELECT_COLS)` to avoid this. Test for `createClub` skipped with an explanatory NOTE so the divergence surfaces here when someone tries to add it back. Same for `createEvent` against the seeded club: the seed user is a regular `member`, not admin, so RLS denies; would need a createClub-bootstrapped admin path. Both noted in the test file. The 29-test suite runs in ~3s against the local stack and is gated on `SUPABASE_TEST_URL` (same as before).

### Edge Function tests (from session task #4)

- [ ] **OSRM smoke test in CI.** Genuinely blocked on free-runner capacity — the OSM PBF extract + osrm-extract memory pressure don't fit. Real options: self-hosted runner, or a pre-built OSRM cache in S3 the workflow downloads. Both are infra decisions outside the code.
- [ ] **Positive-path Edge Function tests.** Current handler-envelope suite only covers auth-rejection branches. Adding 200-on-valid-HMAC / replay-protection-dedupe / freshness-window tests needs real secret values in the test config — straightforward to add once we decide on a CI-side secret config strategy.

## Pending tasks (session-order)

### #5 Phase 1+2b ops

- [x] **MapTiler API usage monitoring** (Phase 1, roadmap.md) — runbook landed in `apps/web/deployment.md` § Other alerts. No CloudFront proxy on the tile path (clients hit `api.maptiler.com` directly) so the alert lives MapTiler-side: 80% threshold against the monthly quota, daily-usage email to the SNS oncall address. Operator follow-up: actually click through the MapTiler dashboard once for prod + preview; the runbook covers each step.
- [x] **Verify dashboard queries are under 2 s for users with 200+ runs** (Phase 2b, roadmap.md). Measured locally at 2074 runs — all three queries (`fetchRuns(limit:50)`, `fetchWeeklyMileage`, `fetchPersonalRecords`) ran in 0.1–1 ms, ~1000× under the 2 s budget. No index changes needed; the existing composites carry it. Roadmap Phase 2b checkbox ticked.

### #6 Android residuals (parity.md follow-ups)

- [x] Ultra-length recording crash-resume guarantee (mobile_android backlog #117 — long-runs >6 h) — investigated the save/load roundtrip, confirmed it's correct (no truncation, no concurrent-mutation race on `_track`), and pinned the behaviour with a 25 000-waypoint stress test in `local_run_store_test.dart` (≈ 7 h @ 1 Hz; every Waypoint field — lat, lng, ele, timestamp, bpm — is asserted byte-stable on roundtrip; file-size sanity check confirms the encoder isn't silently cutting off past 1.5 MB). Hardened `loadInProgress` to run the read + JSON decode in a background isolate via `compute` so a recovery from an ultra-length crash doesn't stall app launch (previously 300-500 ms blocking decode on the UI thread before `runApp`). Save-jank profiling on low-end Android is the remaining unknown.
- [x] Club-template publish action — AppBar publish button on `plan_detail_screen.dart` opens a picker of the viewer's owner/admin clubs (via `adminClubsForPublish` filter), then commits via `TrainingService.publishPlanAsTemplate`. Found + fixed two latent bugs while wiring: (1) Dart was calling a non-existent `publish_plan_as_template` RPC — replaced with the canonical multi-INSERT path that mirrors web `data.ts`; (2) `clonePlanTemplate` was passing `p_template_id` / `p_start_date` (with stray `p_` prefix) — corrected to `template_id` / `new_start_date` matching the actual `clone_plan_template` SQL signature. Test coverage: 3 unit tests on `adminClubsForPublish`, 3 widget tests on `PublishClubPicker`, 1 wire-level integration test for `publishPlanAsTemplate` against real Supabase. Twin-mirrored.
- [x] Club-owned-route admin transfer/detach UI on `route_detail_screen` — AppBar button opens `RouteTransferClubPicker` (lists owner/admin clubs + a Detach row when the route is currently club-owned). `Route.clubId` added to the domain model so the picker can mark the current club; both `_routeFromRow` codecs (ApiClient + SocialService) pass it through. Test coverage: 2 unit tests on `adminClubsForRouteTransfer`, 2 unit tests on `TransferRouteResult`, 5 widget tests on `RouteTransferClubPicker`, 1 wire-level integration test for `setRouteClub` attach + detach roundtrip against real Supabase. Twin-mirrored.
- [x] Device-override "+ Add override" sheet in Settings → Devices — registry of 10 D/UD keys in `devices_screen.dart` drives a key-picker → type-aware value-editor flow (bool / enum / int / double). Excludes purely-universal keys that have no device-scope semantics. 5 new unit tests pin the registry shape. Mirrored to iOS.
- [ ] Pro native RevenueCat purchase sheet + native donate (`purchases_flutter` mobile Pro flow)
- [x] Full-screen route builder (mobile click-to-place + OSRM snap + save). Shipped initial MVP then completed all six deferred items:
  - **Click-to-place + OSRM snap** — `routing.dart` ports the web's `routing.ts` (snapToRoad + fetchRouteThrough with foot/car profiles). Mode toggle (Trail/Road/Straight); switching modes re-routes through existing waypoints.
  - **Elevation while drawing** — `elevation.dart` ports the web's `elevation.ts` (fetchElevations against Open-Meteo, calculateElevationGain, sampleCoordinates batched at ≤100 points). Re-runs after every successful OSRM fetch; status pill shows `N m ↑`. L4 best-effort — a failed lookup leaves the gain at 0.
  - **MapTiler place search** — `geocoding.dart` wraps the MapTiler Geocoding API. Search field in the AppBar, 300 ms debounce, results dropdown opens under the AppBar; tap a result flies the camera there. Empty-on-error contract (no toast spam if MapTiler is unconfigured / down).
  - **Snap-to-start pulsing marker** — `widgets/snap_to_start.dart` exposes `shouldSnapToStart` (3+ existing waypoints AND tap within 30 m of start). When the route is loop-ready, the start pin shows a green pulsing halo so the user sees the affordance; a tap inside the tolerance closes the loop on the start instead of placing a stub-end.
  - **Draggable marker reshape** — long-press a waypoint to enter drag mode; the next map tap moves that waypoint and re-fetches the snapped polyline. The active pin shows an amber outline + the status pill swaps to "Tap anywhere to move point N" with a cancel chip. Tap-on-map is the move target (vs flutter_map's native drag, which conflicts with map pan) — same UX shape as the web's draggable markers, no new package needed.
  - **Overlap detection (purple out-and-back)** — `route_overlap.dart` walks segment pairs with a haversine endpoint + midpoint test plus a path-distance gap gate so dense polylines don't false-positive. Down-samples to ≤200 points for the O(n²) scan. Retraced segments render as a translucent purple over-stroke on the polyline.
  - **Geolocate auto-center** — `Geolocator.getCurrentPosition` behind a "Locate me" FAB; permission prompt mirrors the recorder's. `locateFn` test seam.
  Twin-mirrored to iOS. Test surface: 12 elevation tests, 7 geocoding, 7 overlap, 5 snap-to-start, 9 builder widget/helper tests — 40 new on top of the 13 from the MVP.
- [x] Auto-link unmatched run to saved route on save — `run_screen.dart`'s stop path now fires `ApiClient.fetchRoutesIntersectingTrack`, filters via `bestStrongRouteMatch` (pure helper in `lib/route_match.dart`, same `startOffsetM + endOffsetM < 200` + `lengthRatio < 0.2` thresholds as the web suggest path), and stamps `runs.route_id` before the save. L4-best-effort — network failure swallowed and logged, never blocks the save. Test coverage: 10 unit tests on `bestStrongRouteMatch` (empty list, zero/negative distance, strict-`<` boundary on both gates, walks-past-borderline, symmetric length diff); 3 wire-level integration tests for `fetchRoutesIntersectingTrack` against real Supabase (overlapping track surfaces the seed route, ocean track returns empty, single-point track short-circuits without RPC call). Twin-mirrored.
- [ ] Map-matched track display (gated on Map-matching deploy under #14)
- [x] Per-point BPM HR-zone view from the phone recorder — `RunRecorder.setHeartRate(bpm)` stamps each `BleHeartRate` sample onto the next constructed waypoint. Out-of-range guard at the recorder (< 30 or > 230 BPM). 2 new unit tests in `run_recorder_test.dart`. The breakdown panel on `run_detail_screen.dart` was already wired against `track[].bpm`; now phone-recorded runs feed it too.

### #7 Workout execution v2

Per `docs/workout_execution.md` deferred section:
- [x] `rewindStep` UI control — `WorkoutRunner.rewindStep()` + leftmost band button (disabled on step 0). 4 new runner tests + 2 new band tests.
- [x] Ghost-pacer marker on the map — pure-geometry helper `ghostPacerPosition` in `apps/mobile_android/lib/widgets/ghost_pacer.dart` walks the planned route's waypoints by `elapsed × target speed`, interpolating lat/lng inside the bracketing segment. `LiveRunMap` gets a new `ghostPosition` field and renders a faint outline-only marker under the live blue dot (no animation, no second pulse — secondary signal only). `run_screen.dart` computes it from the active workout runner's `stepElapsed` + step `targetPaceSecPerKm`. Returns null when the ghost overshoots the path so it doesn't pin to `path.last`. 9 unit tests on the helper (empty/short path, non-positive elapsed/pace, mid-segment, cross-segment, exactly-on-waypoint, coincident-points, returned waypoint carries lat/lng only). 2 widget tests asserting the marker mounts when set and is omitted when null. Twin-mirrored.
- [x] Duration-based steps (v2) — `WorkoutStep.targetDurationSec` (nullable int) toggles the runner's axis. Time-based auto-advance, halfway cue at 50% of elapsed, end-of-step warning at the last 10 s for steps longer than 20 s. `expandWorkoutSteps` reads `duration_s` from any block (warmup / repeats.rep / repeats.recovery / steady / cooldown) and from the generator shape's `recovery_duration_s`. Mixed step types are fine (a distance warmup, time-based reps, a distance cooldown). The execution band renders the time target + seconds-to-go; the post-run review table (mobile + web) swaps km cells for time cells when a row carries `target_duration_s`. Metadata registry: `runs.metadata.workout_step_results[].target_duration_s` is optional and only emitted for duration steps; distance steps omit the key (registered in `docs/metadata.md`). Test coverage: 16 new runner tests (axis toggling, time-based auto-advance, halfway-on-time, last-10s gating, progressFraction, stepRemainingDuration, time-axis 80% adherence threshold, toJson with + without `target_duration_s`, 6 expandWorkoutSteps shape tests including mixed types, both-keys-present preferring distance, and the malformed-block dropout); 2 new band widget tests (header + footer; minute formatting); 4 new review-section tests (duration cells, fromMap reads the new key, isDurationBased toggle, formatStepDuration). Twin-mirrored to iOS.
- [x] Crash-checkpoint resume — `WorkoutRunner.reviewMetadata({required String? planWorkoutId})` is the single source of truth for the workout review trail; both `_saveInProgress` and `_stop` call it. A crash mid-workout now lands on the recovered run (promoted in `main.dart`'s recovery path) with `plan_workout_id` + `workout_step_results` + `workout_adherence` already on metadata, so the planned-vs-actual table renders. The in-progress step shows as `skipped` (honest representation of "user crashed mid-rep"); adherence reads `partial`. Tests: 5 new in `workout_runner_test.dart` (helper contract — empty when planWorkoutId null, three-key shape when active, mid-step → adherence=partial + in-progress step marked skipped, abandoned → abandoned, complete → completed). 2 architecture guards pin the call sites: `_saveInProgress` and `_stop` must each contain `reviewMetadata`. Twin-mirrored. Note: this preserves the *review* through a crash — resuming the live recording session (re-spawn GPS, re-enter the runner mid-step) is out of scope.

### #8 In-app route builder (mobile)

Phase 3 "In-app route builder (free)" — 4 unchecked items in `roadmap.md`. Web has it at `/routes/new`; mobile needs the equivalent. Largely subsumed by Android residuals "full-screen route builder."

### #9 Push notifications + Monetisation

- [ ] **FCM (Android) + APNs (iOS) push notifications.** Phase 4b. **Hard-blocked on platform creds** — to unblock, the operator needs to: (1) create a Firebase project, drop `android/app/google-services.json` into the Android target and `ios/Runner/GoogleService-Info.plist` into the iOS target; (2) enable Apple Push Notification service capability in the Apple Developer portal + generate an APNs auth key, upload it to Firebase; (3) generate `VAPID_PRIVATE_KEY` for the web Push API. Once those are in place, follow-up work is to add the `firebase_messaging` plugin, register tokens to `user_devices.push_token`, write the receive-side handler for the workout-reminder + kudos-on-your-run notification types, and wire the web Push API on `apps/web/src/lib/push.ts`.
- [x] **Mobile Pro purchase flow (scaffolding).** `apps/mobile_android/lib/revenuecat.dart` ports the web's `revenuecat.ts`: `isRevenueCatConfigured` + `configureRevenueCat` + `startProCheckout` + `managementUrl`, all gated on `dotenv.env['REVENUECAT_API_KEY_ANDROID']` / `_IOS`. When the key is absent (every build today — RevenueCat dashboard not yet provisioned), the helpers return `notConfigured` and the settings screen's "Subscribe to Pro" tile falls through to the existing web URL. When the key is set, the same tile presents the native RC sheet via `Purchases.purchase(PurchaseParams.package(...))`. User-cancelled purchases map to `PurchaseResult.cancelled` so no red error toast on a normal dismissal. The donate tile still punts to the web (one-off donations aren't an RC product). **Operator unblock for the live sheet**: create a RevenueCat project, configure the `pro_monthly` package matching the web's "$9.99 / month" copy, enable the iOS + Android API keys in the project secrets pipeline (`REVENUECAT_API_KEY_ANDROID` / `_IOS` in `.env.local` for Android, `dart_defines.json` for iOS), and stand up the `revenuecat-webhook` Edge Function to flip `subscription_tier` on purchase. Tests: 7 unit tests on the wrapper (`isConfigured` flag, `configureRevenueCat` no-ops without a key, `startProCheckout` returns `notConfigured` when unset, `managementUrl` returns null when unset). Twin-mirrored.

### #10 External integrations

Phase 3 "External platform sync" — current state:
- [x] Health Connect (Android) — shipped: `lib/health_connect_importer.dart` uses the `health` package's Health Connect backend; reads workout summaries (no GPS traces — Health Connect doesn't expose them) with per-window avg-BPM. Import screen card + button. Roadmap entry was stale.
- [x] HealthKit (iOS) — shipped via the **same** `health` package which transparently dispatches to HealthKit on iOS. UI is now platform-aware: card title says "Apple Health" on iOS / "Health Connect" on Android, body copy names the platform-typical source apps (Apple Watch / Nike Run Club / Strava on iOS; Google Fit / Samsung Health / Garmin / Fitbit on Android), button reads "Import from Apple Health" / "Import from Health Connect". Pure helpers `healthLabelFor` / `healthCardSubtitleFor` / `healthCardDescriptionFor` cover both branches; 3 new unit tests.
- [x] Strava sync — `ApiClient.syncStrava` already wraps the `strava-import` Edge Function (`action: 'sync'`, returns `{imported, skipped, failed}`). Connect button on Settings → Integrations → "Strava" punts to the web `/settings/integrations` for the OAuth dance; the user pulls-to-refresh on return and the integration row surfaces via `fetchIntegrations`. Disconnect drops the row.
- [x] Strava OAuth (Dart helpers) — `lib/strava.dart` ports the web's `strava.ts`: `isStravaConfigured()` reads `STRAVA_CLIENT_ID` from `dotenv.env` with the same 12345-placeholder anti-stub check the web uses; `stravaAuthUrl(redirectUri)` builds the authorize URL. Both gated so unconfigured builds report unconfigured. 7 unit tests. Mobile-native connect flow (in-app `WebAuth` instead of browser punt) still needs: (1) Strava developer client ID provisioned + added to `.env.local` / `dart_defines.json`; (2) a custom URL scheme registered in `AndroidManifest.xml` + `Info.plist`; (3) deep-link receiver wired to `ApiClient.completeStravaOAuth` (already exists server-side). The helpers + Edge Function are ready; the connect button is the only remaining wiring.
- [ ] Garmin Connect — **hard-blocked** on the multi-day Garmin Developer Program application + OAuth client provisioning. Once approved, follow the Strava pattern: a `garmin.dart` helper module + a `garmin-import` Edge Function + Settings tile wired to OAuth.
- [x] parkrun athlete-number import — already shipped. Settings → Integrations → "parkrun" tile drives `ApiClient.setParkrunAthleteNumber` + `ApiClient.importParkrunResults`.
- [ ] RunSignUp — race-results scrape. Needs an API key from runsignup.com (free for non-commercial use). Once provisioned, follow the parkrun pattern: a `runsignup-import` Edge Function + a Settings tile that ingests results into `runs.metadata.event` / `runs.metadata.position`.

### #11 Platform parity (multi-platform)

- [ ] **iOS — hard-blocked: Mac-only verification.** The byte-identical Dart codebase already supports every Android feature (decisions §39); `parity.md` cells stay ✗/Partial *by design* until each row is Mac-verified on simulator or device. No further code edits from Linux move these cells. Remaining gates: `Runner.entitlements` (HealthKit + Sign-in-with-Apple), `pod install`, Apple Developer Portal Sign-in-with-Apple Services ID + APNs setup. Info.plist itself is already complete (`NSHealthShareUsageDescription`, `NSBluetoothAlwaysUsageDescription`, `NSMotionUsageDescription`, `NSPhotoLibraryUsageDescription`, `UIBackgroundModes`, `BGTaskSchedulerPermittedIdentifiers`).
- [ ] **Apple Watch — hard-blocked: Xcode / watchOS device required.** Route nav visuals, ultra-length stress test, live race participant, complication target wiring (Xcode Widget Extension), activity types, lap markers, hold-to-stop, TTS cues, pedometer, GPS self-heal, indoor mode, route picker UI, BLE pairing UI — each one needs to be wired in Xcode + verified on a watchOS simulator or paired device. Linux box cannot reach this surface.
- [x] **Wear OS universal-prefs consumers — complete to the limit of "fetch only what we render".** Every key with a wrist-side consumer is now wired end-to-end. Shipped slices:
  - `default_activity_type` → pre-run picker chip is primed on session restore (guarded against clobbering a manual choice or in-progress run).
  - `privacy_default` → cached on a `@Volatile` field; `handleFinishedRun` snapshots `snapshotIsPublic()` onto `QueuedRun.isPublic` so an in-flight queued upload keeps its visibility under a later pref toggle; `SupabaseClient.saveRun` writes `is_public` only when the snapshot is non-null.
  - `hr_zones` + `max_hr_bpm` + `date_of_birth` → resolved into a 5-cutoff array by `resolveZoneCutoffs` (priority: explicit `hr_zones` → 60/70/80/90/100% of `max_hr_bpm` → 60/70/80/90/100% of (220 − age from DOB)) and surfaced as a live "Z3" badge next to BPM on `RunningScreen` via `hrZoneOf(bpm, cutoffs)`. Falls through to bare "146 bpm" when none of the three signals are set.
  - `resting_hr_bpm` → parsed + cached but not yet consumed in a live UI. The plumbing is ready for the eventual TRIMP / Karvonen pace-adjusted zone work; parity cell stays `Partial` until a wrist consumer ships.
  Test surface: 30 unit tests across the parser (every field validated independently: empty body, null prefs, missing key, range gates for resting/max HR, strict-ascending + range gates for hr_zones, ISO-date shape gate, two-field independence, allowlist sync to docs) + the zone helpers (per-band mapping with boundary checks, null cutoffs, malformed cutoffs, all three priority paths in `resolveZoneCutoffs`, age math against an injected `nowMs`). `./gradlew compileDebugKotlin test` clean. No editor on the wrist by design (`apps/watch_wear/CLAUDE.md`'s "don't build settings panels on the wrist" rule); edits stay on the phone / web.

### #12 Backend Go service (Phase 2/3)

The job-worker half is **shipped + deployed** (map-match jobs draining via `claim_next_job` on Fly.io with the OSRM matcher behind a sibling app). The **live spectator hub is now wired in code** but not yet deployed:

- [x] **Live spectator WS hub (Go code)** — `apps/job_worker/internal/livehub/`. In-process `Hub` with `Publish` + `Subscribe`, served via three HTTP routes mounted on the existing `/health` listener: `POST /v1/live/{run_id}/push` (recorder pushes a `Ping` body), `GET /v1/live/{run_id}/snapshot` (last-known position as JSON, 204 when room empty), `GET /v1/live/{run_id}/subscribe` (WebSocket stream via `coder/websocket v1.8.14`). Late joiners receive the room's last-known ping immediately on subscribe so a spectator sees the runner without waiting for the next 5 s broadcaster tick. Per-subscriber 8-slot buffer + `trySend`-with-mutex serialises sends against close (race-clean under `-race`). Idle-proxy ping every 25 s; `CloseRead` detects peer-close so unsubscribe is prompt. Room GC keeps last-known for ungated refreshes; subscriber cleanup is exercised by a leak test. Auth left permissive (`Server.Authorizer = nil`) with a precise TODO recipe in `server.go` for the Supabase JWT verifier (recorder must own the run for push; spectators must own the run OR `is_public=true`). `LIVEHUB_ALLOWED_ORIGINS=https://run.app,https://preview.threkir.com` enforces the WS upgrade origin check in production. Tests: 10 hub unit + 10 server integration (httptest + WS client). `go test -race ./...` clean.
- [ ] **Live hub deploy to Fly.io** — code is ready, just needs the `fly.toml` for the live-hub HTTP port + an upstream nginx / CloudFront route that maps `live.threkir.com/v1/*` to the Fly app.
- [x] **Upstash Redis swap** — shipped. `apps/job_worker/internal/livehub/iface.go` defines `LivePubSub`; both the in-process `Hub` and the new `RedisHub` (`redis_hub.go`) satisfy it. `Server` + `JWTAuthorizer` now take the interface, so `main.go` picks between them at boot via `REDIS_URL`. Storage model: `live:{runID}:ch` for pub/sub fan-out + `live:{runID}:last` for the last-known snapshot with a 24h TTL (matches the roadmap). Per-process in-memory caches for `LoadZones` + `LoadRunMeta` (per-room, idempotent — each replica fetches at most once per run; the cached value lives for the room's lifetime within that process). Subscriber buffer is the same 8-slot bound as the in-process variant so a stuck spectator can't grow unbounded memory. Test surface: 14 new tests in `redis_hub_test.go` against an embedded `miniredis` — Publish writes `:last`, LastKnown returns nil for absent runs, TTL expiry verified via `mr.FastForward`, Subscribe receives, pre-load fires for late joiners, unsub closes the channel, two subscribers both receive, zones + run-meta both cache per-room (single fetch across 10 calls, negative result also cached), `LivePubSub` compile-time assert, `ConfigureRedis` parses good URLs + rejects bad. **Cutover (operator)**: set `REDIS_URL` (Upstash/Fly Redis/etc.) as a Fly secret; boot log will read `livehub: backend=redis (multi-replica fan-out)`. Unset → falls back to in-process Hub. `apps/job_worker/CLAUDE.md` + `fly.toml` document the secret.
- [x] **Supabase JWT authorizer** — `apps/job_worker/internal/livehub/auth.go` ships `JWTAuthorizer` (HS256 over `SUPABASE_JWT_SECRET`, classic alg-confusion defence via `WithValidMethods([]string{"HS256"})`). Push is owner-only with no anon path even on public runs; subscribe/snapshot are anon for `is_public=true` and owner-only otherwise; unknown run ids deny. The `(user_id, is_public)` lookup goes through `Hub.LoadRunMeta` so the hot publisher's per-5s push is a single map hit after warm-up. Wired in `main.go` — when `SUPABASE_JWT_SECRET` is unset the authorizer is nil (permissive dev mode) and the boot log warns; when set the boot log confirms `livehub auth: enabled (Supabase JWT)`. Test surface: 16 unit + 1 end-to-end (`auth_test.go`) covering owner-allowed, non-owner-denied, expired-token denied, tampered-signature denied, wrong-secret denied, alg:none denied, unknown-run denied, fetcher-error denied (fail-closed), public-run anon allowed, private-run anon denied, run-meta caching (1 fetch across 10 calls), and HTTP-level 403/202 round-trips. `go test -race ./...` clean.
- [x] **Strava webhook migration** — shipped. HTTP endpoint at `POST /v1/strava/webhook` (`apps/job_worker/internal/stravahook/server.go`) handles GET subscription handshake + POST event validation; the POST path body-caps at 4 KiB, rejects unknown fields, gates by URL secret + verify token (timing-safe), filters non-activity + non-create events, runs the 7-day freshness window, dedupes via `webhook_events`, then enqueues a `kind='strava_event'` job and acks 200 — usually well under 100 ms. The new `strava_event` job dispatch (`apps/job_worker/internal/handler_strava_event.go`) does the ingest async: integration lookup, proactive token refresh when expiry is within 5 min, Strava activity fetch (rate-limit branch defers + rolls back the dedupe row), sport-type filter, runs row insert, gzipped track upload, `track_url` PATCH. Matches the EF row shape byte-for-byte (same `metadata.strava_id`, same source='strava', same `activity_type` derivation) so the two paths share dedupe during cutover. The `jobs.kind` CHECK allowlist (migration `20260823_001`) admits the new kind; the pgtap suite gained a 6th test. **Test surface: 23 new tests** — 10 handler (non-create skip, unknown athlete, already-imported, happy-path insert + upload, ride filter, rate-limit defer with dedupe rollback, no-Strava permanent fail, proactive refresh, 3 BuildTrackFromStreams cases) + 13 stravahook server (handshake echo, bad/missing/wrong secrets, missing config 503, happy-path enqueue, non-activity skip, non-create skip, freshness reject, dedupe second-post, body-shape errors, method-not-allowed, enqueue-error 500). `go vet ./... && go test -race -count=1 ./...` clean. **Cutover (operator)**: set `STRAVA_WEBHOOK_SECRET` + `STRAVA_VERIFY_TOKEN` Fly secrets, then re-register the Strava subscription via `POST /api/v3/push_subscriptions` pointed at `https://live.threkir.com/v1/strava/webhook?secret=...`. The EF stays deployed for rollback; `apps/backend/CLAUDE.md` already marks it Deprecated. See `apps/job_worker/deployment.md § Cutover recipe for the Strava webhook`.
- [x] **Token refresh worker** — `kind='token_refresh'` lands in `Worker.dispatch` (`apps/job_worker/internal/handler_token_refresh.go`). Sweeps `integrations` rows where `provider='strava'` and `token_expiry < now() + 1h`, decrypts the Vault refresh token via `get_integration_tokens` RPC, POSTs to Strava's `/oauth/token` (form-encoded, `grant_type=refresh_token`), writes the new pair back via `set_integration_tokens`. One bad row never fails the whole sweep — Strava 4xx/5xx on a single user is logged + skipped (the job still finishes `done`); `FetchExpiringStravaIntegrations`-level errors propagate (5xx → defer/retry, 4xx → fail-permanent). Pluggable `StravaRefresher` seam — production wires `*StravaClient` with `client.HTTP` (pooled), tests substitute a fake. Worker now drains any kind (`ClaimNextJob` with empty `kind_filter`) so map_match and token_refresh share one drain loop. **Test surface: 12 new tests** — 8 token-refresh handler scenarios (no-op, multi-row rotate, Vault-missing skip, single-row Strava 4xx/5xx skip-but-continue, fetch-error transient defer, no-client permanent fail, dispatch unknown-kind) + 4 Strava client tests (form encoding, HTTPError surface, incomplete-response rejection, default URL). `go vet ./... && go test -race -count=1 ./...` clean. **Cutover (operator)**: add `STRAVA_CLIENT_ID`/`SECRET` as Fly secrets — the pg_cron schedule that enqueues the hourly job ships in migration `20260821_001_token_refresh_cron.sql` (verified against the local stack: cron is registered, dedupe SQL coalesces multiple ticks onto a single backlog row, fresh enqueues resume after the worker drains). Both paths (EF + Go) can coexist; whichever runs first wins. `apps/backend/CLAUDE.md`'s Edge Functions table flips `refresh-tokens` to **Deprecated** (rollback path only). See `apps/job_worker/deployment.md § Job kinds + cutover`.
- [x] **Data export** — shipped as an HTTP endpoint on the Go service rather than a job kind (the user is waiting on a signed URL, so async-via-queue would have meant a UX rework). New package `apps/job_worker/internal/dataexport/`. `POST /v1/export` takes a Bearer JWT (HS256 over `SUPABASE_JWT_SECRET`, same path as the live hub's authorizer), runs the tiered rate-limit RPC (`check_rate_limit_tiered`, free 2/h + pro 8/h, fail-closed on RPC error), fetches up to 5000 runs with the same projection the EF used, builds CSV (column order pinned, `track_url` deliberately omitted per the prior audit) or GPX zip (manifest + per-run GPX with `gpxtpx:hr` extensions, XML-escaped titles), uploads to `runs/{user_id}/exports/<ts>.{csv,zip}` with `upsert=false`, and returns a 10-min signed URL — same response shape as the EF (`{url, expires_in, count, format}`). Body shape unchanged (`{format:'csv'|'gpx'}`). New SupabaseClient methods: `CheckRateLimitTiered`, `FetchExportRuns`, `UploadExportArtifact`, `CreateSignedURL`. **Test surface: 14 tests** — 9 httptest (missing JWT 503, method-not-allowed, missing bearer 401, expired token 401, bad format 400, rate-limited 429 with Retry-After, fail-closed on RPC error, happy-path CSV upload + sign, happy-path GPX-zip with valid Content-Type) + 5 pure builder/helpers (CSV column shape + track_url omission, GPX-zip manifest + per-run files, GPX document shape, XML escape of titles, gzipped-track decode round-trip). `go vet ./... && go test -race -count=1 ./...` clean. **Client-side wiring shipped (2026-05-11)** — `apps/web/src/lib/cloud_export.ts` + `cloud_export_helpers.ts` add the `cloudExport(format)` call site. Transport flips on `PUBLIC_EXPORT_HUB_URL`: set → `POST ${hub}/v1/export` on the Go service with the user's JWT; unset → `supabase.functions.invoke('export-data', ...)` on the legacy EF (same body / response shape). A new **"Cloud export (GPX zip)"** button on `/settings/account` exercises it — produces a server-built zip with one GPX-with-HR-extensions file per run (up to 5,000), bypassing the in-browser memory ceiling the existing client-side CSV / JSON buttons run into, and the only path that emits proper GPX-with-HR that imports into Strava / Garmin / Komoot. Free tier: 2 exports/hour; Pro: 8. **Test surface: 7 tests** — 5 unit (`cloud_export_helpers.test.ts`: URL building + trailing-slash normalisation + body shape per format) + 2 Playwright (success → fetches the EF, opens the signed URL in a new tab, surfaces a `"Export ready (N runs)"` toast; failure → 500 from EF surfaces as `"Export failed: ..."` toast). **Operator cutover**: set `PUBLIC_EXPORT_HUB_URL=https://live.threkir.com` in the prod sops blob to flip the button from EF to Go. EF stays deployed for rollback per `apps/backend/CLAUDE.md`'s Deprecated marker.
- [x] **Premium Go endpoints** — shipped. New package `apps/job_worker/internal/premium/` mounts four Pro-only HTTP endpoints on the existing `/health` listener: `POST /v1/premium/vo2max` (Daniels VDOT from the user's best qualifying run in the last 90 days), `POST /v1/premium/race-predictor` (Riegel from the best effort to a target distance, configurable exponent), `POST /v1/premium/recovery` (90-day EWMA CTL/ATL/TSB + advice string), `POST /v1/premium/training-plan` (Riegel-derived pace targets + phased weekly mileage for `distance_5k`/`distance_10k`/`distance_half`/`distance_full`/`custom`). Shared `wrap()` does method-gate → JWT extract (HS256 over `SUPABASE_JWT_SECRET`, same alg-confusion defence as the live hub) → Pro tier check via `user_profiles.subscription_tier` (`pro` + `lifetime` count; anything else falls back to free → 402). Pure-compute helpers ported from `apps/web/src/lib/fitness.ts` (`vdotFromRun`, training-load EWMA, recovery-advice bands) and `apps/web/src/lib/training.ts` (Riegel + plan generator). TRIMP-based stress is stubbed at the distance proxy (10 stress per km) until `resting_hr_bpm`/`max_hr_bpm` thread through the Backend interface. **Test surface: 36 tests** — 16 pure-compute (VDOT known values, Riegel known + defaults + zero inputs, EWMA single-impulse + taper-raises-TSB + empty-zero + daily aggregation, RecoveryAdvice all 6 bands, plan shape for 10k/marathon/half, week-count clamp, stressOf zero) + 20 httptest (no-JWT 503, GET 405, missing bearer 401, invalid/expired/wrong-key tokens 401, free tier 402, unknown tier falls back to free, lifetime accepted, tier lookup 500, VO2Max happy path + no-qualifying 404 + runs error 500, race-predictor happy + custom-exponent + missing-target 400 + bad-JSON 400 + no-qualifying 404, recovery happy + empty zeros, training-plan happy for 10k + defaults for half + custom distance + missing recent_5k 400 + missing goal_event 400 + unknown goal_event 400 + custom-without-distance 400). `go vet ./... && go test -race -count=1 ./...` clean. **Cutover (operator)**: the endpoints come up automatically when `SUPABASE_JWT_SECRET` is set — same secret the live hub already uses. Web/mobile clients flip from the Edge Function URL to the Go endpoint via the same flag scheme used by the live hub / data export.

### #13 Live spectator WebSocket

Client wiring shipped — both ends now have a hub-or-Supabase transport switch gated on env. The hub itself is in-process today (`apps/job_worker/internal/livehub/`, see #12); the swap to Upstash Redis is a Hub-internal change with no further client work.

- [x] **Mobile recorder hub path** — `apps/mobile_android/lib/live_hub_client.dart` is the HTTP wrapper around `POST /v1/live/{run_id}/push` (dart:io HttpClient + pluggable fetcher seam for tests). `LiveBroadcaster` now takes an optional `hubClient`; when `LiveHubClient.isConfigured` is true (deploy sets `LIVE_HUB_URL` in `dotenv.env`), pings route through the hub instead of Supabase `live_run_pings`. Falls back to the legacy `ApiClient.insertLivePing` path otherwise so the feature stays usable every build pre-deploy. Twin-mirrored. 13 new unit tests (9 hub-client URL building + encoding + status code + error path, 4 broadcaster transport selection including L4-swallow contract).
- [x] **Web spectator hub path** — `apps/web/src/lib/live_hub.ts` (env-coupled module) + `live_hub_helpers.ts` (pure URL + backoff math). `openLiveWebSocket(runId, {onPing, onStatus})` opens a browser `WebSocket` to `${PUBLIC_LIVE_HUB_URL}/v1/live/{run_id}/subscribe` with auto-reconnect (exponential backoff capped at 30 s). `fetchLiveSnapshot(runId)` covers the late-joiner case via `GET /v1/live/{run_id}/snapshot`. Spectator page (`/live/[id]/+page.svelte`) gates the entire subscribe + hydrate flow on `isLiveHubConfigured()` — falls back to the Supabase Realtime channel + table backlog when unset, so existing builds keep working. 7 unit tests on the pure helpers (URL building, http/ws scheme flip, run-id encoding, backoff math).
- [x] **Server-side privacy-zone clip on the Go hub** — `apps/job_worker/internal/livehub/privacy.go` ports the haversine `IsInAnyZone` check from `apps/web/src/lib/privacy.ts` + `apps/mobile_android/lib/privacy.dart`. `Server.shouldDrop` runs it on every `/push` against the broadcaster's cached zones; in-zone pings are dropped silently (response carries `{ok:true, clipped:true, subscribers_sent:0}`) before fan-out. Zones are fetched once per room via `SupabaseZoneFetcher` (two PostgREST queries: `runs?id=eq.<id>&select=user_id` → `user_settings?user_id=eq.<owner>&select=prefs`) and cached on the room until GC. **Fail-closed on zone-fetch errors** — a Supabase outage drops the ping rather than risk leaking through a broken fetch (response carries `{ok:false, clipped:true, reason:"zone fetch failed"}`). Mirrors the `live_run_pings_drop_in_zone` BEFORE-INSERT trigger on the Supabase Realtime path so both transports honour the same contract. Wired in `main.go` to the worker's existing service-role HTTP client. Test surface: 8 privacy unit tests (radius boundary, multiple zones, empty zones, 1 m east haversine reference) + 5 server integration tests (in-zone drop, out-of-zone pass-through, per-room cache hit, fail-closed on fetch error, nil-fetcher dev pass-through). 34 livehub tests total, race-clean.
- [ ] **Hub deploy + env flip — config + docs ready, awaiting `flyctl deploy --remote-only` from operator.** Config done: `apps/job_worker/fly.toml` now exposes the hub HTTP/WS on `:443` (TLS termination at Fly's edge), declares `LIVEHUB_ALLOWED_ORIGINS` for the production + preview hosts, and adds the `/health` HTTP probe. `apps/web/.env.example` documents `PUBLIC_LIVE_HUB_URL`; `apps/mobile_android/.env.example` documents `LIVE_HUB_URL`. `apps/job_worker/deployment.md § Live spectator hub` covers the public surface, DNS recipe (`flyctl certs add live.threkir.com` + Route 53 record), env-flip recipe (sops blob for web, mobile build envs for Android/iOS), and the privacy-zone fail-closed contract. Operator-only remaining steps: provision Fly app + run `flyctl deploy`, add the `live.threkir.com` DNS record, set `PUBLIC_LIVE_HUB_URL` in the prod sops blob, set `LIVE_HUB_URL` in the next mobile release builds. The existing client wiring picks up the hub transport on env flip; no further mobile/web code change needed.

### #14 Map matching + Protomaps + parity audit (Future)

- [ ] **Map matching deploy:** OSRM alongside Supabase, OSM extract refresh pipeline, auth endpoint, return matched geom to mobile, raw-vs-matched toggle, offline fallback. Engine choice + trigger wiring already shipped; deploy is what remains.
- [ ] **Protomaps self-hosted tiles** — all 4 items from roadmap.md "Future" section.
- [x] **Cross-platform parity periodic audit in CI** — new `parity-matrix` CI job runs `scripts/check_parity_matrix.dart` on every PR. Validates row column count, legal cell values (`✓ / ✗ / Partial / N/A` only), the iOS-ahead-of-Android impossibility (decisions §39), and Partial-without-Notes. Caught 3 real drift bugs on first run (Garmin row with stray `|`, `"✓ (read-only)"` non-standard cell, empty-Notes Partial); all fixed. Cross-client integration test in CI is still open.

### #15 Competitor backlog + SEO route pages

From `roadmap.md` "Competitor-parity backlog (unphased)" — each item carries the rough size estimate from the roadmap table so future sessions can scope them realistically. None are blocked; they're all sized as discrete multi-day-to-multi-week features that need product green-lighting before they're picked up:
- [ ] Heatmap / popular-route discovery (#4, **~2 wk**) — materialised tile table or a Go tile service; anonymised aggregation. Open product decision: opt-in vs opt-out privacy default.
- [ ] Trail / offline navigation (#5, **~3-4 wk**) — turn-by-turn nav on a loaded route + offline tile packs + condition reports. Needs routing-engine choice (Valhalla vs GraphHopper for turn cues).
- [ ] Gear tracking (#7, **~1 wk**) — `gear` + `run_gear` (link table), mileage per item, retirement reminders. Smallest of the open features; v1 manual-only.
- [ ] Audio-coached runs (#9, **~3-4 wk**) — pre-recorded workout library, TTS-narrated pace cues. Audio CDN strategy + voice-talent budget needed.
- [ ] Race calendar + results import (#10, **~2 wk**) — event discovery + entry links + auto-match results when you record. Imports from RunSignUp + parkrun; gated on RunSignUp API key (see #10 above).
- [ ] Advanced analytics polish (#11, **~2 wk**) — no new tables; richer dashboard breakdowns + race-time predictor over what VDOT / training-load already ship.
- [ ] Premium billing extensions (#12, **~1-2 wk**) — Stripe Checkout + customer portal + paywall enforcement across web + mobile.
- [ ] Treadmill BLE FTMS (#13, **~3-5 d, mobile-only**) — real-time speed / distance / incline from a paired treadmill. Spec in `integrations.md § Treadmills (BLE FTMS)`. Needs hardware-in-the-loop testing on Android + iOS.
- [ ] SEO-indexed public route pages (Phase 3 "Community route library"). **Crawler scaffolding shipped**: `apps/web/static/robots.txt` allows the public surfaces (/, /share/run/<id>, /share/route/<id>, /u/<id>, /feed) and disallows auth-gated paths to save crawl budget; `/share/route/[id]` + `/share/run/[id]` carry the full Open Graph + Twitter card surface (og:title / description / type / site_name / image + twitter:card=summary_large_image / title / description / image), with og:image pointing at the static `apple-touch-icon.png` for the v1 unfurl. Route share gets reactive title/description from the loaded route; run share keeps generic copy because the data fetch lives inside `RunShareView` (the feed modal mounts the same component, so lifting it out is a larger slice). E2e tests pin both surfaces — `share/route.spec.ts` checks the 10 meta fields interpolate the route name + 10.0 km + surface, `share/run.spec.ts` pins the generic title and the scaffolding tags. **Sitemap shipped (2026-05-11)** — `apps/web/src/routes/sitemap.xml/+server.ts` prerenders a build-time XML sitemap from `public_routes` + `public_runs`. `prerender = true` so adapter-static bakes the file into the build (CloudFront serves it straight from S3 — no Lambda per crawl). Defaults to `https://threkir.com` for the canonical base; preview overrides via the new `PUBLIC_SITE_URL` env var. Per-entry shape: top-level surfaces (/, /feed, /routes?tab=explore) first with priorities 1.0/0.5/0.6, then every public route + run as `/share/{type}/{id}` with `lastmod` (routes use `updated_at`, runs use `started_at` because `public_runs.updated_at` is intentionally stripped per migration `20260807_001` to avoid leaking last-edit cadence). Fail-soft on Supabase fetch error (build still ships with the top-level-only sitemap). `robots.txt` now advertises the sitemap via `Sitemap: /sitemap.xml`. **Test surface: 20 tests** — 16 unit (pure XML builder: xmlEscape, normaliseBase, buildSitemap shape + escaping + optional-field omission, composeEntries top-level order + base trailing-slash normalisation + lastmod fallback to started_at + priority/changefreq per entry type, end-to-end well-formedness) + 4 Playwright (200 + application/xml content-type + valid urlset doc shape, top-level surfaces present, seeded public run + route appear, robots.txt advertises sitemap location). `npm run check` clean; build emits a 205-URL sitemap against the seed (~3 routes + ~12 public runs + 3 top-level). **Run share lifted to +page.ts + per-run prerender (2026-05-11)** — `apps/web/src/routes/share/run/[id]/+page.ts` now ships `entries()` + `load()` against `public_runs`, with `prerender = true`. `entries()` lists up to 5,000 public run ids at build time; `load()` does a thin `id,distance_m,duration_s,started_at,source` fetch for that single row. The +page.svelte uses `$derived` against `data.run` to bake per-run `<title>` + Open Graph + Twitter tags into the prerendered HTML — `16.2 km run on 17 Nov 2025 — Threkir` instead of the generic SPA-shell fallback that ate every Slack / FB / Twitter unfurl. Display name is still **not** in the title — `user_profiles` is owner-only by RLS (only policy is `auth.uid() = id`), so a build-time anon fetch can't see it; the right unlock is a new `public_profiles` view + grant to anon (separate slice). Stable formatting: pure helpers in `apps/web/src/lib/share_meta.ts` (`formatKmStable`, `formatDateStable`, `buildRunShareTitle`/`Description`, `buildRouteShareTitle`/`Description`) — viewer-independent (km only, UTC en-GB date) so the wire is identical for every crawler / locale. **Test surface: 22 tests** — 18 unit (`share_meta.test.ts`: distance ranges, marathon two-decimal, date UTC pin, null fallback, all four title/description builders) + 4 Playwright update (the existing `share/run.spec.ts` SEO test now asserts the per-run title regex `\d+(\.\d+)?\s+km run on \d+\s\w+\s\d{4}` + `og:type = article` + every other og/twitter tag still present; the other 5 share/run tests stay green). Build emits 199 prerendered `share/run/<id>.html` files for the seed (one per public run). **Route share + public_profiles shipped (2026-05-11)** — `/share/route/[id]` got the same `entries()` + lifted-load treatment as `/share/run/[id]`: prerender = true, build-time anon fetch of every public route's id, thin `name + distance_m + surface + elevation_m` projection bakes into a per-route `<title>` (e.g. `"E2E demo public route — Threkir"`) on the static HTML. Body keeps the existing owner-aware `fetchRouteById` onMount path so the route polyline still differs per-viewer (full for owners / club members; server-clipped for anon). `apps/backend/supabase/migrations/20260824_001_public_profiles_view.sql` adds a `public_profiles` view (id + display_name + avatar_url) granted to `anon` so the prerendered `/share/run/[id]` title can attribute the run — `"11.1 km run by Jared Howard on 11 Dec 2025 — Threkir"`. Anti-enumeration: no listing path (callers must already hold a uuid from public_runs / public_routes); same privacy posture as the existing /u/[id] surface. `share_meta.ts` builders gained an optional `displayName` param with null-safe fallback; 5 new unit tests cover the attributed shape. Updated e2e regex pins the `by NAME` segment. `docs/api_database.md` documents the new view. **Per-route og:image PNG shipped (2026-05-11)** — `apps/web/src/routes/og/route/[id].png/+server.ts` prerenders a 1200×630 PNG per public route at build time. The pure SVG builder lives in `apps/web/src/lib/og_route_image.ts` (brand strap + projected polyline reusing `projectTrack` so privacy-zone clipping still applies + green-start / red-end caps + title + km · surface meta line). `@resvg/resvg-js` rasterises to PNG bytes — pure JS binding, no native-build pain. `entries()` lists every public route id (same shape as the share/route prerender) so build emits `build/og/route/<id>.png` per route (~20 KB each). `share/route/[id]/+page.svelte` now points `og:image` (+ width/height + twitter:image) at `/og/route/{data.id}.png` so Slack / FB / Twitter / LinkedIn unfurls carry a real track preview instead of the generic favicon. **Test surface: 19 tests** — 14 unit (`og_route_image.test.ts`: viewBox/dimensions, brand strap + name rendering, polyline path emission, start/end cap colours, <2-point fallback to title-only, distance + surface meta, null-name fallback, XML escape, helpers `buildMetaLine` + `truncate` + `xmlEscape`) + 5 Playwright update (existing share/route SEO spec gained per-route image url + width/height + twitter:image assertions; new spec asserts the endpoint returns 200, image/png, valid PNG magic bytes, >1 KB body). **Remaining under #15**: none on the SEO route pages slice — all three deferred sub-items (sitemap, run-share lift, route-share lift, public_profiles for display_name, og:image PNG) have shipped.
